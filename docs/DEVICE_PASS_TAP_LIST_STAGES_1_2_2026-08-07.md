# DEVICE PASS — STAGES 1+2 (L10) — the gate main waits on — 2026-08-07

Seat-authored from STAGE_B_STAGES_1_2_SCREEN_INPUT_2026-08-07.md.
Install OVER (no delete). Same build command as always.

## RESULT — PASSED. L10 for stages 1+2 is MET on Sam's word (2026-08-07)

Sam ran the pass on his device. Recorded per tap:

| tap | result |
|---|---|
| 1 — recovery slot is a short flush | **CORRECT** |
| 2 — conditioning names, dose words, Warm-up row, no cool-down | **CORRECT** |
| 3 — rest-marked day holds nothing | **CLEAN** |
| 4 — out-of-block coach override still renders | **N/A** — no coach override exists in Sam's current state, so the tap had nothing to exercise |
| 5 — general glance across the week | **PROGRAMMING-QUALITY FINDINGS**, filed to docs/COACHING_QUALITY_EXHIBITS_2026-08-07.md |

**Tap 5 is not a gate failure, and the seat ruled it cleanly.** This gate judged
MECHANICS — does the landed stage-1+2 behaviour render as declared. Mechanics
passed. What tap 5 surfaced is PROGRAMMING QUALITY (session composition,
ordering, density), which Sam explicitly deferred in the same breath: *"move on
and get all the other shit in, then we can optimise the coaching cause a lot of
it is pretty bad."* The exhibits are filed so the future coaching-quality unit
starts from real screens rather than from memory — they are not this gate's
scope and they do not hold the merge.

**Nothing renamed, nothing doubled. No mechanics finding on any tap.**

Consequence, as this doc's own last line states: **stage-b-stage2 merges to
MAIN.**

## Five taps

1. Open a day holding a RECOVERY slot → it's a short flush: 10–20 min
   continuous easy, 3–4/10, "finish better than you started" wording.
   (A 30–50 min steady run at %MAS there = finding.)
2. Open any conditioning session → the NAME is one of your 55 signed
   names, dose words exactly as your workbook says, a Warm-up row on
   top, NO cool-down row, your effort cue on the row. A composed or
   machine-suffixed name like "— Assault Bike" = finding.
3. Mark a day as Rest (or open one already marked) → the day holds
   NOTHING. No stowaway sessions.
4. If you have any coach override outside the current block → it still
   renders on its day.
5. General glance across the week → nothing renamed, nothing doubled.

## Already known — seeing these is NOT a finding

- Off-season block-2 deload reshapes instead of shrinking (declared,
  fix deliberately reverted — it broke pre-season generation).
- A lighter day can be re-filtered by a LATER injury (behaviour delta,
  recorded).

## Honest edges

- Button wording was read from code, not tapped — if a button label
  differs slightly from this list, the list is wrong, not the app.
- If your app is currently in a bye / bye-recovery / deload / optional
  / illness week: that ground is untested by machine — findings there
  are extra valuable, but judge gently.

Findings: tap number + what you saw. One pass. Pass = stage-b-stage2
merges to MAIN.
