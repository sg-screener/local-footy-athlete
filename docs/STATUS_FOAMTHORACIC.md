# Foam Roller Thoracic Extension — 2 September 2026

Owner: `foamthoracic`

## Boundary

Add Sam's supplied Foam Roller Thoracic Extension as one ordinary exercise in
the existing Mobility catalogue. Do not create a new equipment answer, loading
system, session type or exercise-specific selector.

## Design choice

Two options were checked:

1. Add a special beginner/loading branch just for this exercise.
2. Use the existing mandatory-support plus optional-loading contract already
   used by the catalogue: the foam roller remains required, the dumbbell or
   plate is the normal loading choice, and the named unloaded regression keeps
   the movement available without inventing a second exercise identity.

Option 2 is used. `Barbell and plates` is the existing answer that proves a
weight plate is available. Athlete-chosen load records the one held object's
total kilograms and never invents a starting weight.

## Red-first receipt

The focused exercise-intake run reached 743 passing cells and 41 failing cells
before production data was added. The failures named the missing Foam Roller
Thoracic Extension dose, catalogue identity, thirteen injury ratings, muscles,
equipment, cues, video, loading authority, automatic routes and durable manual
Add/Swap path. The instrument counted assertion cells; 14 distinct submitted
exercise identities were in scope.

## Verification

- Focused exercise-intake: 791 passing assertion cells, 0 failing, across 14
  distinct submitted exercise identities.
- Four isolated mutations were killed: dose, equipment choice, load authority
  and automatic Primer exclusion.
- The real builders selected Foam Roller Thoracic Extension through upper-body
  Movement Prep, Mobility and Recovery. The real Add/Swap transaction saved it,
  preserved its dose, accepted total kilograms and reconstructed the same row
  after restart.
- Equipment vocabulary: 95/95. The existing foam-roller, dumbbell and
  barbell-and-plates answers remain sufficient; no question was added.
- Muscle/workbook equality: 97/97 over 211 distinct workbook rows. The new row
  is `Exercise Master!A218:H218` and the everyone-gate count is 144.
- Content reconciliation: 20/20. Recovery template: 39/39. Primer: 25/25.
  Mobility door: 33/33. Load rendering: 30/30. Exercise Bible library: 164.
- Workbook inspection found 0 formula-error cells. Its one sheet was rendered
  before and after the edit; the new row matches style 5 with wrapping enabled.
- Product and developer-tool compilation each report 0 errors. The test-only
  compiler still reports the same 7 errors in
  `sessionWorkOwnershipJourneyTests.ts`.
- Law registry structure and the new guarded row pass. Its overall result is
  13/14 because 21 of 256 older laws remain `UNENFORCED`; 235 are guarded.
- The authored-cue suite remains 54/56 because Tib Raises and Copenhagen Plank
  (Half) retain their unrelated existing load-authority gaps. The general
  strength-pool suite remains 476/480 on four unrelated squat-selection cells.

## NOT COVERED

- Independent clinical validation of Sam's supplied ratings and restrictions.
- Physical-iPhone acceptance or external video playback.
- Unrelated existing red suites outside the focused exercise surface.
