# R5.3 — LEG (v) RE-SCOPED, BUILT ON THE SCAFFOLD, PRICED: THE READ HALF IS FREE, THE UNIT IS NOT (2026-08-07)

LOOP CHECK: green-claimed-from-a-printed-tail-instead-of-the-exit-line —
harness-lies sighting 5, and it is mine — COMPRESS, and the compression is
shipped in this commit rather than written down as a reminder. I ran the six
witness suites as a hand-rolled shell loop reading `npm run … | tail -3`,
reported **six-for-six green**, and the full sweep — which reads EXIT CODES —
found four of those six still red. The suites printed a green-looking totals
line and exited 1 on cells further up. The repo already had the law (gate.sh:
trust only printed exit lines); what it lacked was a way to run a SUBSET under
that discipline, so the shortcut got hand-rolled. `SWEEP_SUITES` now makes a
witness batch the same instrument as a full sweep.

Twentieth pass. Answers inbox item 1 (a), (b), (c) and the first half of (e).
**NOTHING BUILT into the branch** — the arm lives flag-gated on
`scratch/r53-legv-readers` (`d91d23fc`), inert by default.

## THE HEADLINE

| step | result |
|---|---|
| the re-scoped leg (v) | **BUILT on the scaffold**, both halves flag-gated |
| (a) does the read half work? | **YES, and it is FREE** — 0 new, 0 fixed, control set EXACTLY |
| (a) does the unit land? | **NO — 8 new reds of 156.** STOP under standing conditions |
| how much of the nine does the read half pay? | **2 of them, not six.** My "six" was the tail-read |
| (b) the two unexplained | **STILL RED, and NOT the same sentence** — the ruling's own stop condition |
| (c) the D13 entry | **CONFIRMED PAYABLE** — the ratchet reds on it, deletion owed with the landing |
| (e) the parity gate's expiry | **FIRES AS BUILT** — the declared diff goes stale and reds |
| the prize | **`test:fixture-identity` GREEN** — control red, unit green |

## WHAT WAS BUILT

Leg (v) re-scoped exactly as ruled: **the readers derive, then the writer
stops**, both halves in one tree, each behind its own flag so the halves can be
priced apart and together.

**The read half (`LFA_SCAFFOLD_LEGV_READERS=1`).** The week's IDENTITY stops
being a thing you look up. `weekIdentityForWeek` asks the fixtures what it
already asked them, and now also asks the athlete's SOURCE FACTS: a severe
illness covering the week mints `optional_week`. It is minted by the **same
single owner generation asks** — `deriveIllnessRecoveryWeekMode` — not by a
second predicate written beside it, so a stored week and a derived week cannot
disagree about what kind of week the athlete is in. Installed at the accepted
reader (`acceptedEffectiveWeek`), which reads the facts from the surfaces bundle
or, for the two dozen callers that hand it the store whole, from
`acceptedMaterialContext` — the same list either way.

**The write half (`LFA_SCAFFOLD_LEGV_WRITER=1`).** Both overlay publication
sites stop storing `exposureContractV2` — the builder
(`weekRebuild.ts:235`) and the fixture-rebuild publication (`:567`). The
declaration retires at its OWNER, not at one call site.

## THE PRICE — ALL 156 SUITES PER ARM, ONE TREE, SETS DIFFED

World-identity preamble printed on every arm (`cwd`, `head=d91d23fc`,
`files_with_symbol=1`). Failure SETS, never totals.

| arm | failures | vs control |
|---|---|---|
| **control** (flags off) | **2** | — `program-control-durable`, `fixture-identity` |
| **readers alone** | **2** | **control set EXACTLY — 0 new, 0 fixed** |
| **writer alone** | **11** | 10 new, **`fixture-identity` FIXED** |
| **the unit** (readers + writer) | **9** | 8 new, **`fixture-identity` FIXED** |

**THE READ HALF IS FREE.** Identical set to control, not merely an identical
count. That is the seat's compression proven as a measurement: a stored
representation's readers can be taught to derive BEFORE the writer stops, at
zero behavioural cost, because in a world where the declaration is still stored
the derived answer and the stored answer agree — and where they would not, the
deriver returns the stored object byte-for-byte.

**THE READ HALF PAYS TWO OF THE NINE, NOT SIX.** Writer-alone minus unit =
`readiness-ownership` and `deriving-device-commit`. Those are the two cells
whose subject is the illness mode. The other four class-A suites are red under
BOTH arms with byte-identical failure text — the read half does not touch them.

## THE NINE, CLASSIFIED (the unit arm)

**NOT LEG (v)'S (1):** `program-control-durable` — the branch's own control red,
present in every arm, unchanged.

**DEBT PAYMENTS, i.e. the ratchets working (2):**

| suite | what it says |
|---|---|
| `derived-week-lawfulness` | "a declared parity diff no longer happens in any world … the commit that turned it green owes the deletion of its entry" — `stored_declaration_costs_mondays_power_row` **EXPIRED**, exactly as its `expiresWhen` predicted |
| `action-walker` (cell 1 of 2) | "declared red no longer reds in the bounded tier — delete the entry" — `session_list_calls_a_conditioning_day_recovery`, the D13 debt (c) authorises deleting |

Both are red BECAUSE leg (v) works. Neither is a defect, and both deletions
belong in the commit that lands it.

**ALREADY ATTRIBUTED, PAYER ALREADY MEASURED (1):** `fact-horizon` T4 —
"illness_severe rewrote 2 completed day(s): 2026-07-20 High→Moderate,
2026-07-21 High→Moderate". This is the fourteenth pass's named tier-4 read-time
write (`section18SafetyFinaliser.ts:265-278` + the offer placer, both blind to
`governedFromISO`), and the fifteenth pass measured its fix: **the one-owner
boundary greens `fact-horizon` in EVERY measured combination** (`c076f53f`).
Leg (v) exposes it the same way leg (ii) did — by removing a stored answer that
was hiding it.

**THE READ SIDE DOES NOT YET SUPPLY THE CONTENT (3):**

| suite | cells | what it says |
|---|---|---|
| `athlete-session-deletion` | 18 | "bye-build hard conditioning template missing from both the derived week and the rebuilt week" |
| `accepted-state-transactions` | 5 | "Wednesday game anchor missing", "bye table was not selected", "practice-match table not selected", + 2 readiness cells |
| `illness-clear-game-week` | 2 | G3 "precondition: illness week authored, **mode=undefined**"; G1 restStress `achievedActiveRecoveryCount` 1 vs 2 |

Two things worth separating here, because they are not the same kind of thing:

- **`illness-clear-game-week` G3 reads the STORED DECLARATION directly**
  (`illnessClearGameWeekResolveTests.ts:224-227` reads
  `weekScopedOverlays[anchor].exposureContractV2.identity.mode`). Its precondition
  asserts the existence of the very thing leg (v) retires — a WITNESS reading
  storage, the same shape `seedExactSundayRegression` was re-pointed for. It is a
  re-point candidate, **not** a licence to edit an expectation to match a
  regression: the re-point is only legitimate if the DERIVED week says
  `optional_week` there, which is unmeasured.
- **`athlete-session-deletion` already reads the derived week FIRST**
  (`:427-430`, re-pointed in an earlier pass) and falls back to the payload.
  Both are empty. So the derived week genuinely does not produce the freed day's
  hard conditioning, and that is a real gap, not a fixture artifact.

**STILL UNEXPLAINED, AND NOT THE SAME SENTENCE (2):**

| suite | cell |
|---|---|
| `phase-structure` 8 | "the flush SURVIVED the fixture-change rebuild on 2026-08-11" |
| `action-walker` + `action-walker:deep` | "accepting a lighter day derives from the fact and never touches the athlete's surface" (`:deep` is 20/1 — this cell alone) |

The ruling's (b) said: set-diff them against the six first, and **if either is
not the same sentence, it stops the build**. Neither is. The six were "the week
no longer knows what kind of week it is"; these are an offer surviving a rebuild
and a lighter-day acceptance reaching the athlete's surface. **So this is the
STOP, taken on the ruling's own terms.**

A CANDIDATE, offered as a candidate and nothing more: both read consistently
with the eighth-pass tape's hop — the payload is materialised inside
`commitAcceptedStateTransaction` **gated on the proposed overlay's
declaration**, so retiring the declaration stops the rebuilt week being
materialised and the base microcycle shows through (content MISSING in the
deletion cells, stale content SURVIVING in cell 8). That would make the content
class and the unexplained pair one sentence after all. **It is not attributed.**
Three hypotheses died in this unit by being plausible and unexecuted; the tape
that settles this one watches `commitAcceptedStateTransaction`'s materialisation
branch under the writer arm, both worlds, and it has not been run.

## THE COMPRESSION, SHIPPED

`scripts/sweep.sh` gains `SWEEP_SUITES` — a targeted witness batch now runs
through the same runner as a full sweep: same mandatory world-identity preamble,
same exit-code verdict, same failure set, no tails. Proven both directions in
one run: `SWEEP_SUITES="test:fixture-identity test:phase-clock"` →
`failures=1 of 2`, naming the known red and not the known green.

L12 — what catches the NEXT one of this class: not a reminder to read exit
codes, but the removal of the reason anyone hand-rolls a loop. The subset was
the only measurement the runner could not do, and that gap is what the shortcut
filled.

## FOR THE SEAT — WHAT I RECOMMEND

1. **Land the READ HALF alone, now.** It is measured free (control set exactly,
   0 new / 0 fixed), it is the seat's own named standing shape ("READ-SIDE
   FIRST, writer last"), and it converts leg (v)'s price from 10 new to 8 before
   anything else is attempted. It is not a half-state in the sense the repo
   fears: it stores nothing new, removes no writer, and adds one derivation with
   one owner.
2. **Then attribute the content class by ONE tape**, not serially — the
   materialisation branch inside `commitAcceptedStateTransaction`, both worlds,
   under the writer arm. That one run is positioned to answer the three content
   suites AND the two unexplained cells together, because the candidate above
   predicts they are the same hop.
3. **`fact-horizon` is already paid** by the one-owner boundary; whether it
   lands with leg (v) or before it is a build-order question, not a new unit.

## NOT COVERED

- The two unexplained cells are CHARACTERISED and set-diffed, **not attributed**.
  The candidate mechanism is unmeasured and named as such.
- Install sites 2 and 3 of the deriver (`sessionResolver`'s tier-4 host,
  `fixtureMinimalReplan`'s publisher) do NOT receive the facts in this arm — only
  site 1 does. They already call `deriveWeekContract`; what they lack is the fact
  input, which matters only for the illness mode. Untested, and it means the LIVE
  resolver path could still answer the mode from storage where the accepted
  reader now derives it. `assembleScheduleState` already carries
  `acceptedMaterialContext`, so the input is reachable without a new store read.
- Whether the derived week says `optional_week` at `illness-clear-game-week` G3's
  coordinate — the fact that decides whether its precondition is a legitimate
  re-point or a real defect.
- `test:compile` was outside the sweep, as in every prior pass.
- No device evidence. Sam's pending flag is unchanged: Monday's `Vertical Jump`
  returns WHEN leg (v) lands, and leg (v) has not landed.
