# Priority D rulings — Sam, 2026-08-05

Answers to the three option spaces in
docs/STAGE_B_PRIORITY_D_SURVEY_2026-08-05.md. Recorded by the review seat;
committed by the terminal as authored. Sam's general direction, recorded:
legacy stored state is presumed useless, but conviction stays
receipts-first, unit by unit — today's two premise flips (LR-26
before-side, restoration.workout) are the standing reason.

**D-1 — RULED (a): contained cut now, census unit for the rest.** The
overlay writers stop copying the v1 contract (the cheap, contained slice).
Generation still minting v1 on every fresh microcycle is FILED as its own
census unit (the full sweep: generation gate, bye-mode fallback, ~7 test
files) — the staged "until the commit-gateway slice is approved" gate gets
an owner by being ratcheted, not by memory.

**D-2 — RULED (a): measure first, LR-27 method.** No implementation, no
redirect, no reorder until a probe on a WORN acted world (long life,
overrides authored, hydrate + accepted-commit cycles) brings back receipts
on what the hydration-repair in-place branch actually does and stamps. The
probe report comes back to Sam via the review seat BEFORE any D-2 ruling.
The known facts stand recorded: writes under the athlete's dateOverrides
key, bypasses the door's tape, stamped athlete-authored, asserted nowhere.

**D-3 — RULED (a): the athlete's tap is the decision; the guess dies.**
Pass the authored factId through end to end; DELETE the alphabetical
derivation at temporarySourceFact.ts:577 (byte-order artifact, authored by
nobody). The Clear button unifies onto the same single selector — one
owner for "which fact is the fact today"; the cleared-but-still-trimmed
lie to the athlete dies with it. Walker cells pin: (i) tap→factId→day
ownership end to end, (ii) clear restores the original session in the
SAME world that authored two overlapping facts. No authored kind-priority
exists and none is being invented — option (b) explicitly declined.

**Bookkeeping (no ruling needed):** the census file's missing stage-1
LR-27 entry is fixed as part of this batch.
