# Programming follow-up — 28 August 2026

Owner: programming-remedy. Installation hold remains. No phone was installed to,
reset or wiped. This report supersedes older P15/P19 decisions and verification
pending statements only where a newer receipt is named below.

## Checkpoint and scope

Revised candidate: `bb42e66475163a6849ce1720ff5b2646fa5866b3`, isolated at
`/private/tmp/lfa-release-reviewed.urEcKn/source`. The original verified
`0bcc3353` and subsequent `263369ba` evidence remain in their original folders.
Other-seat checkout changes are untouched. This is an existing-owner correction,
not a new architecture, injury engine, progression system or dose generator.

Final evidence folder:
`outputs/programming-gap-closure-2026-08-28/candidate-bb42e664/`.
Application code is identical to `01334a5a`; only the ownership-review fingerprint
and this seat's status changed. Exact gate, build and four report years were
rerun on the saved candidate, not relabelled from the previous run. **20/20
release units passed, exit 0.** Both start/end manifests verify all 2,366 tracked
files against the exact Git blobs. `verification.json` consolidates the receipts.

## Changes and failure-first protection

- **P15:** In-season club attendance no longer blanket-excludes automatic speed.
  The existing sprint answer distinguishes acceleration-only from top-speed-only;
  selection serves the reported missing quality through the existing compiler.
  Existing injury, fatigue, deload, availability, game-spacing and weekly limits
  remain. Covered needs do not force extra work; legacy frequency answers are
  not silently converted into invented missing-quality reports. No dose added.
  `inseasonSpeedTruth` includes male/female real onboarding, all four explicit
  answer worlds, multiple blocks and restart. A further no-club novice regression
  caught the new fatigue condition spilling into existing off-season allocation;
  only that new condition was narrowed to club top-ups (102/102 after correction).
- **P19:** Explosive Landmine Press is selectable as power and earns no strength
  credit. The actual name-to-strength-slot owner excludes power, the existing
  power pool retains its role, and authored Primers carry power evidence without
  changing their dose or order. Full years caught paths missed by the initial
  short test: one male and three female late-year accessory rows, plus ten female
  Primer rows labelled prehab. Expanded checks first failed 15 assertions, then
  passed 77/77; the annual compiler invariant inspects actual role and evidence.
- **P18:** No further programming change. All seven flush templates retain the
  agreed short, easy, concrete-machine prescriptions; G+2 placement, optional/core
  rules and recovery classification are unchanged. The injury matrix was expanded
  to four numeric bands: 4,489 assertions across seven templates, 16 machine
  subsets and three injury combinations. No fitness-conditioning credit.
- **Injury:** Main flow remains area → severity → adjustment. Pain reporting is
  optional; explicit pain and historical restrictions survive severity updates.
  Shared ratings account for the tested indirect-load/support examples. Limiting
  and severe work, replacement selection, actual conditioning modalities and
  manual edits use the same authority. Serious symptoms are independent of score.
  Native testing caught a clipped optional-pain return button, a serious-symptom
  modifier falsely claiming 8–10, and an injury banner claiming upper-body/core
  work over Bodyweight Squat. The existing Back control and copy owners were
  corrected. The replacement-summary journey reached 508/508 after four real
  male/female 7/9 severity summary failures; the existing adjustment suite is 54/54.
- **Severity appearance:** The 4–5 icon background and divider use the same neutral
  styling as the other rows. Yellow severity bars remain. Native visual receipt
  and deliberate highlight mutation cover this small UI correction.
- **Accumulated low-load Remove:** Native Add → Undo → re-add → reopen → Remove
  exposed a false “already has that” result. The existing idempotency owner now
  compares the actual remaining-workout effect, not merely a shared target id.
  Eight male/female Mobility/Recovery order worlds: 32 pass / 8 fail before,
  64/64 after; existing deletion matrix remains 147/147 over 18 coordinates.
- **Native exercise Undo:** Add and Quick Swap saved correctly but the existing
  Undo model hid its button because the shared journal phrase owner had no words
  for those two action types. The simulator attempted Undo within 1.6 seconds,
  not after expiry. Added only the two missing exercise-action phrases; no new
  toast, navigation or Undo implementation. Real durability checks first failed
  8 assertions (48 pass), then passed 56/56; existing Undo 28/28 and journal 13/13
  remained green. Both missing-phrase mutations were caught.
- **Native severe manual edit:** The actual simulator refusal was reproduced
  through fresh onboarding with the same answers, not by seeding saved stores.
  Male/female × 7/9 × live/reopened first produced 10 failed assertions: eight
  refused safe swaps and two duplicate male exercise days. Additions now see
  actual preceding substitutions, lower additions prefer an eligible uncovered
  pattern, weekly requirements respect the shared rated pool, and the existing
  injury-imbalance justification reaches validation. Numeric healthy-week limits
  remain. The manual medical stop now uses the shared serious-symptom predicate,
  not severe score alone. Final targeted witness: 564/564, including unsafe-swap
  refusal, serious low/high stops, Undo and exact healthy Clear/restart. Six new
  mutants caught; the first partial duplicate mutation is recorded as incomplete,
  then both original inputs were reintroduced and the correct assertion failed.
- **Indirect loaded support:** Native severe shoulder testing then exposed a
  Single-Leg Box Squat still prescribed at 17.5 kg despite a generic Good rating.
  Corrected ten existing rating cells across that exercise, loaded Back Extension,
  Hip Thrusts and Single-Leg Hip Thrust. Prescribed loads/doses were not changed.
  A cross-metadata check enumerates all 19 lower-body free-weight load profiles ×
  three upper-limb regions × five limiting/severe scores (285 checks, not 285
  exercises). Expanded injury witness: 799 pass / 50 fail before, 849/849 after;
  three old-unsafe-rating mutants caught. Earlier native saving of Back Extension
  is explicitly NOT complete safety acceptance. The corrected native severe
  session retains Single-Leg Leg Press and Bosch Hold instead of loaded Box Squat.
- **Ownership prerequisite:** The 2f63ced4 annual run reached 416/416 green weeks
  but release stopped at 10/20 because one of 1,138 executable owner reviews had
  a stale fingerprint. The full changed function, 18 sites, six capable callees
  and sole caller were reviewed; it remains the existing injury compiler stage.
  Only that review record was updated. Detector 41/41; all 1,138 owners reviewed,
  no rival author or derived publisher. Restoring the old fingerprint fails the
  exact owner's check. No programming rule or test requirement was relaxed.

Thirty-one deliberate follow-up mutations were rerun against bb42e664: all
31 landed and were caught by their intended assertions. The separate ownership
stale-review control also fails as intended. All four category mutations were
rerun on bb42e664 and caught as well.
Earlier receipts remain: 53 flush/onboarding/programming mutations on 263369ba
and nineteen follow-up mutations on 8ada6c67.
They are not presented as newly rerun final-candidate results.

## All 55 conditioning templates and delivered-year spacing

The original manual output review is in `FLUSH_ONBOARDING_RELEASE_2026-08-28.md`.
The 01334a5a recapture exactly matched all 55 templates / 110 actual emitted
variants, including descriptions, dose and recovery, with 54 automatic templates.
Bodyweight Circuit remains available for saved lookup but is retired from
automatic selection. Final-candidate recapture/comparison is recorded below.

**P07 remains partly open:** visual inspection of the actual male commercial
year found a non-flush Bike Extensive Tempo row still prescribing “30 s walk”
recovery. The exact 110-variant comparison only proves existing output was
preserved; it does not certify all resolved off-leg wording. No dose, active
recovery or intensity was silently changed. This needs a modality-aware review
at the existing prescription owner. The seven agreed flush formats are separate
and remain unchanged.

The old 15 adjacent same-part pairs per year are all conditioning, not adjacent
Upper/Upper or Lower/Lower lifting days. Across four years this is 60 occurrences
on 15 distinct date pairs. Reading actual efforts and including Speed adds eight
pairs per year: 23 distinct date pairs / 92 occurrences across four input worlds.

| Per year | Pairs | Actual assessment |
| --- | ---: | --- |
| Easy/moderate → hard | 5 | One hard day, not two. |
| Easy/controlled → easy/controlled | 10 | Includes deload Tuesday. |
| Easy/controlled → Speed | 8 | Previously omitted by the heading counter. |
| Hard → hard | 0 | Actual prescriptions inspected on both dates. |

Normal build asks for five agreed conditioning/speed exposures in seven days,
which necessarily creates at least three cyclic adjacencies. The selected
Mon/Tue/Thu/Fri/Sat arrangement meets that lower bound. All six legal receiver
alternatives were inspected: moving the adjacency produces either a four-day
core streak or six core-active days. Some adjacency is unavoidable; no individual
date pair is claimed compulsory, and no genuinely poor avoidable placement was
demonstrated among these alternatives. The illness week is separately classified,
not excused using the normal-build five-exposure lower bound.

`spacing-classification.json` lists every actual pair, efforts, doses and recovery;
`PROGRAMMING_GAP_CLOSURE_2026-08-28.md` records the search observations and
alternative comparison. Final bb42e664 years match those actual adjacent
prescriptions and date pairs. This is not a claim that every athlete's
availability pattern or every possible placement is ideal.

## Diagnostic classification

The requested four old C12 failures were four assertions around one deleted
`categoryToFlavour` function, not four current programming defects. Obsolete
function/shape expectations are retired; category coverage and no-loss behaviour
are held at the current typed selection/composition owner, including COD with no
permanent exception. Diagnostic 104/108 → 152/152; four mutants caught. The new
helper is in the release compiler witness.

The later exact 8ada6c67 release stopped at 17/20 units, with 172/173 assertions
in `injuryFallbackJourneyTests`: an old expectation ignored serious symptoms
below the severe band. R-267 explicitly supersedes it. The replacement checks
all ten scores at the same refusal owner (182/182); production safety was not
changed to satisfy that old test. It passed the complete dd00b81e release and
the final bb42e664 release rerun.

Additional historical diagnostic debt is not hidden:

- `test:slot-coverage`: imports deleted `applyPoolRotation`; already classified
  for test rewrite. Does not justify restoring a deleted writer.
- `test:power-counting`: baseline and corrected candidate both retain four old
  failures (9/13). Two expect the retired gateway strip/budget repair, one demands
  every power row have order zero despite approved authored Primer/contrast order,
  and one compares a July golden predating approved changes. Current power rules
  are held by compiler/power/year tests; rebinding that historical diagnostic
  remains work. No golden was regenerated to conceal a failure.
- `test:law-registry`: unchanged baseline debt is three failures: missing
  `test:game-feedback`, unregistered LR-18, and 21/210 UNENFORCED rows (208 was the
  older baseline denominator). No waiver
  or false guarded status was entered. The whole `test:bible` fleet is not green.
- `test:section18-v2`: fails before assertions on both dd00b81e and the revised
  source because the old fixture omits required onboarding answers. Rebind that
  fixture; do not weaken onboarding validation or change correct programming.

## Native coverage and automation classification

These are real, accumulated onboarding journeys in a dedicated iOS simulator,
not saved-state seeds. The native shell is an existing Debug build; exact frozen
candidate JavaScript is supplied by a dedicated Metro server. The separately
built Release binary was not installed. No claim of physical-device acceptance.

- Area → severity → adjustment, optional painful-movement report, limiting
  shoulder and low-score serious symptoms were reached through actual screens.
  Missing mandatory triggers are intentional, not an unresolved defect.
- Mobility → Mobility, Recovery → Mobility and Mobility → Recovery include
  distinct-drill comparison, modifier visibility, Undo and reopening. Accumulated
  removal now removes the intended added work and can itself be undone.
- G-1 Gunshow plus Mobility: six original strength exercises and six different
  Mobility drills, 12 distinct displayed names across two sessions, retained
  through Add → Undo → re-add → reopening. Game/G-1/G+1 cases and two-session
  capacity refusal are separately identified in the underlying native receipts.
- Final 01334a5a severe shoulder: safe replacement displayed, actual Quick Swap
  and immediate Undo, original restarted state retained, modifier visible, Clear
  restored healthy Barbell Row. Three further cold reopens kept Clear applied,
  healthy Barbell Row next Tuesday and Gunshow + Mobility on Friday.
- One Maestro run reported an app crash during the launch transition. The new
  process 93463 was successfully bootstrapped and stayed running-active-Visible
  in SpringBoard logs. Without relaunching, its screenshot and continuation
  confirmed retained state. Corrected automation waits for launch animation;
  three consecutive cold reopens passed, not just one retry. Original failed
  tape, screenshot and system log remain in candidate-01334a5a.
- bb42e664 was cold-opened on its own Metro 8098: the added Friday Mobility and
  cleared injury state were retained. On next Tuesday, Add correctly refuses a
  third session. Its actual Back → Back navigation dismisses the capacity screen
  and the healthy Barbell Row session remains. Failed Done/Close selectors refer
  to controls on different sheet steps; they are preserved automation errors,
  not evidence of an app dismissal defect. Other result-sheet routes are not
  claimed exhaustively tested.
- Final severity-screen tap sequence and Cancel passed on bb42e664. Visual
  inspection confirms a neutral 4–5 chip/divider, yellow bars retained, optional
  pain visible and serious-symptom route separate. Receipt:
  `artifacts/gap/bb42-final-neutral-severity.png`.
- Full-strength warm-up overlap is now covered natively on bb42e664. Monday
  31 August originally had strength plus conditioning; the test removed only
  conditioning to free the existing second-session slot. It then added Mobility,
  used Undo, re-added and cold-reopened. All four warm-up drills and six strength
  exercises retain their exact displayed names and doses. Six added mobility
  drills overlap with neither: 16 placements, 16 distinct names, two sessions.
  Reopened lists match exactly. The first comparison reader assumed nested
  XCTest row containers and correctly failed its nonempty-anchor assertion;
  XCTest exposes those rows as siblings. The corrected reader is bounded by the
  actual Monday card and expanded-row anchors, excluding headers and app tabs.
  A duplicate-warm-up mutant is caught. Both original reader failure and corrected
  evidence remain; this was instrumentation, not a programming change.

Source-equivalence is explicit: 01334a5a → bb42e664 changes only the ownership
review JSON and seat status. Native receipts are never represented as a newly
installed Release binary or a complete simulator cross-product.

The four old C12 assertions were rechecked on bb42e664: 152/152 pass at the
current owner. The additional slot, power, Section 18 fixture and law-registry
diagnostics were also rerun; their previously classified debt remains visible in
`candidate-bb42e664/diagnostics/receipt.json`. It is not part of a claimed green
whole diagnostic fleet.

## Original P01–P22 status

“Implemented” means implemented and covered by named automated checks; it does
not mean physically accepted. No programming answer is outstanding from Sam.

| Items | Status and remaining acceptance |
| --- | --- |
| P01, P09, P14 | Implemented: clearer structured prescriptions, actual machine identities and retired incomplete automatic circuit. 55-template capture comparison; physical acceptance remains. P07 caveat still applies. |
| P07 | Partly complete: 55-template copy reviewed, but resolved non-flush off-leg recovery wording above still needs correction and verification. Not closed by field checks or capture equality. |
| P02, P06, P16 | Implemented: single-leg priority; Leg Press and conventional Deadlift retained for manual choice and suitable fallbacks. Existing kit/prioritisation journeys retained. |
| P03 | Implemented: fixture-qualified automatic extras and manual Add retained. Native game-adjacent acceptance remains limited to listed journeys. |
| P04 | Implemented: power rest retained in domain data, hidden in display/report. Strict four-year audit; physical acceptance remains. |
| P05, P10, P21 | Implemented/reviewed: whole-week selection history and spacing. Above classification replaces “15 pairs proves fixed”; other availability shapes are not claimed reviewed. |
| P08 | Implemented: advanced loaded-curl preference with legitimate band fallback. Regression retained. |
| P11, P12 | Implemented: lift-name/machine identity and rollover/projection protection. Accumulated-year and weekly restart checks; physical acceptance remains. |
| P13 | Implemented with shared injury, load-support, replacement and native corrections above. 849-check witness plus actual simple-flow/manual-control/Undo/Clear/reopening evidence. Not physically accepted. |
| P15 | Implemented per latest decision: distinct unmet speed needs, no blanket in-season exclusion, no new dose. Compiler/restart checks; new answer tiles need native acceptance. |
| P17 | Implemented: agreed jumps remain in the ordinary eligible pool, not an invented preference hierarchy. Existing kit/injury checks retained. |
| P18 | Implemented, unchanged: short easy flush, agreed G+2 optional/core rule, recovery only. Matrix/restart checks retained; physical acceptance remains. |
| P19 | Implemented per latest decision: selectable power-only Landmine, no strength slot/credit, actual Primer role/order preserved. Full-year role/evidence invariant and restart; physical acceptance remains. |
| P20 | Automated verification recorded below; not closed while remaining native gaps and physical acceptance remain. |
| P22 | Implemented: second Mobility/Recovery excludes all same-day drills, including derived strength warm-ups; separate completion, Undo/restart and accumulated Remove protected. Native permutations listed in final receipt. |

## Exact-version verification

Final release gate: **20/20 units green, exit 0**, covering 23 current product
contracts plus bootstrap and all-scope typecheck. The following completed checks
are all against bb42e664 unless their earlier native source is explicitly named:

- Clean generic iOS **Release build succeeded**, not installed.
- Compiler annual acceptance: **416/416 athlete-weeks**, eight distinct athletes,
  52 weekly restarts each, zero distinct failure keys; ownership prerequisite green.
- Delivered years: male/female × original-partial/corrected-commercial equipment,
  each 52 weeks and 364 athlete-days, **208/208 weekly restart checks**, **4/4 strict
  content audits**. Exact profile, dated action and phase inputs match baseline.
- All 55 conditioning templates / 110 emitted variants match the preserved
  baseline, 54 remain automatic. This is not a waiver for the P07 wording issue.
- All 23 actual adjacent energy date pairs per year match their individually
  classified prescriptions; zero hard/hard pairs in these four delivered worlds.
- **31/31 follow-up mutants + 4/4 category mutants caught**. Separate stale-owner
  review and native duplicate-warm-up controls also fail as intended.
- Native exact-version capacity navigation, neutral severity screen and full
  strength/warm-up/Mobility Add/Undo/reopen checks passed. Earlier exact-app-code
  severe-swap/Clear and three cold reopens are identified in the native section.

Reports are `corrected-commercial-male-year.html` and
`corrected-commercial-female-year.html` in the final evidence folder, alongside
the original-partial pair, `year-comparison.html`, `before-after-summary.md`,
`year-audit.json` and `final-comparison-audit.json`. Browser spot-checks of the
final male Week 8 and female Week 29 inspected actual rendered prescriptions,
including the P07 exception and actual selectable power-only Landmine/flush rows.

Remaining work, owned by programming-remedy: correct and verify P07's resolved
non-flush off-leg recovery wording at its existing owner without silently changing
recovery; rebind the explicitly classified historical diagnostics; finish the
uncovered native answer/refusal routes. Physical acceptance and the recorded
2 km Skip QA remain held for Sam after release preparation, not treated as closed.
No programming answer is required from Sam.

## What catches the next defect

The annual invariant examines real later-week role/evidence, not catalogue labels.
Speed checks use real onboarding answers, missing-quality selection and restart,
including no-club controls. Injury checks cover numeric boundaries, historical
facts, actual replacement names and resolved machines. Low-load journeys reach
accumulated Add/Undo/re-add/reopen/Remove state. Source mutations must land and
kill the intended assertion. Native checks must assert the reached date and actual
control/result, not accept an old selector or a passing tap as proof.

## NOT COVERED

- Every refusal-sheet route and the new speed-answer tiles on native glass.
  Compiler coverage is not physical acceptance;
  no complete simulator-matrix claim.
- Physical iPhone acceptance, Release runtime on a phone, installation or wipes.
- Onboarding 2 km Skip physical-QA acceptance. The original inconsistency remains
  recorded even though the layout correction and repeated real simulator journeys
  are verified on 263369ba; a successful retry alone never closed it.
- Every athlete availability combination, clinical validation, or a claim that
  all 22 original items are finished.
- Rebinding the additional historical slot/power diagnostics and law-registry debt.
- Remaining non-flush off-leg recovery wording in actual delivered output (P07).
