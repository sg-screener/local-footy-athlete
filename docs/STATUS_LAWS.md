# LAWS — your own status file. ONE WRITER: you.

**NAMED 2026-08-13 by Sam: *"You are laws."*** Checked before claiming it:
`ls docs/STATUS_*.md` showed ten seats and no `laws`; no commit in twelve hours
carries `Agent: laws`. **The name was free.**

**EVERY COMMIT FROM THIS SEAT ENDS `Agent: laws`.**

**THE LANE:** `src/rules/lawRegistry.ts` and the gates that hold it —
`test:law-registry`, `test:repo-law-guards`. **Standing order 13: keep the
unenforced law count falling.**

---

## THE COUNT: 24 → 21

Measured with the SUITE, not `grep -c` (the registry records that grep sends
every seat one high). Baseline `LAW REGISTRY: 125 rows, 101 guarded, 24
UNENFORCED`; now **104 guarded, 21 UNENFORCED.**

### THE BAR I SET, AND IT IS WHY THIS IS THREE AND NOT FIVE

**A guard counts only if it holds the law the row STATES.** Three of the 24 say
in their own text that the obvious cheap gate is refuted — and they are right,
so **building it anyway and flipping the row would be the rubber-stamp the
registry exists to refuse.** So the bar became: **build the gate the row itself
prescribes.** Three rows had a prescription I could build today.

### ✅ LAW-no-completeness-claims — GUARDED

Its own words asked for *"a vocabulary gate over reports and NOW.md forbidding
… unless beside a cited run"*. Built exactly that: **60 reports scanned**, a
completeness phrase legal only when a run is cited within two lines (a `test:`
name or a pass/fail count — the law's own MEASURED-DONE form). **3 pre-existing
violations carried as dated debt**, not forgiven by weakening the list.
**Mutation-checked**; liveness proves a bare claim reds, a cited one passes, and
"final" in ordinary prose is not swept in.

### ✅ LAW-elegant-two-options — GUARDED

Its own words asked for *"a boundary-report field (options compared) checked by
`test:repo-law-guards`"*. **The law's SENTENCE IS UNTOUCHED** — the row warned
that flipping on the strength of editing its own wording would be the
reads-as-covered failure.
**From-here-forward, cutoff 2026-08-13.** 3 of 59 reports carry the field, so a
retroactive gate would red on 56 whose authors were never asked — the shape
already refuted for `LAW-L9-checkpoint-discipline`. **Two reports written
earlier that day are named as debt rather than pushing the cutoff to tomorrow**,
which would have let the rest of the day through.
**BOTH DIRECTIONS**, and that is the lesson this seat paid for earlier today: a
silent in-scope report reds, AND a debt entry that gains the field must be
struck. **A one-directional ratchet survives its own list being wrong.**

### ⚠ A THIRD GUARD IS BUILT AND ITS ROW IS **NOT** FLIPPED — LAW-doc-truth

`test:law-registry` gained a cell nothing previously did: **every commit sha a
law receipt cites must exist** (13 citations across 125 receipts; all resolve).
The gate already checked `by` and `ruledAt`; **the `receipt` field — the one
that says "BUILT `<sha>`" — was read by nothing.**

**I DID NOT FLIP THE ROW, AND THE ROW IS WHY.** It reasons at length that a
doc-scan gate is refuted by scope and ends *"this row stays UNENFORCED rather
than wearing a gate that passes on 32 files and ignores 419."* **My cell covers
125 receipts, not 451 docs.** It is a real guard on LAW ZERO's own integrity and
it is NOT this law. Counting it would be exactly the failure the row predicted.

**⚠ AND IT ALMOST SHIPPED WRONG.** The first version also resolved FILE PATHS
out of receipts and produced a false positive immediately:
`LAW-one-startup-command` says *"a FABRICATED `scripts/tmp-rival-start.sh` reds
it"* — a mutation fixture, deliberately temporary, not a claim. **A receipt is
free prose and a path inside it may be hypothetical; a sha is always a claim
about history.** The scope is shas by reasoning, not by luck.

## FOR THE NEXT PASS

The remaining 22 sort into three kinds, and only the first is worth a cell:
1. **Rows with a prescription nobody has built** — `LAW-mock-first` (mechanisable
   per its own text), `LAW-L8-reporting-calibration`, `LAW-judgment-ledger`
   (blocked: the ledger doc does not exist).
2. **Rows whose cheap gate is refuted IN WRITING** — `LAW-green-gate-is-a-claim`,
   `LAW-doc-truth`, `LAW-L9-checkpoint-discipline`. **Do not re-derive these.**
3. **Process laws honestly unmechanisable** — `LAW-coach-escalation`,
   `LAW-L16-vertical-slice`. Their row should say PROCESS, not UNENFORCED.

### ✅ LAW-L8-reporting-calibration — GUARDED (the third, 22 → 21)

Its own words asked for *"a repo check over boundary reports: an estimate must
appear beside a measured prior."* Built exactly that.

**IT IS THE FIRST GUARD HERE THAT NEEDS NO DEBT LIST, AND THAT IS A MEASUREMENT
RATHER THAN LUCK.** Four reports in all of `docs/` state an estimate and **all
four already cite a measured prior** — the law is being FOLLOWED, and the gate
stops it lapsing. Every other ratchet in this suite carries dated debt because
its law was being broken when the gate arrived. **A debt list added here for
symmetry would be inventing forgiveness nobody needs**, so there isn't one.

**NON-VACUITY IS ASSERTED, NOT ASSUMED:** the cell reds if NO report forecasts
at all. Without that, a repo where nobody estimates would report a comfortable
zero and read as guarded — the shape this seat has now seen three times in one
day.

**WHAT IT CANNOT DO:** it reads that a prior actual is PRESENT, never whether
the forecast was calibrated against the right one.

### WHY IT STOPPED AT THREE

`LAW-mock-first` is the next honest one and it is **blocked on a convention, not
on effort**: its gate needs "a mock artefact referenced in its unit doc", and no
such artefact convention exists. `test:ui-picture-manifest` is the nearest
thing and it maps SCREENSHOTS OF BUILT SURFACES — the opposite end of the law.
**A gate over a convention nobody follows is either red on arrival or vacuous**,
and inventing the convention is authoring process, which is the seat's call.
`LAW-judgment-ledger` is blocked the same way: **the ledger doc does not exist.**

---

## R-089 — BOTH HALVES BUILT

### THE ORDERING — 0 WORLDS REFUSED, WHERE THE REPLACEMENT REFUSED 12

`readiness` built this as a REPLACEMENT and §18 refused twelve weeks: it counts
main-strength exposures PER PATTERN, so a full-body day that stops planning
squat/hinge stops paying them. **An ordering cannot have that failure, and that
is the whole design** — `orderPlannedByUncoveredFirst` returns an **IDENTICAL
MULTISET**, so volume, §18 and the target cannot move.

**Census byte-identical before and after:** 92 deficient / 318 laddered /
174 worlds / 6 refused. The replacement moved it to 162 / 18.

**⚠ THE LIMIT, PROBED NOT ASSUMED.** It delivers R-089 points 2 and 3. It does
**NOT** deliver the example in point 1: a week where only the single-leg slots
are open comes back **UNCHANGED**, because the full-body template is
`['squat','hinge','push','pull']` and **an ordering cannot introduce a member
the list does not have.** Single leg on the Wednesday needs the template to
WIDEN — which is the substitution class that refused twelve worlds. **Reported,
not smuggled in under an ordering.**

### THE PAIRING — THE HALF NOTHING HELD

**It could not be checked at ROW level, which is why nobody had:**
`mainPatternForExerciseMovement` maps `lunge` → `squat` and has **no single-leg
member at all.** The SLOT vocabulary is the only one that can express it.

**Directional, not an equality** — "every squat is MATCHED WITH a hinge" bounds
squats by hinges. **74 worlds are legitimately `sq0/hi1`** and an equality would
have failed every one.

**⚠ MY FIRST NON-VACUITY CELL WAS WRONG, AND THE THRESHOLD WAS THE WRONG
THING.** It demanded ≥3 distinct week shapes and failed at 2. **Shape variety
is not what makes the cell mean something — both counters registering is.**
Lowering it to 2 would have been fitting the gate to the answer; it now asserts
a week exists carrying a squat AND a hinge, and another carrying both
single-leg slots.

**⚠ AND THE HONEST LIMIT: this corpus never produces two squats in one week**,
so the exact `2 squats / 1 hinge` shape Sam named is **currently unreachable
here.** The guard is green and mutation-proven, **not green-because-exercised.**

### A NOTE ON THE SHARED CHECKOUT

Hit `HEAD.lock` twice. **The first was 10 minutes old, ZERO BYTES, with no git
binary running — stale from a crash, and another seat was spinning a 45-attempt
retry loop against it.** Removed it; git's own error sanctions that and it
unblocked both of us. **The second was LIVE** — `vocab` had a real
`git commit` running — so I waited, and it cleared in 10 seconds. **The check
is whether a git BINARY is running, not whether the file exists.**
