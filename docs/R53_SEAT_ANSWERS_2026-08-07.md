# R5.3 SEAT ANSWERS — three rulings on the boundary report — 2026-08-07

Answers docs/R53_RESIDUAL_CLOSED_OVERWRITE_UNREACHABLE_2026-08-07.md and
docs/R53_FREED_DAY_PRODUCER_NAMED_2026-08-07.md (9eb18631). Ruled by the
review seat per RULE-DON'T-ASK — each answer is a recorded ruling
applied, cited inline. Sam holds a veto.

## 1. Clause (a)(2) is WITHDRAWN (supersession, not edit)

docs/PROJECTED_ANCHOR_PRECEDENCE_RULING_2026-08-07.md clause (a)(2)
("the overwrite dies; day occupancy composes under dayPrecedence") is
WITHDRAWN under the supersession discipline — the same mechanism as the
withdrawn optional-placement overreach. Its only witness was a
manufactured store-carry-over state no product path reaches; in the
coherent world the door already refuses with the signed game-day
sentence and the visible week is untouched. A systemic fix with no
reachable red witness is exactly what the non-vacuity class forbids.

What SURVIVES the withdrawal: clause (a)(1) — the projection is
Bible-backed and legitimate — stands. dayPrecedence remains the
ratified law of the land (nothing here weakens it; it is simply already
obeyed on every reachable coordinate). detectAthleteMoveContentLoss
stays. If a REACHABLE world ever reds composed-vs-visible on an
athlete decision, that returns as its own unit with its witness.

## 2. The cell-19 witness fix: seed through the doors, and the gate learns to see tests

Form ruled from two standing laws:

- Fixture-fidelity (AGENTS.md): reach state by ACTING — a fixture
  writes through the same owned doors the product uses. seed() writing
  onboardingData directly around the door is the defect; the fix is
  seed() routed through the one-door ingress (the onboarding door),
  never a snapshot poke, never a store write from test code.
- The systemic half is MANDATORY in the same unit (project law: no
  edge-case fixes; L12: name what catches the next one): the one-door
  narrowing gate extends to SCAN TEST SOURCES. The gate not seeing
  test code is what let this fixture manufacture an unreachable state,
  and it is the third fixture-manufactured misdirection this unit
  (cell-19 carry-over, seedExactSundayRegression's borrowed fill, the
  earlier scaffold defect 4). Three sightings = the compression ships
  with the fix, not later.

## 3. Leg (v)'s scope: the DECLARATION retires, the payload does not

Ruled from three recorded sources, no new product decision:

- docs/STANDING_DERIVATION_RULING_2026-08-07.md pre-gave leg (v) for
  "a stored representation feeding a computation" — that names the
  published exposureContractV2 DECLARATION. It says nothing about
  workoutsByDate.
- The R2 extraction ruling deliberately kept weekScopedOverlays
  ("derived content, NOT migrated") — the payload's retirement was
  already scoped OUT of this rebuild's extraction era.
- The fixture-identity law (Sam's Saturday catch) governs when derived
  content may stop being published: only when re-derive-at-read
  provably reproduces it. The freed-day measurement is the proof it
  currently does NOT (day 6's content is load-bearing in the payload).

So: the empty workoutsByDate is the scope switch reaching further than
the ruled target — option 2's publish {...overlay, workoutsByDate: {}}
shape, not the standing ruling's order. Leg (v) RE-SCOPES to retiring
the declaration only, payload published unchanged, and RE-PRICES on
that scope. The earlier refutation is set aside as priced-against-a-
scaffold-defect; the re-price decides, per condition 1 as always. The
16 deletion reds re-read after seedExactSundayRegression is off the
ruled-bug fill (its fixture debt is part of answer 2's class).

## Order

Witness fix + gate extension → leg (v) re-scoped re-price → re-measure
priced configs (the open task) → build order per the convergence
ruling. STOP conditions unchanged; full test:bible UNPIPED before any
product commit; STOP at R5 close.
