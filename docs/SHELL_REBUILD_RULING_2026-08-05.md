# Shell rebuild — Sam's ruling, 2026-08-05 ("yes b cut signed")

Rules on docs/SHELL_REPAIR_VS_REBUILD_SIZING_2026-08-05.md — see that doc
for the full evidence. Recorded by the review seat; committed by the
terminal as authored.

**RULED: Option B — the shell is rebuilt on the proven engine.** The
~11k-line accepted-state transaction/mirror/hydration machinery is
replaced with the north-star shape: stored inputs (decisions) + derive().
The engine (src/rules, src/data, generateProgram — bible-bound), the door
vocabulary, the screens, and the harness survive unchanged.

**SIGNED: the beta coach scope cut.** The Renee/TestFlight beta ships the
typed doors and buttons only; the coach free-text pipeline (already under
the LR-6 STOP) sits out of beta and returns in the coach rebuild, on the
clean shell. This is a scope cut, not a retirement.

Standing conditions, all previously ruled and carried in:

1. The five evening cells (test:device-pass-2026-08-05-evening) are
   path-neutral acceptance gates for the rebuild.
2. Performance acceptance numbers must be RE-MEASURED ON A RELEASE-CLASS
   build before being quoted — the 2026-08-05 dev-build timings
   (31.5 s/transaction) are diagnostics-inflated (Sam's catch).
3. Visible-first stands: every slice ends athlete-visible on Sam's phone.
4. Census units that Option B deletes rather than performs get closed by
   ruling reference, ratcheted, when their machinery is deleted — not
   silently.
5. One-time envelope→inputs migration is its own named unit with a
   conformance target (device exports remain targets, never seeds).
6. Sam's context: no income, the beta is the milestone that matters —
   sequencing favours the shortest honest path to Renee-on-TestFlight.
