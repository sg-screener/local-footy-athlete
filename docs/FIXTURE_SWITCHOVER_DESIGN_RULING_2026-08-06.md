# FIXTURE SWITCHOVER DESIGN RULING — 2026-08-06

Ruling on the diagnosis + design pricing committed at d145364a.
Ruled by the review seat per RULE-DON'T-ASK; Sam holds a veto.

## APPROVED — V3, leg (ii) first

The unit is V3 (settle-by-re-derive AND delete the materialiser), built
leg (ii) first exactly as recommended: leg (ii) carries the unknown
blast radius, and its absence is what makes leg (i) safe. V1 alone is
REJECTED and recorded as a named trap: it greens fixture-identity-5 by
moving the tap down to boot's wrong answer — shipping the regression
immediately and closing the cell that would have caught it.

## Binding conditions

1. The three deliberately-unmeasured items are measured BEFORE their
   leg lands, not after: (a) materialiseFixtureMarksForCandidate runs
   on every accepted commit — measure the other doors that reach it
   before deleting it; (b) stageRollingHorizonFixtureRepair is priced;
   (c) full test:bible runs under each leg off the printed exit line.
2. Cell 5 is strengthened to assert WHICH answer is right, not that
   the sides agree. The V1 pricing proves agreement can green a
   regression. The cell pins the deriver's answer (Lower Squat|8 in
   the measured world) as the truth all surfaces must match.
3. Decision ORDER becomes a permanent gate axis: both orderings of the
   two-decision world (W2 removal→fixture AND W2r fixture→removal)
   are pinned green against the deriver. The missing axis that hid
   this defect was order, not count — cell 5's own "three engines"
   why-text is corrected to two producers in the same commit.
4. The behaviourally-redundant fixture replay interpreter retires only
   with measurement (the boot suites do not cover it — that gap is
   declared, and its retirement commit carries the proof that
   markedDays carries the net state on the add-then-move world where
   both replays fail today).
5. The build starts on a FRESH tank only. The current session spends
   its remaining tank on R5.7 (coach scope cut, signed) and R5.8's
   structural-sweep half — both independent.
