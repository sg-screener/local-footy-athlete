# FIXTURE BOOT-ORDER RULING — 2026-08-06

Rules question 4 of the option-2 stop, on the mechanism proven at
945e0cb4. Supersedes docs/MICROCYCLE_SELECTION_RULING_2026-08-06.md
(its mechanism was refuted by measurement — the covering-selector
already covers; the ruling doc stands as history, unedited). Ruled by
the review seat per RULE-DON'T-ASK; Sam holds a veto.

## Ruling — the ledger is the ONLY fixture input at boot

Option (a): the replayed ledger is the sole source of fixture
decisions at boot, and markedDays DERIVES from it. A pre-restored
markedDays that makes the replay no_change is a second representation
of the decision pre-empting its owner — the recurring class, in boot
order rather than a writer. Grounds: the R1 spine (the decision ledger
is the one owner; boot is quiescent and derives), NORTH_STAR, and the
measurement itself — the ledger-first world runs 20/20 with the cell
unedited. Option (b) is rejected: it feeds a stored consequence back
into generation, adding a representation where (a) removes one.

## Conditions

1. Blast radius measured BEFORE landing: every reader and writer of
   markedDays enumerated; any site treating it as an independent
   persisted fact (rather than a derivation of the ledger) is named
   and routed. Replay idempotency is witnessed by the ledger, never by
   the pre-restored mirror (the R2 precedent).
2. The harness-below-the-door sighting is paid in the same unit: the
   walker's mark_calendar enters through the durable door
   (fixtureMutationTransaction's append), retiring the compatibility
   seam for this action — both halves are needed for the green, as
   measured.
3. The L16 slice greens UNEDITED. Option 2's stale-contract correction
   stands. Then, in order: full bible to BIBLE_TRUE_EXIT=0 off the
   printed line, condition 4 (markedDays proof attached), merge to
   feat/stage-b-stage2, post-merge bible, R5.7, remaining batches.
