# R5.3 — THE PARITY GATE IS RED, AND LEG (v) IS PRICED AT 9-FOR-1 (2026-08-07)

LOOP CHECK: recorded-scope-measured-on-a-world-that-has-moved — sighting 3 of
the stale-record class (the D13 coordinate was 1, the seat's own withdrawal was
2) — COMPRESS, and the compression is stated at the end: an arm's price is not
believed until a positive control proves the arm CHANGED something in the world
the claim is about. `sweep.sh` already refuses to measure a world where the
symbol is absent from `src`; present-in-source is not live-on-path, and this
pass paid for the difference — leg (v) turns out to reach one of this unit's
two worlds and not the other.

Eighteenth pass. Answers inbox item 1 (a) and (d). **Two things are BUILT and
on the branch** (`77565f00`); **leg (v) is priced, not built**.

## THE HEADLINE

| step | result |
|---|---|
| (a) D13 DECOMPOSE, built | `test:action-walker` 21/21, `:deep` 21/21 — the 2026-07-27 red is covered by its two declared constituents |
| (d) `derivedWeekLawfulnessProof` ported | **12/12 on the branch, unedited** |
| (d) THE PARITY GATE, built | **RED in all three worlds — and the row is named** |
| control sweep, 155 suites, HEAD `77565f00` | **2 failures** — `program-control-durable`, `fixture-identity` |
| (d) LEG (v) ON ITS REAL PRICE, 155 suites, sets diffed | **10 failures — 9 NEW, 1 FIXED** |
| the one FIXED | **`test:fixture-identity` 6/6** — condition 1's own witness |
| condition 1's re-measure | **NOT GREEN, residual named** |

The control is DOWN FROM 3: the D13 decompose retired `action-walker:deep`,
measured in the sweep rather than inferred.

## (a) THE DECLARATION MATCHER DECOMPOSES — BUILT

A declared red no longer carries a regex over the offence sentence. It declares
the SET it covers — what its defect makes the session list OMIT and what it
makes the list INVENT — and the matcher decomposes a day exhibiting two
declared defects into its constituents.

The deep walker's 2026-07-27 red, `omits ["speed"] and invents ["support"]`, is
now covered by `session_list_has_no_representation_for_speed_work` and
`session_list_badges_a_midline_row_the_projection_has_no_part_for`
**together**, both credited to the stale-debt ratchet, with no third entry for
the pair. That is the gap the walker file has carried in prose since
2026-08-04, closed by construction rather than by another entry.

**The ratchet is not loosened, and that is proven rather than asserted.** An
entry participates only when its own set is a SUBSET of the day's, so it can
never be credited for an element the day did not exhibit; ONE novel element
anywhere in the offence leaves the WHOLE day undeclared and red; and an entry
declaring no elements at all — the wildcard — fails the file. The matcher cell
walks 8 shapes, **3 of which must NOT match**, plus a formatter/parser round
trip so the sentence and its reader cannot drift apart silently.

The debt is unchanged in rank and owner, exactly as ruled: all three entries
are still carried, all three still owed by the D13 session-template owner.

## (d) THE INSTRUMENT IS PORTED, AND IT PASSES

`lastTierFourDerivation` landed as a write with no reader. Its reader is
`derivedWeekLawfulnessProof`, now on the branch and green **unedited**: 12/12
across three worlds (removal-then-fixture, fixture-then-removal, fixture-only)
— patterns legal, balance held, §18 green with zero unresolved shortfall, and
byte-determinism across two consecutive derivations and a relaunch.

That discharges step 1 of cells 5/6's procedure in the pattern-identity ruling:
the derived week is proven LAWFUL in the pin's own world.

## (d) THE PARITY GATE — RED, WITH THE ROW NAMED

`lawful-5`: derived == materialised, byte-equal, taken in the one instant both
representations exist — immediately before the world builder drops the stored
week.

**It reds identically in all three worlds:**

```
2026-08-10  materialised  Lower Squat | 7 rows
2026-08-10  derived       Lower Squat | 8 rows
only-derived ["Vertical Jump"]
```

Three measurements make it a finding rather than a difference:

1. **The generated program's own Monday carries all 8 rows, Vertical Jump
   included.** The derivation reproduces the program; the write path is the
   side that moved. The gate reports the program's own rows, so the direction
   is named by construction, not by argument.
2. **§18 does not discriminate.** Both weeks score identically against one
   contract — 0 blocking / 0 shortfall in W2 and W2r, 1 blocking `conditioning`
   in W3 on BOTH sides. The extra row is not a shortfall repair, and its
   absence is not an authorised reduction.
3. **Non-vacuity is asserted before equality.** The gate fails if the stored
   week materialises no days, so it can never pass by having nothing to
   disagree about.

**This is the recorded two-engines residual, given a row-level coordinate.**
`fixtureIdentityTests` has said since 2026-08-06 that "the pure resolve says
`Lower Squat|8` … the published replan says `|7`", with `paidBy: R5, when
derive() has no rivals and there is no published week left to disagree`. The
parity gate reaches the same coordinate from the other direction and adds the
row's NAME.

**Pre-existing, and now honest rather than declared.** On the merge target
`feat/stage-b-stage2` (`eb93923e`) `test:fixture-identity` is 5/0 — because
cells 3 and 5 are DECLARED REDS there. This unit deleted both declarations, so
the same defect fails outright. The branch's red is the ratchet working, not a
regression this unit introduced.

## (d) LEG (v) ON ITS REAL PRICE — 9 NEW, 1 FIXED

Leg (v) is the published week's DECLARATION retiring, and the payload the
accept path materialises beside it going with it. Built in a pricing worktree,
unflagged, at BOTH sites the declaration is written from: the publication call
site (`weekRebuild.ts`, the scaffold's own flag site) and the overlay BUILDER
(`buildWeekScopedWorkoutOverlay`, which stamps the field onto every overlay it
makes). World identity printed by `sweep.sh`; both arms at HEAD `77565f00`.

| arm | failures of 155 |
|---|---|
| control | **2** — `program-control-durable`, `fixture-identity` |
| leg (v) | **10** |

**Diffed as sets: 9 NEW, 1 FIXED.**

**THE ONE FIXED IS THE ONE THAT MATTERS.** `test:fixture-identity` goes
**6/6** — including cell 3 (*a published week equals the week derived from the
same inputs*) and cells 5/6 (*one composer, and it is the deriver*), the pins
the pattern-identity ruling re-ruled and the exact witness condition 1 names.
Leg (v) IS the payer the branch's own comment nominated, and the measurement
says so.

**The nine new:** `accepted-state-transactions`, `athlete-session-deletion`,
`readiness-ownership`, `deriving-device-commit`, `illness-clear-game-week`,
`fact-horizon`, `action-walker`, `action-walker:deep`, `phase-structure`.
Characterised only by suite name — **none is diagnosed**, and the two walker
tiers going red means the D13 decompose's green does not survive this arm
either. Nine is a price, not a verdict; what they have in common is not
measured.

## THE SAME ARM, TWO WORLDS, TWO ANSWERS — AND BOTH ARE MEASURED

Under the identical arm, in the world `derivedWeekLawfulnessProof` builds
(`applyPlanChange` removal + `executeFixtureMutationTransaction` add):

| probe | control | leg (v) arm |
|---|---|---|
| stored overlay carries `exposureContractV2` | true | **true** |
| stored overlay materialises days | yes | **yes** |
| parity gate | RED (`Vertical Jump`) | **RED, same row** |

So leg (v) clears the published week on the fixture-identity path and **does
not clear it here**. The stored week in this world survives the removal of both
declaration writers, which means **at least one more producer exists and is not
named**. One further coordinate, recorded and not chased: that stored overlay
answers `id: undefined` and `reason: undefined` while carrying both a contract
and a payload, so it is not an object either overlay builder produced.

**One candidate NAMED AND REFUTED BY TAPE, not by reading.**
`postGenerationConstraintValidation.ts:1742` reads exactly like the producer —
it falls back to the microcycle's contract when the overlay carries none, and
at `:1813-1817` writes BOTH `exposureContractV2` and `workoutsByDate` back onto
the overlay from `accepted.canonicalWorkouts`, a §18-accepted recomposition
that is also a candidate for where a power row stops surviving. Taped on the
parity path: **zero hits**. It does not execute there. The executes-on-path law
did its job; the source reading alone would have produced a confident wrong
attribution.

## CONDITION 1'S RE-MEASURE — NOT GREEN, RESIDUAL NAMED

Condition 1 asks for the full set priced once more, and rules: *green → build
in order; not green → STOP with the residual named.*

Priced on the branch: **2 of 155**, one of which is `fixture-identity`, whose
residual is now named to the row — Monday's `Vertical Jump`, present in the
program and in the derivation, absent from the published replan. **Not green.**

Cells 5/6's step 2 (re-pin to the derived week's literal values) is **NOT DONE,
deliberately**: the cells already pin the derived answer (`|8`) and receive the
replan's (`|7`). Re-pinning them to 7 would be
`expectation-edited-to-match-the-regression` with a ruling for cover. The pins
are right; the write path is short a row — and leg (v) greens them.

The other of the two, `program-control-durable`, is unchanged and still
declared to the door-unification debt.

## WHAT IS ON THE BRANCH

`77565f00` — the matcher decomposition, the ported proof and the parity gate.
All three are harness. **No product code moved in this pass.**

`test:derived-week-lawfulness` is deliberately **NOT in the bible chain**: it
carries a red that is the same declared defect as `test:fixture-identity`, and
a second suite reding for one reason double-counts the debt. It joins the chain
in the commit that greens it.

Gate: full `test:bible` UNPIPED, **EXIT 1 on `test:program-control-durable`** —
the declared red, unchanged; the chain stops there, so the 63 suites after it
are measured by the sweep instead. `test:compile` **EXIT 0**.

## THE COMPRESSION THIS PASS OWES

**An arm is not believed until a positive control proves it changed the state
it claims to change, in the world the claim is about.** `sweep.sh`'s preamble
proves the symbol exists in `src`; that is presence, not effect. Had the arm
only ever been measured on the fixture-identity path it would have been
recorded as "leg (v) retires the published week", full stop — and the parity
world says otherwise under the same arm. The rule that pays it: every priced
arm carries one probe reading the state the arm claims to change, in both arms,
in the world the conclusion is about; a price measured without that probe is
reported as unproven rather than as fact.

Same discipline as the standing harness-lies pair (files not stdout; no zero
without a same-run positive control), extended from the INSTRUMENT to the ARM.

## FOR THE SEAT — THE DECISION THIS PASS PUTS UP

Leg (v) is now a measured trade, not a hypothesis: **it pays the switchover's
last carried red (`fixture-identity` 6/6) and costs nine suites.** Under the
standing conditions it cannot land in this state. The shapes, none taken:

1. **Diagnose the nine first**, worst-first, as with leg (ii) — the two walker
   tiers and `accepted-state-transactions` are the laws, the rest may share a
   root. RECOMMENDED, on the unit's own precedent: leg (ii)'s seven were one
   class once measured.
2. Land nothing and re-scope leg (v) to the path fixture-identity exercises
   only — rejected on sight as a half-state; the eighth-pass ruling already
   said materialisation and declaration retire together.
3. Name the parity world's third producer first, since leg (v) demonstrably
   does not reach it — cheap, and it decides whether option 1's nine are even
   the whole cost.

Nothing is proposed for the phone in this pass. Sam's single device-pass flag
(completed-day display) is unchanged.

## NOT COVERED

- The nine new reds are named, not diagnosed; no root is claimed.
- The producer of the stored declaration/payload in the parity world.
- Whether the `Vertical Jump` drop and the two team-night pairings the
  2026-08-06 note records are one mechanism or several.
- `test:compile` was run on the branch, not per pricing arm, as before.
