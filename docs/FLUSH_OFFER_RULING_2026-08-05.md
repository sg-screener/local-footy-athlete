# In-season flush offer — Sam's ruling, 2026-08-05 ("a")

The optional flushout is a REQUIRED OFFER on every in-season week: the
app always presents it; doing it is the athlete's choice (same pattern as
the recovery door). Bible :81's three ideal structures are conformance
targets on this point, not illustrations.

Supersedes the signed game-week threshold at weeklyExposureContractV2
(optionalFlush min 0 → min 1). The offer does not count toward :127's
in-season conditioning target arithmetic — TT + game still satisfy the
target; the flush rides as the always-present optional.

Sunday-ends-in-recovery (:81 "rest or recovery") builds alongside,
unambiguous per the terminal's read.
