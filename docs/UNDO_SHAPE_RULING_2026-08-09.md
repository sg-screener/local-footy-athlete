# UNDO SHAPE — SAM'S RULING (2026-08-09)

**Committed as authored, before a line was built.** This is the record of what
Sam ruled, not the seat's reading of it.

---

## THE RULING, VERBATIM

> "it should be last change i think? you can already undo changes using coaches
> notes for older things."

## WHAT IT DECIDES

**UNDO LAST CHANGE — a single step.** Not a recent-changes list, not a
wind-back depth chooser.

The replay unit's kickoff addendum left this open and reserved it for a design
moment:

> Depth/shape (one-step undo vs a wind-back "recent changes" list) is an OPEN
> design choice — seat brings lettered options + recommendation at unit
> opening. Not pre-decided here.
> (`docs/REPLAY_UNIT_KICKOFF_2026-08-07.md`, addendum 2026-08-09)

**The question was answered before the moment arrived.** The seat never brought
the lettered options; Sam ruled without them. Recorded as a LOOP CHECK —
`design-question-answered-by-owner-early`, sighting 1 — because the cheapest
design moment is the one that never has to be staged.

## THE GROUNDS, WHICH ARE HIS AND NOT THE SEAT'S

Coach Notes already carries the athlete's route to unwinding older adjustments.
A second history list would be **two answers to one question** — which is the
defect class this repo names most often and the north star's own objection.

## THE BOUNDARIES THAT STAND UNCHANGED

Carried from the kickoff addendum; this ruling touches none of them.

- **Program DECISIONS undo via the ledger** — annul the entry, re-derive.
- **Recorded FACTS are NOT covered by this button.** A saved session outcome or
  a journal note is not a program decision; its undo is remove-last-entry at its
  own store's door, designed separately.
- **A logged session never silently vanishes.**
- What "everything" covers at v1 follows from the dependency list's measured
  map — the ledger has no vocabulary for illness, injury, readiness or phase —
  **stated honestly, never overclaimed.**

## THE ONE PROCESS CONDITION SAM ATTACHED

**The surface comes MOCK-FIRST, before any screen is built.**

This is the journal lesson promoted to practice. That unit's surface outran its
design, Sam's eye pass vetoed it outright, and the tab is now hidden
(`docs/JOURNAL_HIDDEN_RULING_2026-08-09.md`). The mock is
`docs/UNDO_MOCK_2026-08-09.html`, and no undo screen is built until he has
ruled on it.

---

## SEAT NOTE — THE GROUNDS ARE A CLAIM, AND CLAIMS GET MEASURED

Recorded here rather than acted on silently. The ruling's grounds assert
something about an existing surface: *"you can already undo changes using
coaches notes"*. This repo's standing law is that **a ruling premise is a claim
too — quote the line before building against it.**

The claim has two halves, and only the first is answerable by reading source:

1. Coach Notes carries an undo route — `clearReversibleAdjustment`, reached
   from `ActiveCoachNote.reversibleAdjustmentId`. **TRUE by inspection.**
2. That route actually unwinds the change. **Measured, and the answer is in
   `docs/LR29_UNDO_ROUTE_TAPE_2026-08-09.md`.**

**Nothing in the measurement changes the shape Sam ruled.** One-step undo
stands. What it changes is which MECHANISM the button is built on — and it
points at the one the kickoff addendum already ruled.
