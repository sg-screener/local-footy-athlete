# COACH REBUILD KICKOFF — 2026-08-09, Sam-approved

LOOP CHECK: mock-first-then-kickoff — sighting 2 of the practice
(undo was 1) — iterate; this is the journal lesson working.

Sam saw docs/COACH_TAB_MOCK_2026-08-09.html and approved it with three
rulings, verbatim:

1. "I think the initial message can be shorter, I want the coach to
   mostly get to the point and ask questions when he doesn't know.
   it's important that the coach understands the bible and all of my
   rules - I basically want it to be my brain yeah?"
2. "I really like the little pop up 'the change' that makes it clear
   what was changed and why"
3. "I want all the buttons working properly, no hidden enter button
   behind the keyboard pop ups or anything - i want nike level UI and
   interaction"

## THE THREE LAWS THOSE RULINGS CREATE

L-C1 THE BRAIN LAW. The coach's knowledge IS the Bible plus Sam's
recorded rulings — the app-side twin of the seat's BIBLE-FIRST law.
The coach never states coaching policy that contradicts the Bible or a
ruling; when a recorded rule answers, it answers from the rule; when NO
rule answers, it ASKS the athlete or says it doesn't know — it never
invents policy. ("ask when no fact answers; derive when one does;
never both" — now binding on the coach's mouth.) Every proposed change
still passes the same validation the athlete's own taps pass; the
coach cannot propose what the door would refuse without showing the
refusal honestly.

L-C2 THE CHANGE CARD IS THE CONTRACT. Sam-loved, now law: no coach
mutation without the card — what changes, from what, to what, and why,
before the athlete's yes. The card renders from the ProgramControlAction
itself (one projection — no surface composes its own words about a
change), so card and change cannot drift.

L-C3 THE NIKE BAR. Interaction quality is a gate, not a polish pass:
no control may ever be occluded by the keyboard (the onboarding
keyboard unit's lessons apply from slice 1, not retrofitted); no dead
tap zones (the day-first invisible-witness lesson); send/confirm
reachable at every keyboard state; 60fps on the conversation scroll.
Device pass includes explicit keyboard-up cases for every interactive
element. Voice: SHORT openers — the coach gets to the point.

## ARCHITECTURE (settled, not reopened)

docs/COACH_ARCHITECTURE_REASSESSMENT_2026-08-09.md §0 governs: the
coach's output becomes a ProgramControlAction; the athlete's own door
executes it; the ledger records the door's vocabulary verbatim. No
coach writer, no coach adapter, no ninth representation. Undo/replay/
durability arrive free through the door. Remaining ledger-door work
(injury/illness/readiness destinations) stays ENGINE work and follows
its own reassessment §9 boundary — not rebuilt inside coach modules.

## SLICES (each ends athlete-visible, mock-first honoured, full chain)

S1 THE TAB TALKS. Coach tab restored at the navigation owner (one
   Tab.Screen block, the R5.7 comment's own promise). Conversation
   shell to L-C3's bar. SHORT opener composed from real data
   (fixtures, week shape) — read-only, zero mutation paths. All copy
   PROPOSED -> batch.
S2 IT ANSWERS FROM THE BRAIN. Read-only Q&A grounded in the program,
   the week, and recorded rules per L-C1. The salvage layer (intent,
   target resolution, clarifiers, truth gate — the survey's living
   half) is re-pointed here, not rewritten. No mutation.
S3 IT CHANGES THINGS. The change card (L-C2) + confirm + execution
   through executeProgramControlAction; ledger records; undo covers
   it. Allow-listed kinds first (the exercise door already landed
   2026-08-09); kind-by-kind behind the same card.
S4 IT KNOWS HOW YOU'RE TRACKING. Journal record as context: load,
   regions, feel, niggles — NOT free-text notes (survey §6.5: the
   privacy-clean first cut; the notes opt-in decision stays parked
   with Sam's earlier ruling).

Retirement rides the slices: as each new piece supplants frozen
modules, census-before-retirement applies; the frozen root
(CoachScreen) is retired when S1-S3 replace its every reachable duty —
that answers the overnight pass's parked root question by supersession
rather than surgery.

Inherited owed work lands inside slices (survey §7): MetCon rename
(S1-S2 window, LR-6 lifts), three unsigned fallback sheets, the
CoachStackParamList ghost routes, D-2 hydration-repair branch (its
"just move it" is a precedence decision — surface to seat when
reached).

## WHAT REACHES SAM

Slice boundaries in plain words; copy batches; the S1 device pass
(with keyboard cases); the notes opt-in decision ONLY when S4+ wants
notes. Voice/wording tuning happens against his "get to the point"
ruling — bring examples, not questions.
