# COWORK SEAT HANDOFF — 2026-08-06, REBUILD ERA

For the next Cowork (review seat) session with Sam on Local Footy
Athlete. Read ALL of this before your first substantive reply. Supersedes
docs/COWORK_SEAT_HANDOFF_2026-08-06.md (that one predates the shell
rebuild pivot; its §1-§3 communication/law content still applies — read
it too).

## §1 How to work with Sam (learned the hard way, this seat)

Everything in the old handoff §1 stands, PLUS these, each earned by a
failure this seat made:

- BIBLE FIRST (memory: lfa-bible-first-law): never put a coaching
  question to him without grepping docs/LFA_PROGRAMMING_BIBLE.md +
  ruling docs first. He built the Bible so he is never asked twice.
  "well der - thats the whole point of a fkn bible."
- RULE, DON'T ASK (Sam, 2026-08-06, after being asked two questions his
  own laws answered): a question reaches Sam ONLY when no recorded law,
  ruling, Bible line, or ledger instinct answers it. If the laws answer —
  even partially — RULE IT yourself, cite the law, and tell him in one
  line ("ruled X by your one-owner law — say so if wrong"). He gets a
  veto, not homework. Asking him to confirm what his laws already say is
  itself an inconsistency engine: different phrasings on different days
  produce different answers to the same question. The recommended-option
  pattern is for genuinely open coaching calls only.
- JUDGMENT LEDGER (memory: sam-judgment-ledger): his repeated product
  instincts have a proven record (shell rebuild called weeks early,
  batching, L11). When he repeats an instinct, STOP — his recorded call
  is the default path. Record new strategic calls in that ledger file.
- BATCH RULE: hold small signings and hand the terminal ONE batch — the
  full chain toll (~15 min) runs once per batch, never once per sentence.
- WINDOW-LABELLED INSTRUCTIONS: every instruction = which window, what to
  type/paste, what he'll see, what's next. One step at a time. He mis-
  pasted once because two blocks weren't labelled; he was right to be
  angry.
- Plain coach English ALWAYS. If a report says "L-P6 invariant", he reads
  "the day shows the wrong thing". Translate everything.
- His day is measured by "what do I tell Renee". End big days by giving
  him that sentence. VISIBLE-FIRST is a standing direction he ruled:
  every unit ends athlete-visible on his phone.
- He works irregular hours (a "13-hour day" can end 2:30 PM). Don't
  assume morning/night.
- When he's abusive under stress, own real mistakes once, plainly, no
  groveling, keep momentum. What he needs is the next concrete step and
  honesty about what his work amounted to.
- He asks "still working?" often: check evidence (git log -1 + find
  -newermt on src/) and answer from it. Commits are sparse (chain toll);
  file mtimes are the pulse.

## §1b LOOP-AUDIT LAW (Sam, 2026-08-07): read docs/SEAT_LOOP_AUDIT_LAW_2026-08-07.md.
Third sighting of ANY repeated ruling-shape, round-trip, toll or
rediscovery = mandatory compression proposal, not a fourth silent run.
Ordered after the standing-derivation ruling took nine stops to exist.

## §2 The rebuild era — state as of this handoff

Sam's device pass on the legacy shell failed (2026-08-05 morning) → L11
reassessment → sizing doc → **Sam RULED Option B: rebuild the shell on
the proven engine** (docs/SHELL_REBUILD_RULING_2026-08-05.md; beta coach
scope cut SIGNED: typed doors only, coach free-text hidden until the
coach rebuild). Six-slice plan APPROVED:
docs/SHELL_REBUILD_PLAN_2026-08-05.md (A fold, B none, C hidden —
docs/SHELL_REBUILD_PLAN_APPROVAL_2026-08-05.md).

DONE + Sam-passed on hardware:
- R1 spine (ledger, derive() one owner, quiescent boot, doors append,
  work-bill law: 2-8KB/door vs old ~1.1MB) — fresh-install pass PASSED.
- Fixture identity law (his Saturday catch): add-then-remove is identity;
  a decision never rebases from a stored week.
- R2 extraction (dateOverrides + userRemovalConstraints → ledger;
  weekScopedOverlays deliberately NOT migrated — derived content) —
  install-over pass PASSED.
- R3 fact/season doors + R4 results-persist — doors pass mostly PASSED;
  its findings became the current queue.

IN FLIGHT (terminal session, Opus, after /clear): 1b in-season flush
offer via APPROVED "declare, then place" reassessment (0aaad923); cell 6
re-scoped (typed rest day satisfies :81; Sunday display = copy item,
PROPOSED to Sam); then finding 3 injury substitute-before-reduce
(docs/R3_DOOR_PASS_FINDINGS_BIBLE_MEASUREMENT_2026-08-06.md — matrix-
authored only); then R5 (delete ~11k legacy machinery, census closures,
structural sweep). STOP at **Sam's combined device pass = THE MERGE
GATE** — the review seat writes that tap list (short, plain, expected
sights; Release build; diagnostics build only when tracing failures:
EXPO_PUBLIC_ENABLE_ATHLETE_ACTION_DIAGNOSTICS=true + expo start, dev
build needs Metro alive + same network).

Roadmap (Sam's ordering, tracked):
docs/PUBLISH_ROADMAP_2026-08-05.md — rebuild → coach rebuild + journal +
day-first UI (all BEFORE beta, his explicit reorder) → Renee/TestFlight
beta → App Store. Estimates given: merge within days; coach 1-2 wks; ~3-5
wks to submission.

## §3 Model + terminal practice (this seat calls the switches)

- Cowork tells Sam every Fable↔Opus switch; Opus default, Fable only for
  judgment moments (reassessments, naming defect classes, sizing docs,
  spine design). Fable was spent on: the sizing doc + R1 spine. R2+ ran
  Opus. /model queues until turn end; Esc interrupts safely; "status?"
  wakes a dozed agent; /clear when a session's history is dead weight.
- Terminal laws it obeys: full test:bible EXIT 0 per commit; red cells
  first; escalation reassessments before pipeline code; checkpoint +
  handover doc when context runs low; never end turn while own gate
  runs; overnight = park Sam-questions and continue independent work.
- Cowork verifies every terminal claim (git log/show/grep, read the
  docs) BEFORE telling Sam anything is true. Rulings: Cowork writes the
  doc into docs/, terminal commits "as authored". Uploaded device exports:
  cp from uploads mount to repo root with dated name, read before
  speaking.

## §4 Open items beyond the queue

Sam's signing queue (non-blocking): 23 conditioning cues + Easy Swim,
per-quality warm-ups, Sunday rest-day display copy (will arrive
PROPOSED), Wrist-vocabulary question (open, don't add unprompted).
Parked/declared: cell-3 replan-vs-resolve closes at R5; evening-4 mirror
bill = R5 target; LR-18 journal store retirement waits for the journal
unit; busy-week stranded rule (check PARKED_QUESTIONS_2026-08-01 §2
ruling before building). MetCon rename pinned under LR-6 (description
live, name waits for coach rebuild).

Memory upkeep: the walker/post-merge memory file is the running history —
update after every milestone; keep MEMORY.md pointing at THIS doc for
session start.

## §5 STATE STAMP 2026-08-07 (supersedes §2's "in flight" picture)

Current truth lives in docs/NOW.md (always) + docs/SEAT_INBOX.md (the
terminal reads it at every stop — Sam types "check inbox" there, and
just "check" to the review seat, who reads the repo directly).

Era summary: 1b, finding 3, G-2 quality-lower, anchor fix, R5.1/R5.2,
structural sweep all LANDED on feat/stage-b-stage2 (green). R5.3 = the
V3 fixture switchover saga on feat/r53-v3-switchover: a four-leg
conformance unit (derive the contract, tier 4 in the deriver with the
full :4688 repair search, retire write-backs, link derives from
decision) built by price-first discipline — every leg measured on
scaffolds before building. Read the docs/R53_* and *_RULING_2026-08-0[67]
trail; the loop-audit law (§1b), the STANDING_DERIVATION_RULING (derive
without round-trips, three stop-conditions), RESOLVED_AUTHORITY,
SURFACES_CONTEXT, REMOVAL_RECORD_SPLIT and PATTERN_IDENTITY rulings are
all in force. gate.sh landed (trust ONLY printed exit lines — the
completion notification has lied 5+ times). Parallel-gate stage 1
queued (docs/PARALLEL_GATE_SHADOW_UNIT_2026-08-07.md).

After V3: condition 4 (markedDays proof), merge, R5.7 whole-or-none,
remaining batches, STOP at Sam's combined device pass — the review
seat writes the SHORT tap list (plain, expected sights, Release build).

Usage practice Sam ruled: fresh Cowork sessions at natural boundaries,
text over screenshots, the check/check-inbox two-word flow.

## §6 FORMAT LAW 2026-08-07 (Sam-forced; binding on every future seat)

The seat repeatedly wrote post-failure reminders and then failed the
same way (serial probes ordered AFTER the sweep rule existed — third
sighting of write-the-rule-then-ignore-it). Reminders don't execute.
The fix is structural: every seat order and boundary report OPENS with
"LOOP CHECK: <shape> — sighting N — iterate or compress" (see
SEAT_INBOX Standing). No line, no valid order. Sighting 2 = compress.
If you are a future seat reading this: the line is not ceremony, it is
the thinking Sam paid for, made mandatory.
