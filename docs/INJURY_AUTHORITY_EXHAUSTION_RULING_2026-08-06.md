# INJURY-AUTHORITY EXHAUSTION RULING — 2026-08-06

Ruling on the fix shape proposed in the diagnosis committed at 45fb91cb
(main-strength exhaustion, upper_body 9/10 x G-2 intersection).

Per RULE-DON'T-ASK, ruled by the review seat from recorded law; Sam
holds a veto. Governing lines:

- Bible `:4688` repair order, step 4: relocate → substitute → move
  optional work → THEN "record an explicit typed reduction for any
  unavoidable shortfall". This week is step 4's authored subject.
- 8-10/10 band (`:1920-1926`): pause affected training; clearly
  unaffected training only.
- 6-7/10 band precedent: "Create a clear Coach Note so the athlete
  knows the program is being adjusted" — disclosure is authored, not
  optional.
- `:4108`: a door that claims recomposition while changing nothing is
  forbidden.

## APPROVED

Main strength gains the same exhaustion proof the equipment domain has
always had (substitutionStatus-shaped: not_attempted | substituted |
exhausted). MEASURED exhaustion — proof, never inference — emits a
typed `main_strength_frequency` reduction; projection and authorisation
already work off that metric and are untouched. The husk becomes
unrepresentable: a day never ships titled with zero rows. The Coach
Note owes the reason; its copy ships PROPOSED for Sam's signing batch.

REJECTED (as the diagnosis itself proposed): widening
hasFrequencyReduction to accept strength_pattern_count — a pattern
statement standing in for a frequency one greens unearned shortfalls.

## Binding conditions

1. Instrument FIRST the placement function that consumes the G-anchor
   constraints — the G-2 conclusion is currently inferred from quoted
   anchors plus day arithmetic, not stepped. Verify before building.
2. The matrix question joins the gate: a no-game world with the same
   injury must NOT reach exhaustion (three free days carry the count);
   exhaustion must be witnessed only where it is real.
3. The typed reduction is emitted only from measured exhaustion at the
   owner — never authored by a repair path, per the finding-3 boundary.
4. The `:319` citation drift (Bible `:4755` cited under an Equipment
   line; injury bands are `:1920-1926`, `:2200`, `:4108`) is corrected
   in the same unit.
5. R5.1 pops only after this pays test:injury-authority green.
