# LEDGER-LINK DERIVATION RULING — 2026-08-07

Rules the question at docs/R53_RESTORATION_REPRICE_STOP_2026-08-07.md.
Ruled by the review seat per RULE-DON'T-ASK; Sam holds a veto.

## Ruling — the link derives from the DECISION

linkedTypedReductions is computed from the decision (the constraint
plus the reduction withRemovalLedger re-applies at read) — never from
a stored before/after contract diff. The diff is stored arithmetic;
leg (i) retires that class by design, and the link joining it would
be the class reinstalled. Authorised as the FOURTH LEG: own install
sites, priced before built, condition 1 unchanged. Cell 23 is
expected to pay as the same root's other side; if it does not, that
is a finding, not a disappointment.

## Also ordered in this unit

1. The flags-off fixture-identity red (3 cells red on the branch
   BASELINE) is diagnosed by bisect over the branch's own commits
   before the next pricing — condition 1's 6/6 is unmeetable until
   its cause is named, and no leg can move it. It predates the legs,
   so it is findable the same way everything today was found.
2. Cell 7 is diagnosed (not fixed) in the same pass — named cause or
   declared with a payer.
3. The env-var harness defect (unquoted expansion labelling clean
   runs as pairs) is recorded as a measurement-method law in the
   report: env assignments are always written literally, never
   through a single expanded variable.
4. The two tracked debts (hydration pin, todayISO clock) remain owed
   in the main build. The queued snapshot-to-reference unit stays
   queued.

## Then

Price the four legs together. Green → build → debts → run home
(unpiped bible, condition 4 markedDays proof, merge, post-merge
unpiped bible, R5.7, remaining batches). Not green → STOP with the
residual named.
