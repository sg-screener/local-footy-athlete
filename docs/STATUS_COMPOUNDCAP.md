# STATUS — seat `compoundcap`

Owner: `compoundcap`. Started 2026-09-02 from exact product checkpoint
`3e6ac18bf90c73de2996fdc43fafc6fcf9a6a69b`; current HEAD adds only two
audit-tool commits and remains an exact descendant of that checkpoint.

## Boundary

- Add one optional `compound` / `isolation` answer to the canonical exercise
  metadata and make every selector/test read it.
- Limit automatically composed strength sessions to four compound exercises.
- Replace upper/lower support compounds with legal isolation, then prehab,
  then core/robustness, then an empty slot.
- Preserve equipment, injury, weekly identity/family limits, block stability,
  restart reconstruction and unrestricted athlete-added work.
- Do not run a full-year audit, generate PDFs, touch a phone or repair unrelated
  red suites.
- Preserve the existing uncommitted `docs/NOW.md` and every unrelated tracked
  or untracked shared-worktree path.

## Options compared before coding

1. Trim fifth-and-later compound rows after a session is composed. This is the
   smaller patch, but the removed rows have already affected block selection,
   weekly identity memory, traces and restart state.
2. Put the classification on the one exercise metadata owner and reject an
   illegal support compound at the existing selection boundary, before a row or
   durable choice is authored. Support slots then walk the ruled fallback ladder.

Option 2 is the target. It removes the class at its decision point and keeps the
saved choice equal to the athlete-visible choice.

## Starting overlap check

- `weeklyselector` owns the completed R-318 weekly identity/family selector at
  the exact checkpoint; this task extends that same boundary rather than adding
  a parallel post-processor.
- `yearrepair` has current broader annual programming work recorded, but the
  working tree contains no uncommitted production or focused-test file from it.
- `cap` owns the older seven-total-strength-row policy. That is a separate cap:
  this task limits only compound exercises to four and leaves the seven-row
  session policy intact.

## NOT COVERED (starting state)

- No full-year audit, PDF, simulator, Release build or physical phone.

## Cause measured

The composer treated the optional upper support seats as another visit to the
horizontal/vertical press and pull pools. Those pools contain compound presses,
rows and pulldowns, and the weekly selector tracked identities and main families
but had no session-level compound count. A generated full-upper founding world
therefore delivered six compounds: Bench Press, Chest-Supported DB Row, Seated
DB Press, Pull-Ups, Push-ups and Lat Pulldown.

## Change landed

- The existing exercise-tag record now owns the only optional
  `compound | isolation` answer. Its governed pool-derived scope is 84 distinct
  identities: 49 compound and 35 isolation, with zero missing. The focused
  contract checks every ruled pool/category and keeps Face Pull and Cable Face
  Pull distinct; power, carry, core and prehab controls remain unlabelled.
- The whole-week selector now opens one session state per planned day. A
  compound spends one of four seats and may fill only an uncovered genuine
  strength direction. Isolation and unlabelled rows cannot impersonate a main.
- Optional upper support reads the isolation pool's authored muscle groups.
  Fallback is legal isolation, genuine prehab, core/robustness, then empty.
  The old core-as-prehab overlap was removed so the rungs remain real.
- Slot coverage recognises isolation support by canonical metadata plus authored
  muscle group. A compound press or pull no longer fills a support seat.
- R-319 is recorded in the Bible, ruling registry and guarded law registry. Its
  focused tape is chained through `test:full-body-balance`.

The same founding shape now produces Bench Press, Chest-Supported DB Row,
Seated DB Press and Pull-Ups as its four compounds, then Tricep Pushdown and
Face Pull as isolation support.

## Focused proof

- Red first: the new focused tape was 0/7 before the metadata, session owner and
  final audit existed.
- Final chained boundary after the grouping correction: `test:full-body-balance`
  is 41/41 across balance,
  weekly variety, weekly main budget, automatic weekly selection and the new
  compound-session tape. Its 13/13 durable arm includes exact save/restart and
  unrestricted athlete-added duplicate work.
- New tape: 8/8, including the corrected 13-name lower-accessory grouping, the
  exact founding six identities, dedicated lower
  behavior, a two-week slice from four real generated worlds, commercial and
  bodyweight kit, male/female, all three phases, and a 6/10 shoulder injury.
  Every reached automatic row remained legal for its kit and every classified
  row remained legal for the injury.
- Adjacent focused compiler checks: generated-week assembly 30/30, slot coverage
  84/84, and TypeScript product/devtools/tests 0/0/0.
- Liveness mutation 1: temporarily changing the canonical maximum from four to
  five made the tape 6/7; the deliberate fifth-compound cell alone failed.
  Restoring four returned 7/7.
- Liveness mutation 2: removing Back Squat's canonical classification made the
  tape 4/7; the exact-boundary, completeness and mutation-subject cells failed.
  Restoring the field returned 7/7.
- Law registry structure, named scripts, chain membership and new R-319 row all
  pass. The overall law-registry command remains red only on its inherited 21
  distinct UNENFORCED law rows (13/14 cells), which this task may not repair.

## Unresolved

- `test:upper-split-composition` remains 22/29 on seven inherited source-shape
  expectations that already disagree with the checkpoint's six-slot split
  tables and current football-foundation composition. This repair does not
  change those contracts or widen into them.
- Physical-device acceptance remains open under L10.

## Classification grouping correction — 2026-09-02

Sam moved SL 45° Back Extension plus the five calf/groin isolations into the
same `Lower Accessories — Isolation` heading as the seven existing lower
isolation-pool exercises. Their canonical `isolation` values were already
correct, so production metadata and programming did not change. The guarded
contract now derives the complete 13-name grouping from the existing hinge,
lower-isolation, groin and calf pools and checks it against Sam's exact set.
No exercise pool, calf/groin/muscle tag or selection route changed.
Temporarily changing Copenhagen Plank (Half) from isolation to compound made the
focused tape 5/8: the corrected grouping, exact classification boundary and
49/35 classification-split cells all failed. Restoring its existing value
returned 8/8.

## NOT COVERED (final)

- No full-year or 52-week audit and no broad profile census beyond the named
  focused four-world/two-week sample.
- No PDF or other generated artifact.
- No simulator, native glass, Release build or physical iPhone.
- No broad `test:bible` run and no repair of the 21 inherited UNENFORCED laws or
  the seven inherited upper-split source-shape expectations.
