# Live Legacy Wiring Census — 2026-07-24

Read-only census, no code changes. The inverse sweep of
`unreachable-product-files` (109 files dead from `App.tsx`): this document
covers only code reachable from the three live tabs (Program:
`HomeScreenV2`/`DayWorkoutScreenV2` + `PlanChangeSheet`/`GuidedInjuryFlowSheet`/
`EquipmentLimitationSheet`/`SessionFeedbackPanel`; Coach: `CoachScreen`;
Profile: `ProfileScreen`/`FAQ`/`Privacy`/`Terms`) plus the 21-screen onboarding
stack — i.e. wiring an athlete can actually hit today, that predates or
diverges from the current accepted-state-transaction / curated-copy owners.
`HomeScreenClassic`, `DayWorkoutScreenClassic`, `HomeQuickActionSheet`, and
everything under `src/screens/{journal,program,workout,auth}/` are confirmed
unreachable (see `docs/audits/DEAD_AFFORDANCE_INVENTORY_2026-07-23.md`) and
are out of scope here.

**Method:** six parallel static source-trace passes (one per census
dimension below), cross-referenced against `docs/SECTION18_OWNERSHIP_REASSESSMENT_2026-07-22.md`
(the §18 retirement ledger), `docs/audits/DEAD_AFFORDANCE_INVENTORY_2026-07-23.md`,
`docs/audits/FULLSWEEP_2026-07-23.md`, `docs/TYPECHECK_BASELINE_TRIAGE_2026-07-24.md`,
and the unreachable-109-file list, so nothing already tracked is double-counted.
No device pass was run — see NOT-COVERED.

---

## Ranked findings

| # | Surface | Legacy path | Athlete-visible blast radius | Recommended owner / retirement slot |
|---|---|---|---|---|
| 1 | Coach tab — free-text "add a session" (e.g. "add a recovery session Wednesday") | **NEW.** `runAddSession` → `defaultApplyAddSession` (`src/utils/coachCommandExecutor.ts:3109-3160,3620`) writes the new session straight via `useProgramStore.getState().setManualOverride(...)` with **no `UserRemovalConstraint` pin** — bypassing `commitAthleteSessionAdditionTransaction`, the dedicated owner the *tap* door was migrated onto in §18 stage 3 specifically because an unpinned add does not survive §18 (the gateway silently canonicalises it back to `Rest` on the next repair pass, per the reassessment doc's stage-3 "design correction," ~line 638). | **High.** An athlete who adds a session by chatting with the coach can see it accepted ("Done"), then have it silently vanish (revert to Rest) on a later, unrelated §18-triggering edit — a false-Done / silent-content-loss shape, the exact class Process Law L6 calls a release blocker. Every other coach-door mutate op (`move_session`, `remove_session`, `replace_exercise`) already routes through its transaction owner; only `add_session` was missed. | Route `defaultApplyAddSession` through `commitAthleteSessionAdditionTransaction` (same primitive `resolveAthleteMutation`'s tap-door empty-day add uses), retiring the direct `setManualOverride` call. Add a coach-door regression test mirroring `sectionOwnershipInvariantTests` #10 (empty-day add) driven through the chat pipeline. |
| 2 | Program tab (day/weight estimation, rebuild, phase-shift) + Coach tab (AI mutation context) + Profile tab (edit-draft diff → commit) | **Known-but-widened.** `profileStore.ts`'s `useProfileStore.subscribe` compatibility-mirror fence (`:34,106-160`) is scoped, not retired (per the 2026-07-24 mirror-residual note). Every live reader of `profileStore.onboardingData` that hasn't migrated to `acceptedMaterialContext.acceptedProfileSnapshot` directly still treats the mirror as input. This census found the reader set is **entirely decision-feeding, none display-only**: `PlanChangeSheet.tsx:118,295`, `useHomeScreen.ts:252,696-697,817`, `useDayWorkout.ts:115,156` (feeds `estimateStartingWeight`), `DayWorkoutScreenV2.tsx:538`, `CoachScreen.tsx:960,1877-1908` (spread wholesale into the AI mutation-request context — highest-severity single site), `ProfileScreen.tsx:126,136-336,441-470` (seeds the edit-draft that `commitProfileProgramTransaction` commits), plus transitive reads in `coachWeekDiff.ts`, `coachRevisionTemplateContext.ts`, `coachTurnController.ts`, `coachCommandExecutor.ts`, `programControlActions.ts`, `resetCoach.ts`, `postGenerationConstraintValidation.ts`, `programEditWriteGuard.ts`, `activeProgramModifiers.ts`, `planChangeProducer.ts`, `useSchedule.ts`. | **High.** This is the same shape as the confirmed 2026-07-24 fresh-install bug (a stale mirror silently reverting onboarding answers) — but every one of these call sites is a live decision point, not a display, so any future gap between the mirror and the canonical accepted profile can silently misdirect weight estimates, AI-authored plan edits, or profile-commit diffs, not just cosmetics. | No single retirement slot — this is a migrate-the-readers job, largest first: `CoachScreen`'s AI-context spread and `ProfileScreen`'s edit-draft seed carry the highest blast radius per the mirror-residual doc's own trigger ("the next unit that touches accepted-profile readers"). Do not patch the fence again; migrate readers to `acceptedMaterialContext` directly, per CLAUDE.md's escalation rule (this already crossed the "same class of bug reachable a second way" threshold). |
| 3 | Coach tab — chat, injury-progression follow-up (rare failure branch, not gated) | **NEW.** `CoachScreen.tsx` inline reply-builders `buildNoOverrideFallbackReply` (:358-363) and `buildNoVisibleDiffFallbackReply` (:378-380), called from `handleCoachTurn`'s injury-progression branch (:1606,1613), splice raw internal debugging notes straight into the rendered chat bubble: *"Investigate event targeting (likely date / session mismatch)."* / *"Investigate the apply layer or visible-diff verifier."* Not sourced from any copy owner, not `__DEV__`-gated. | **Medium-high** (low frequency, high embarrassment when hit — reads as leaked developer TODOs, not athlete copy). Reachable whenever an athlete reports an injury-severity follow-up and the deterministic apply layer fails to move a visible field. | Move both strings into `planChangeRefusalCopy.ts` (or a sibling "coach fallback copy" module) as plain-language failure copy; keep the diagnostic detail server/log-side only, matching the pattern `generateProgram.ts`'s `ProgramGenError` already uses (curated `userMessage` vs. log-only `diagnostic`). |
| 4 | Coach tab — chat, "swap in a Farmers Carry" / any LLM-phrased carry-family request | **NEW (vocabulary gap).** Three independent, disagreeing exercise-name maps: canonical pools/cues/tags (`exercisePoolsStrength.ts:357` etc.) key on `'Farmer Carry'`; the video-lookup alias map (`exerciseVideoService.ts:445-448`) *does* cover `'farmers carry'`/`'farmer carries'`; but `loadEstimation.ts`'s `EXERCISE_ALIASES` (:268-650), which gates `coachCommandExecutor.ts`'s `resolveExerciseName()` fuzzy matcher (:3059-3080) for the coach-chat `replace_exercise` tool, has **no** entry for `'farmers carry'`/`'farmer carries'`/`"farmer's carry"` — the LLM's free-text tool output (`supabase/functions/coach-chat/index.ts:1090-1108`) fails all three match tiers and is explicitly rejected ("I don't recognise 'Farmers Carry'"). | **Medium.** Fails safe (explicit clarify/reject, not a silent bad write), but rejects exactly the phrasing an LLM is statistically most likely to produce for this exercise family — a real, live UX gap on the coach-chat exercise-swap path. | Add the missing plural/possessive forms to `EXERCISE_ALIASES` (`loadEstimation.ts`) so it matches the video map's coverage; longer-term, unify the three maps into one canonical alias source (video, load-estimation, and pools/cues currently each maintain their own list — a natural follow-up now that content unification work is underway). |
| 5 | Program tab — "Add to this day" on an already-occupied day; any category pick with no resolvable template; add onto a day just vacated by an active removal | **Confirmed-current, already tracked.** The four §18-retirement-ledger residuals (occupied-day stack add, `add_defers_to_legacy_stack`, `no_template_for_category`, removal-constraint re-add) are unchanged, still in `planChangeProducer.ts` (:1009,1018-1021,1152,1163), still falling through to the legacy `applyCoachRevisionDateOverrides` writer (`coachRevisionOverrideWriter.ts:58-208`). One correction to the record: that legacy writer *does* terminate through `setManualOverride`/the accepted-state owner (:187) — these residuals bypass the newer dedicated-transaction pattern, not accepted-state validation outright, a narrower gap than finding #1's raw bypass. Already confirmed live and athlete-visible via `docs/audits/FULLSWEEP_2026-07-23.md` item 8 (raw `"...section18_week_rejected"` error code shown verbatim). | **Medium** (already tracked, cited here only for completeness/non-duplication). | No new action — tracked in the §18 reassessment doc; a future stage migrates occupied-day stack adds through a transaction-owned stack primitive per that doc's own note. |
| 6 | Program tap-door "I'm cooked/wiped → Recovery mode" + coach-chat "I'm cooked" / body-part soreness phrasing | **Legacy-inline-copy**, widened beyond the known `set_recovery_mode` instance. `readinessFactAttribution.ts`'s `readinessFactTitle` is the intended single owner for fatigue/soreness/poor_sleep/illness titles, but `activeProgramModifiers.ts:898` lets any inline `modifierTitle` permanently win over it. Five live call sites still hardcode: `tapProgramModifiers.ts:57` (`'Recovery mode active'`, the known instance), `tapProgramModifiers.ts:87` (`'Load reduced this week'`, an unreported twin on the same tap door), `coachConstraintProducers.ts:237` (same two strings, reached via coach-chat `CoachIntent` dispatch — i.e. the bug is reachable through *two* doors, not one), `coachConstraintProducers.ts:288` (body-part soreness title, coach-chat). | **Medium.** A severity-8 week-scoped fatigue fact should read e.g. "Cooked this week" per the owner's tiered copy but is hard-locked to a generic string on both the tap door and the coach-chat door — same bug class, two entry points, matching the CLAUDE.md escalation trigger ("same class of coach bug appears twice"). | Stop passing `modifierTitle` at these four call sites for readiness-typed constraints (mirror `rules/temporarySourceFact.ts`'s canonical `globalConstraint()`, which already omits it so the owner's fallback applies) — a deletion, not a patch. `readinessConstraints.ts:56,87` is a fifth, likely-superseded instance behind an already-empty-in-practice branch (`buildReadinessActiveConstraints` short-circuits when `temporarySourceFactIds` is populated) — confirm dead before touching. |
| 7 | Program tab — "Busy week," "Away days," "Missing equipment" cards | **Confirmed-current, already tracked** as `NO-OP-DEAD` in `docs/audits/DEAD_AFFORDANCE_INVENTORY_2026-07-23.md` (G7/G8/G9). `commitTemporarySourceFactSet` (`temporarySourceFactTransaction.ts:457,610`) unconditionally passes `skipConstraintProjection:true` into `programStore.ts:2004`'s `canonicaliseAcceptedStateCandidate`, so the fact is durably recorded but the already-materialized week is never re-validated/rebuilt. | **Medium** (already tracked). Classification note for this census: this reaches the accepted-state owner but the owner's real work is disabled by a flag — a distinct defect shape from a true bypass (findings #1 and #5), worth keeping separate in any future fix scoping. | No new action — tracked in the dead-affordance inventory; recommend fixing the flag (make it trigger a real rebuild, or stop recording a fact that visibly changes nothing) rather than adding a bypass-specific patch. |
| 8 | Program tab — undo/restore a Coach Note adjustment, on a rare durable-persist failure | **NEW.** `reversibleAdjustmentTransaction.ts:423,523,1015,1039` falls back to raw `error instanceof Error ? error.message : String(error)` on failure, which reaches `useHomeScreen.ts:1683`'s `Alert.alert('Couldn't restore this adjustment', result.reason ?? ...)` unmodified. The parallel `injuryEpisodeTransaction.ts` module was hardened to never do this — this module was apparently missed. | **Low** (rare failure branch), flagged because it's the same defect class as finding #5's raw error code, just in a native Alert instead of a chat/sheet string. | Curate a plain-language fallback string in this module (matching `injuryEpisodeTransaction.ts`'s pattern) instead of surfacing `error.message`. |
| 9 | Profile tab — `SetupUpdateSheet` text fields; Program tab — `DayWorkoutScreenV2` weight-edit field | **NEW, minor.** Both wrap `AppTextInput` in a raw `ScrollView keyboardShouldPersistTaps="handled"` instead of the shared `KeyboardSafeArea` owner (`ProfileScreen.tsx:1298-1306`; `DayWorkoutScreenV2.tsx:1104,1636`). Not caught by `keyboardConventionContractTests.ts` because that test only bans specific raw components (`TextInput`/`KeyboardAvoidingView`/etc.), not "not using `KeyboardSafeArea`." | **Low.** Both fields get tap-persistence but no scroll-into-view `bottomOffset`/sticky footer/Done accessory — harmless today, real drift risk if `KeyboardSafeArea`'s behavior changes and these two are forgotten (the exact bug class the convention was built to prevent). | Wrap both in `KeyboardSafeArea`; optionally extend `keyboardConventionContractTests.ts` to flag bare `ScrollView` + `AppTextInput` combinations outside the three owner files. |
| 10 | Coach tab — message composer | **Confirmed-current, already tracked/documented exception.** `CoachScreen.tsx:2307` wraps its input bar in a raw `KeyboardStickyView` from `react-native-keyboard-controller` rather than `KeyboardSafeArea`; `docs/ONBOARDING_RELIABILITY_KEYBOARD_UNIT_2026-07-24.md`'s NOT-COVERED section names this explicitly and the contract test deliberately excludes it. | **Low** (already tracked, device-verified working). | No new action — tracked; retire only if/when `KeyboardSafeArea` gains sticky-footer-with-tab-bar-offset support equivalent to what `CoachScreen` hand-rolled. |
| 11 | (latent, not currently rendered) | `generateProgram.ts:1562` stores the raw AI/edge-function reply as `TrainingProgram.description`; no live screen currently renders `program.description` (only workout-level `.description`, which the generator never populates). | **Low today, landmine for tomorrow.** Not an active leak — flagged so a future UI surfacing program description doesn't unknowingly render unfiltered LLM text. | No action needed now; note in the copy-owner docs that `program.description` is uncurated if anyone wires a renderer to it. |
| 12 | (dead code, landmine if reconnected) | `AddExerciseModal.tsx:38` (confirmed orphaned — not imported anywhere in `src/`) has a `'Farmers Carry'` suggestion chip that doesn't match the canonical `'Farmer Carry'`, and its target write path `addExerciseAtDate`/`coachActions.ts:703-712` performs **no** canonical-name validation at all. Also dead: `supabase/functions/generate-program/index.ts` (not referenced from `src/`), which uses plural `'Farmer Carries'`/`'Suitcase Carries'` throughout, mismatched vs. the current singular canonical set. | **None today** (both unreachable), but a real landmine — re-wiring either without first fixing `EXERCISE_ALIASES` (finding #4) would reintroduce the same gap live, with no guardrail at the write site this time. | If either file is ever revived, fix the chip text and add a canonical-name check to `addExerciseAtDate` first — do not reconnect as-is. |

---

## NOT-COVERED (per Process Law L2)

- **No device or Maestro pass was run for this census** — every finding above is a static source trace (grep + call-graph read), not a runtime observation. Per L4 ("device is arbiter"), every finding here needs on-device confirmation before a fix is scoped or a severity is finalized.
- **Coach-chat's other mutate operations' edge cases** — `move_session`/`remove_session`/`replace_exercise` were confirmed to route through their transaction owners at the top level, but their failure/rollback branches were not traced as exhaustively as `add_session`'s.
- **`readinessConstraints.ts:56,87`'s actual liveness** — flagged as "likely-superseded" based on one short-circuit condition in `buildReadinessActiveConstraints`; not proven dead, just not proven live either.
- **Dimension 3's coverage of non-carry exercise families** — the "Farmers Carry" mismatch was traced as the known prototype case; the full `EXERCISE_ALIASES` vs. curated-pool diff was not exhaustively enumerated for every exercise name in the pools (only the carry family, plus the three-map inconsistency pattern that produces it).
- **`coachUpdatesStore.ts:1014`'s `.subscribe()` internals** — confirmed it isn't a cross-store mirror by shape, but its own side-effect bookkeeping wasn't traced past that determination.
- **Accessibility, generation-quality, and non-legacy correctness bugs** (e.g. the separately-tracked Move-to-occupied-day content-loss bug from `docs/audits/FULLSWEEP_2026-07-23.md` item 6b) — explicitly out of scope; this census targets *legacy wiring predating current owners*, not all live defects.
- **Supabase edge-function code beyond `coach-chat` and `generate-program`** — not swept; other functions under `supabase/functions/` were not checked for legacy-copy or legacy-mutation patterns.
- **Full enumeration of `profileStore.onboardingData` readers** (finding #2) — the list given is every call site this census's grep passes found across live-reachable files; a renamed or dynamically-accessed reader could exist and wasn't caught by static grep.
