# STOP — ITEM 1 IS DONE, THREE NEWER ORDERS ARRIVED MID-TURN, AND SIX LAWS ARE OWED TO SAM

**Sam typed "check inbox" three times during this turn.** He was right each
time: the seat wrote three new orders while the previous one was being worked,
and the newest of them supersedes the theory the previous pass was built on.

## WHAT LANDED (`a0c0cabd`)

**Inbox item 1, all six orders.**

- **(c) THE BUILD — THE CLEAN-RESET DOOR EXISTS.** `rules/unreadableWorldReset.ts`
  (pure decisions) + `store/unreadableWorldResetDoor.ts` (the only file in the
  product that clears storage). A stored world the current code cannot read is
  RESET CLEAN and the athlete is told once. `rules/legacyPowerBlockMigration.ts`,
  its suite, its six throw sites, `migrateHydratedStatePowerBlocks` and
  `assertNoUnmigratedPowerBlock` are **deleted**. The generator-recovery lift
  that rode inside the deleted function **survived, with a cell proving it** —
  that was the censused risk.
  `test:unreadable-world-reset`, **73 cells, in the chain, born guarded.**
  Four mutations bite: the first-run trap, "once", the mount, a re-added salvage path.
- **(a)** Accounts **LOCAL-ONLY for v1** — recorded DECIDED, open item deleted.
- **(b)** v1 is **iPHONE-ONLY** — Android marked out-of-scope-v1 where it was open.
- **(d)** The power-row **deletion claim is WITHDRAWN in all five places** it was
  written, AGENTS.md's LAW ZERO founding sentence included.
- **(e)** **20 → 42 of 62 is not a regression**, said plainly in NOW.md and the
  stop report.
- **(f)** Owed to Sam — **below, and it is the reason this stop exists.**

Registry after: **63 rows, 21 guarded, 42 UNENFORCED.** The unguarded count did
not rise. The chain stays red by design.

## THE SIX LAWS NO SCRIPT CAN CHECK — SAM'S RULING OWED, ONE PER LINE

Per his own rule: an uncheckable law is a **finding with a proposed re-wording**,
never a quiet `UNENFORCED` row. He has not seen these.

1. **`LAW-L4-device-is-arbiter`** — *the device is the arbiter; a claim about
   what the athlete sees is settled on the phone, not in a test.*
   **Why no script can hold it:** it says a test is NOT the judge, so a test
   cannot be the judge of it.
   **Proposed re-wording:** *"a claim about what the athlete sees that has not
   been seen on the phone is written OPEN-UNKNOWN."* That IS checkable.

2. **`LAW-L10-phone-is-done`** — *Sam's phone is the definition of done.*
   **Why no script can hold it:** "Sam looked at it" happens outside the repo.
   **Proposed re-wording:** *"a report may not call an athlete-facing change DONE
   while its device line is unseen — it says NOT ON GLASS YET."* A doc check, and
   it would have caught three claims this week.

3. **`LAW-rule-dont-ask`** — *a question reaches Sam only when no recorded law,
   ruling or Bible line answers it.*
   **Why no script can hold it:** it governs what a person chooses to send.
   **Proposed re-wording:** *"a question put to Sam names the registry rows it
   checked first."* That is a doc check against the registry.

4. **`LAW-plain-coach-english`** — *everything said to Sam is plain coach English.*
   **Why no script can hold it:** chat never reaches the repo.
   **Proposed re-wording:** scope it to what IS in the repo — *"every ⚠ SAM line
   in docs/NOW.md is jargon-free."* Same shape as the vocabulary gate that
   already runs.

5. **`LAW-sam-chat-simplicity`** — *every reply is three parts at most, no file
   names or commit ids in chat.*
   **Why no script can hold it:** **no repo footprint at all — the only such row
   in the registry.** Nothing in the repo can see a chat message.
   **Sam's call, and there is no clever answer:** accept that it is held by
   discipline alone, or re-word it onto a surface that IS in the repo.

6. **`LAW-commit-before-mutation-testing`** — *commit before mutation-testing;
   revert by copying the file back, never `git checkout --` or `git stash`.*
   **Why no script can hold it:** it forbids a shell command the terminal chooses
   to run.
   **Proposed re-wording: none — this one is honestly a habit.** Its guard is
   that the loss it causes is now loud. Sam may rule that a habit with a founding
   case is allowed to stay a habit.

## THE THREE ORDERS ARRIVED MID-TURN AND ARE NOT STARTED

Newest first. **The newest supersedes the theory the last pass was built on.**

1. **THE SOURCE LABEL MUST NAME THE DOOR.** `source: "tap"` is emitted by BOTH
   the Program tab's sheet and the coach's change card, so it cannot diagnose
   which door was used — and it sent the seat's own investigation down the wrong
   path for a pass. Fix: distinct values per door, `lastTransaction`'s prefix
   minted from the same value, derived from `route` rather than passed in beside
   it. Then one widening sweep over the other label enumerations. **NOT STARTED.**
2. **THE COACH-RUN EXPORT NAMES THE DEFECT.** Two exports, revisions 6 and 7,
   the same move both directions. **One half is measured this turn** —
   docs/PLAN_ENTRY_IDENTITY_MEASURED_2026-08-10.md: a Mixed day gets ONE plan
   entry whose kind comes off a first-match-wins ladder where strength beats
   conditioning, which is why Sam's Monday is identified `:strength`.
   **The other half is NOT started:** `gatewayStatus: "repaired"` four times in
   one transaction with `rejectionCodes: []` — if the §18 repair is what re-lands
   conditioning, it is being RE-DERIVED, not moved. **Measure it, do not assume.**
3. **SAM'S REAL WORLD IS IN THE REPO.** `device-export-2026-08-10-sam-monday-wednesday.json`
   and `device-export-2026-08-10-sam-coach-wed-to-mon-KEYEVENTS.json`, at repo
   root, **untracked — this terminal did not commit them and has not read them
   in full.** His team nights are **Tuesday and Thursday**, so Monday and
   Wednesday carry no anchor and the whole destination-axis/team-night theory is
   **not his defect.**

## AND SOMETHING IS SITTING IN THE WORKING TREE THAT NEEDS SAM'S WORD

`_to_delete/CORRUPT-partial-transfer-do-not-read.json` — 159KB, untracked, and
its own filename tells the reader not to open it. **It has not been read and has
not been committed.** If it is a failed transfer of his export, the useful part
may still be in it and that is his call, not the terminal's.

## NOT COVERED

**No device evidence for anything in this document.** The clean-reset notice has
never been rendered. The copy in it is **PROPOSED, UNSIGNED**, and the sheet says
so where the gate reads it. **42 laws still have nothing holding them.**
