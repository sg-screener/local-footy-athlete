# RESOLVED-AUTHORITY RULING — 2026-08-06

Rules the second condition-1 stop (df4e1187). Extends
docs/REDUCTION_PROVENANCE_RULING_2026-08-06.md — same law, completed.
Ruled by the review seat per RULE-DON'T-ASK; Sam holds a veto.

## The completed law — authority must RESOLVE

1. The third provenance case is closed: provenance is only valid while
   it RESOLVES. A fact-derived reduction exists only while its named
   fact resolves against CURRENT facts; a fact key that resolves to
   nothing means the reduction does not exist — and the stamp site
   that produced it is the defect. powerReductionReason's fall-through
   (section18AcceptedWeekGateway.ts:325 returning game_load_protection
   on a week with no game) RETIRES: the owner returns a resolvable
   reason or none, never a default. Systemic gate: any authority stamp
   that cannot name a resolving fact or decision is a build error —
   the same gate as the provenance rule, now two-sided (present AND
   resolving).
2. REPAIRS carry the same law. A tier-4 repair is authorised by the
   ledger decision that triggered it (a deletion's relocation) or by a
   resolving fact — and it carries that authority. Bounded tier 4
   performs exactly the authorised set: decision-authorised repairs
   run (the deletion suite's mutation witnesses must detect their
   mutations again — 3/3, not 0/3), unauthorised repairs do not (the
   no-game power trim dies, cell 1 greens). "It can't tell them
   apart" is answered the same way as the fourth door: it must be
   able to, structurally.

## Also ruled

- Condition 4 is AMENDED: scaffolds are preserved on a scratch branch
  (or stash) until the real build lands — the c45792e9 loss cost a
  full rebuild and is not paid twice.
- Fixture-identity-5's true cause (decision loss, scaffold defect) and
  leg (iii)'s two install sites are adopted into the build spec: the
  deriver's real contract-selection site gets the install, measured.
- The §6 collateral (selected-contract table movement, shortfall
  repair landing) stays in leg (iii)'s scope, as measured — not
  claimed solved by this ruling.

## Condition 1 stands — third pricing

Re-price everything: three legs + provenance + resolved authority,
full witness set, deletion mutation witnesses included. Green before
building; STOP and report if not. If green: build in order, then the
run home unchanged. STOP at R5 close.
