# Programming coverage — legprogram

Owner: legprogram. Sam approved the five-part root-cause proposal on 8 September 2026.
Worktree: /private/tmp/lfa-programming-coverage-20260908.
Branch: codex/programming-coverage-20260908.
Baseline: 0505e3bb, unchanged snapshot of confirmed HEAD 8d06bbde plus existing
uncommitted source. Hash receipt: /private/tmp/lfa-programming-coverage-baseline.json.
Original UI checkout, branch, server, simulator and live data are out of scope.

Options compared: extend existing composition, whole-week placement, injury and
dose owners; or replace the scheduler and introduce individual return protocols.
Sam approved the first. No automatic merge, deployment or phone installation.
Small shared context/workload interfaces were flagged in the approved report;
any required screen wiring will be named before editing it.

Baseline law gate: 311 registry rows, 290 guarded, 21 UNENFORCED. The sole failing
assertion is NO LAW IS UNENFORCED. Receipt: /private/tmp/legprogram-law-baseline.log.
The baseline commit initially requested disabled hooks; automatic approval review
rejected that. Retried with checks enabled and succeeded. No bypass was used.

Implementation uses the existing canonical generation, injury and fixture owners.
Shared leg coverage credits normal Nordic/curl and calf strength, respects dated
health and exclusions, and never caps extra existing work. Whole-day ranking counts
working sets, power, club/games and the existing shared hard-conditioning categories.
Derived prehab and replacement paths receive dated accepted injury context.
Demanding prehab is counted and reduced without changing lifting weights.
Running return replaces existing field rows and uses repeatable jump rep ceilings.
A combined-goal repetition budget defect exposed by the new calf rows was fixed in
its existing owner: added sets share the muscle goal's repetition allowance.

Flagged shared changes before implementation: optional dated constraints on athlete
context, lower-workload power sets, and a derived workout return-stage marker.
Screen changes on this branch only pass that context in HomeScreenV2 and
DayWorkoutScreenV2; no layout changes here.

Sam subsequently requested the Load history padding fix. That was made separately
from the newest UI snapshot in /private/tmp/lfa-ui-spacing-20260908,
codex/load-history-spacing-20260908, fix commit 8059c65f. The 44-point arrow row
caused the extra heading space. The fix offsets its extra height and uses the
existing 16-point inset for cards/session rows. Compilation has zero errors.
Source read, not native pixel measurement. The UI checkout and preview stay untouched.
Only that fix commit is intended for combination; its parent 2901f4cf is an unchanged
snapshot of somebody else's ongoing UI work, not part of the authored correction.

Verification recorded: eight independent 52-week compiler journeys with offline
isolated stores, including full reruns of the three home profiles after the
equipment-eligibility correction. All 416 athlete-weeks were reached; 407 pass
every weekly check, and nine fail the existing strength-frequency check.
Earlier exploratory annual runs were stopped once their code became obsolete.
The final three-day 52-week audit reached 364 athlete-days and 52/52 identical
reopens: output/legprogram-three-day-final. It retains the baseline selection
findings listed below, with no exact exercise repeats or required plane gaps.

Final review found two checker/coverage gaps. The five-day home profile's
deliberate whole-session deletion was being counted as an accidental calf gap.
The shared coverage rule now uses canonical dated removal ownership; the actual
three-week removal/reopen/advance/Undo recheck passes all reached weekly checks
(output/legprogram-year-removal-recheck). A separate probe found that an
experience-blocked Nordic was being treated as available equipment, suppressing
the home-equipment hamstring fallback. Eligible choices now define availability;
the experienced home athlete must still receive a Nordic/curl. The three home
profiles each completed all 52 weeks after that correction, with no failed weekly checks.

Current focused results: 27/27 domain cases, 135 real-journey assertions and 13
distinct injected faults caught. Compilation has zero errors in all three scopes.
The compiler-year detector passes 45/45 infrastructure controls. Detailed results
are in [the verification report](/private/tmp/lfa-programming-coverage-20260908/docs/BOUNDARY_LEGPROGRAM_2026-09-08.md).

Checkpoint 06722458 contains the main programming corrections. Final review then
found the remaining saved-add-on prehab path: earlier add-ons and the optional flow
were not sharing area credit. A small internal work-row view now includes those
saved doses; the lower-body counter also includes recovery-section calf strength
and excludes injury-blocked add-ons. The new cases reach a real drawn flow and
check duplicate saved add-ons, deload, injury filtering and a JSON round trip.
The full programming, mobility, Saturday Speed and whole-week suites pass after
that follow-up, saved as c2236f64; its three-day 52-week journey also completed.
The eight-profile
audit remains evidence for the preceding programming checkpoint, not a claim
that every profile was rerun after this final follow-up.

The annual repair suite now passes all 45 cases and catches all 25 injected faults:
/private/tmp/legprogram-yearscan-acceptance.log. The initial goal-budget failure
and its corrective run remain available in the earlier receipts.

Confirmed starting-code findings: 21 UNENFORCED registry rows; the ProgressTabScreen
ownership fingerprint is stale; a zero-equipment profile save is refused with
main_strength_required_minimum:2; two legacy zero-equipment assertions fail;
the three-day annual selector checker finds two repeated main-lift families
(four exercise occurrences) after midweek injuries, with zero exact-identity
repeats and zero required movement-plane gaps. Its 18 trunk-plane findings are
soft checks, plus one accepted plane exception. The same counts occur on the
unchanged baseline. Old fixed-day/access and all-aerobic-session-preservation
assertions were updated against existing R-391 and R-382, preserving their
actual behaviour checks instead of accepting a red test as a code defect.

The nine annual failures are five lifting days against a maximum of four during
knee-injury weeks in the two six-session profiles. The same failure reproduces
on the unchanged baseline for both Pre-season profiles and through the actual
phase-change door for the no-standing-fixture In-season profile. All 416 weekly
calf/hamstring checks and all 416 weekly reopen checks pass. The annual gate stays
red for the nine frequency findings and the existing ownership prerequisite.
The final ownership scan has one unresolved owner out of 1,191. The repository
process guard has 12 failing checks out of 63 on both this branch and the baseline;
no new failing check was introduced. Raw results are retained under output/.

NOT COVERED: physical phone, native pixels, live/remote persistence, real OS process
death, combined UI/programming version, deployment or merge; the final prehab
follow-up was not followed by another complete eight-profile annual run.

## Verification follow-up approved by Sam

Sam: “okay let’s get verification green then... whats going wrong?”

Options compared: enforce component ownership in the existing injury merge and reserve kit-achievable main seats before selecting exercises; or rebuild and move all accepted strength during injury / lower the target to whatever survived. The existing-owner correction preserves accepted training and keeps the judge able to reject missing work. No UI source or live data was changed.

Measured defect: the injury source-fact fold kept accepted lifting days but imported the entire newly planned mixed session onto an empty date. Its shared energy merge now transfers only running/conditioning rows and removes obsolete energy rows from occupied receivers. Three real six-session journeys (both Pre-season profiles, no-standing-fixture In-season) preserve four lifting days through injury, reopen and Clear.

Measured kit defect: Off-season Lower/Upper/Full Body and Pre-season Full Body x3 reserved unavailable vertical lifts for the last day. The weekly budget now transfers a kit-legal main from a day with more than one legal main, before composition; supporting rows yield. All three phase journeys now save a permanent zero-kit answer, reopen identically, restore equipment and reopen identically. Their selected strength targets remain enforced.

Audit corrections: retain the row’s addedForInjury marker and the actual adjustment’s paused/added lists; the shared family checker recognises only one evidenced injury compound per day under R-386. Ordinary duplicates and unproved markers still fail. Annual coverage declares its expected athlete scope before the run and checks unique weeks/dates, restart evidence, failed actions and recorded failures. A one-athlete preset no longer pretends to be an incomplete two-athlete run; a partial year remains red.

The zero-equipment diagnostic separately tests removing push rows and removing a whole lifting day. Its missing-slot list now includes the minimum-useful-session accessory attempt. 11/11 pass. The latest focused rule run is 31/31; compilation is zero in all three scopes. Broader release and fresh annual runs are pending. Historical process/law diagnostic failures remain visible, unchanged; the current release contract registry already classifies those suites as needing rewrite rather than product witnesses.

NOT COVERED: combined UI/programming version, physical phone, live/remote persistence and full annual reruns at this in-progress checkpoint.

## Deload ownership follow-up

The fresh eight-profile audit at 202535f5 reached 416/416 weeks with zero failed checks, 416 exact reopens, 416 calf/hamstring checks and six successful annual fault controls. A date/phase/check-id comparison confirms all nine previous main-strength maximum failures pass. The separate three-day year is also green, with its 18 soft trunk advisories retained.

The subsequent canonical compiler guard found a real second dose resolver in weeklyLegCoverage. A new regression then showed the consequence: late calf/hamstring rows could retain two sets during a deload instead of one. Coverage now receives the compiler's existing dated policy in trial composition, final composition, injury completion and fixture repair. The real journey observer caught the initially missing fixture argument before verification was restarted. Existing lifting receives no second reduction. This is an internal callback input; no UI or saved-format change.

Focused proof: 32 rule/checker cases, 137 explicit journey checks with production argument observation, and 22 distinct injected faults caught. The release ordering checks pass 18/18, including missing/delayed prerequisites and duplicate-witness controls. The release runner keeps the same 47 contracts / 44 command groups, but ownership, focused programming and the canonical compiler precede the expensive annual replay. No witness or failure is removed.

The failed release run was stopped only in its verified isolated process tree; the UI server and simulator were not touched. The annual PASS above belongs to the preceding code checkpoint. Fresh final-code release and annual runs are required after this correction; pending at this entry. Raw logs remain under /private/tmp/legprogram-dose-ownership-* and the old annual comparison is output/legprogram-annual-failure-comparison.json.

NOT COVERED: fresh final-code release/year runs at this checkpoint; combined UI/programming, native device, live/remote persistence, merge and deployment.

## Coach bundle freshness follow-up

The independent release-tail preflight found one failing coach integration check: the generated server knowledge bundle still contained the earlier Bible and ruling registry. The existing generator rebuilt all six sources; only those two bundled source entries changed. The same unmodified integration gate now passes 73/73, including stale/missing/duplicate-source and authority fault controls. No server deployment, provider call or UI change. The three-day annual audit at 78869c85 has independently completed green: 52 distinct weeks, 364 days, 52 exact reopens, zero failed actions/checks, 18 soft trunk findings and one accepted injury plane exception. The full release run continues; its annual replay has not started at this entry. The bundle refresh does not change training generation.

NOT COVERED: complete release/fresh eight-profile year at this checkpoint; native/device, combined UI, provider/live persistence, merge and deployment.

## Full-prescription replay follow-up

The remaining annual gates found actual differences that the smaller signature did not expose. Controlled two-day week 43 had Single-Leg Hop and Stick at five morning reps versus three after reopening. The fixture compiler had reduced it correctly; carryOwnAcceptedLoadsIntoWorkout then copied the earlier automatic power dose back over it. The same broad copy restored two Cossack sets over one in minimal-kit week 22. The shared carry now preserves athlete-managed loads and ordinary strength dose, while field/power and demanding-prehab prescriptions retain the compiler's dose. No UI, persistence format or new policy resolver.

The next controlled-year comparison found a later fatigue report appending a calf row to an earlier day. Source-fact completion previously supplied the history boundary only for injury reports; it now uses all dated deriving facts. Every injury/coverage/dose fold preserves dates before that existing boundary. The earlier day remains available for weekly credit; future work still receives health restrictions and duplicate-dose reductions.

Proof so far: 34/34 focused cases; 137/137 explicit real-journey checks; 25/25 distinct injected programming faults caught; 10/10 established accepted-load preservation cases; zero compile errors in all three scopes; 73/73 coach checks after regeneration. Controlled two- and three-day years and the minimal-equipment year now pass full prescription/reopen comparison. The remaining annual profiles and final current release are pending at this entry. The two failed release/preflight attempts remain failed receipts; no assertion was weakened. Raw evidence and logs are /private/tmp/legprogram-jump-* and /private/tmp/legprogram-history-*.

NOT COVERED: complete final release/eight-profile and three-day annual reruns at the final code checkpoint; remaining annual profiles at this entry; native/device, combined UI, live/provider persistence, merge and deployment.

## Annual evidence serialization follow-up

All four controlled schedules' existing programming checks pass: each reaches 364 dates, 20 prior-history comparisons and 52 exact reopens. Minimal-kit and the 52-week beginner journey also pass. The five-day driver nonetheless exited 1 after reaching week 52: JSON.stringify of the complete accumulated selection-trace report exceeded V8's string limit. This was a failed evidence export, not an accepted green run. Its completed year-programs/raw data was separately checked without changing it; the raw failure remains recorded.

The shared lived-year save function now uses write-json-evidence.cjs to write the complete JSON tree in bounded chunks, replacing the destination only after a successful write. No evidence is truncated or filtered and the JSON data format is unchanged. The test-truth chain now includes a 1,200-batch exact round-trip, a guard forbidding container-wide stringify, failed-write preservation and a real dropped-array-tail mutation. A fresh five-day replay with the new writer is running at this entry. Whole-week planning is 10/10 with 7/7 faults, Saturday speed has 8/8 faults, and the full-body group also exited 0. The final release restart remains pending.

NOT COVERED: completed fresh streamed five-day export and final release/annual runs at this entry; native/device, combined UI, live/provider persistence, merge and deployment.

## Return-display and test-shape follow-up — 9 September

The caf275c2 current release was stopped after 14 failing assertions in the canonical journey group; the release reported 4/44 groups green and exit 1. Eight assertions assumed Saturday was empty, although the actual four-day profile now puts speed there and rests Sunday. The existing low-load removal journey now locates a real future rest day, asserts it is empty, and retains all eight male/female Mobility/Recovery combination journeys through Add, Undo, re-add, reopen, Remove and Undo.

The other six assertions exposed two linked return issues. The day projection looked up the original maximal sprint template and displayed eight reps/start every 60 seconds, while the session held four controlled run-throughs/full recovery. Conditioning return titles also replaced the stable authored identity, hiding their run-only modality rule. runningReturnDose.ts now owns the existing authored return wording for both the compiler and the signed day projection; speed projection receives the same workout context as conditioning. Counts come from the actual row. Return options retain the authored 20 m template identity. The pure modality rewrite uses the existing templateSupportsSelectedModalities rule and refuses an incompatible rewrite; it does not invent machine run-throughs. Source-modality targeting is preserved. The clarity journey explicitly checks refusal for run-only work and still exercises legal machine swaps.

Red-first receipts: /private/tmp/legprogram-return-display-red.log reproduces both the wrong projected dose and invalid rewrite. Focused cases now pass 36/36, including both stages × speed/conditioning and copy provenance. The new injected faults remove return projection, discard speed context, lose template identity and allow an invalid machine rewrite. Full fault/journey reruns are in progress. There is no new saved field or screen-layout change. The Bible, R-393, current-contract path list and coach bundle were updated together; the existing law/guard remains the owner.

Additional historical diagnostics, measured against the preceding source: projection-ownership is 13 pass/1 fail because its game-day fixture expects canAdd=false; copy-rulings-binding is 7 pass/2 fail for missing signed/proposed copy. Both failures match the prior caf275c2 code; no fixture policy or UI wording was changed to silence them. Signed-copy extraction passes 7/7. These are additional named diagnostics, not current release witnesses and not claimed green. Baseline receipts: /private/tmp/legprogram-projection-ownership-before.log and /private/tmp/legprogram-copy-binding-before.log.

NOT COVERED: complete release/eight-profile/three-day annual reruns after the display/identity correction; remaining full canonical journeys at this entry; native/device, combined UI, live/provider persistence, merge and deployment.

Return follow-up focused verification: 37/37 named cases pass, including a permitted bike-to-row change that preserves a separate run-only component. The corrected original low-load/clarity journeys pass 2,764 assertions with zero failures. The full conditioning-modality persistence group exits 0 (its intensity-route portion has 2,700 assertions). Compilation is zero errors in all three scopes after correcting the filter parameter type; ownership is zero unresolved out of 1,191 inspected owners. Test-truth and coach integration exit 0. The complete programming-input preflight and expanded 30-fault run continue; full release/annual restart is pending. NOT COVERED remains as above.
