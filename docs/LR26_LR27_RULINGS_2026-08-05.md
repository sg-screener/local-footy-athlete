# LR-26 / LR-27 rulings — Sam, 2026-08-05

Answers to the two PARKED FOR SAM questions in
`docs/STAGE_B_STAGE2_CHECKPOINT_2026-08-05.md` §4. Recorded by the review
seat the same session Sam ruled; committed by the terminal as authored.

These rulings **partially supersede** kickoff ruling 3 (2026-08-06 batch,
"LR-27 gets LR-26's ruling, twins ship together"): the builder's receipts
corrected the premise (the after-side/before-side split was unknown when
ruling 3 was made, and the LR-27 doubling turned out to be a filler
self-copy, not a restoration). Ruling 3's *method* stands — consumer check
first, delete stored output at the root — and produced these corrections.

---

## Ruling 1 (checkpoint §4a) — LR-27's residue: option (a)

**The behaviour is real product behaviour.** An expired G+1 recovery that
displaced a genuinely accepted session must give the athlete back what it
displaced. The athlete's acceptance is a decision; restoring it is the
north star's own sentence.

- The `restoration.workout` snapshot **stays for now** — deleting on the
  current evidence (one hand-built fixture, no acted path) would be
  guessing.
- **Queued unit (file in the census):** replace the stored copy with
  reference + derive-at-read, the same shape LR-27's filler fix used. The
  snapshot deletes only when that lands.
- The hand-built fixture is modelling something true, but it remains the
  mirror image of the 2026-08-04 class: the matrix/walker reaching the
  displace-an-accepted-session composition by ACTING is that unit's
  entry gate (L13/L11 — no device or deletion before an acted world
  reaches it).

## Ruling 2 (checkpoint §4b) — LR-26: option (a)

**Delete the after-side snapshots** (`afterWorkout`,
`afterSurfaceWorkout`, `afterDateOverride`, `afterOverrideContext` as
stored content) — measured never-read; the identity
(`planEntryId ?? id`) and `afterFingerprint` that already sit beside them
are the surviving record. Real payload cut, zero behaviour change,
whole-bible green required as usual.

**The before-side stays and is re-classified:** it is the undo
transaction's working data — read in full at
`reversibleAdjustmentTransaction.ts:511-553` — not dead stored output.
For now it is ruled *the decision's own content*.

- **Queued unit (file in the census, not silently kept):** rebuild undo as
  replay-from-decisions, at which point the before-side becomes derivable
  and deletes. This is its own future unit with its own sizing — NOT part
  of Stage 2.

---

## What this unblocks

- LR-26 implementation (after-side deletion) may proceed when the terminal
  returns from Priority C — it no longer waits on anything.
- LR-27 closes as delivered (`92484f3`) plus ruling 1's queued unit; the
  census entry should reflect both queued units so the ledger stays
  honest.
