# PATTERN IDENTITY RULING — 2026-08-06

Rules the third condition-1 stop (report c8142770): the stored §18
contract is an INPUT to pattern selection, so retiring it changes
which patterns land on which days. Who owns pattern identity?
Ruled by the review seat per RULE-DON'T-ASK; Sam holds a veto.

## Ruling — pattern identity DERIVES; the derived contract owns it

The §18 contract is derived output of persisted inputs (mode, facts,
covering microcycle, authored policy) — leg (iii) exists because of
exactly that. Pattern selection therefore reads the DERIVED contract,
and the stored contract retires as an input. Grounds:

1. North star: the stored contract stores no decision — every input
   that produces it is already persisted. It is the last stored
   output feeding composition.
2. R5.1's law: a landed decision settles by re-deriving. The athlete's
   decision was to ACCEPT; the layout was the planner's output, and
   this architecture re-derives outputs from decisions by design.
3. Determinism holds: the derivation is a pure function of persisted
   inputs (proven repeatedly today), so relaunch identity (L16) is
   preserved — same inputs, same week, every read.
4. The one-time visible shift this causes on EXISTING worlds (a
   stored-vintage contract disagreeing with today's derivation) has a
   bounded blast radius of zero athletes: the app is not live; the
   only affected devices are Sam's test devices. The transition cost
   is paid now precisely because it never gets cheaper.

## Cells 5/6 — re-ruled, not edited

The pins captured the deriver's answer under the stored-contract
input. Under this ruling that input retires, so the pins are
superseded BY THIS DATED DOC — the legitimate form. Procedure:

1. First prove the derived week LAWFUL where the pin's world lives:
   patterns legal per the Bible's calendar rules, balance held, §18
   green with zero unresolved shortfall, and byte-determinism across
   two consecutive derivations and a relaunch.
2. Then re-pin the cells to the derived week's literal values (value
   pins, never agreement — the cell-5 law stands). The commit cites
   this doc.

## Condition 1, final form

Price the full set once more with the stored-contract input retired.
The witness set is: fixture-identity 6/6 against the re-ruled pins,
deletion 24/24·5/5·3/3 (mutation witnesses 3/3), accepted-state full,
phase-structure 11/11 with cell 8, L16 20/20 both walkers,
derived-repair-ownership able to red. Green → build in order → the
run home (unpiped bible, condition 4, merge, post-merge bible, R5.7,
remaining batches). Not green → STOP with the residual named.
