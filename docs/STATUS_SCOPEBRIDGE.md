# STATUS — SCOPEBRIDGE

Owner: `scopebridge`. 2026-09-06. Sam authorised the three follow-up tasks from
`CODEX_APP_MAP_2026-09-06.md`, in order. Starting HEAD: `8d06bbde` on
`integrate/2026-09-04-morning`. Existing changes, especially `docs/NOW.md`, are
other work and remain untouched. No commits or phone builds authorised by the
earlier inspection brief; implementation is now authorised.

## Approach

1. Trace the dated equipment change and missing bilateral coverage through
   selection, accepted facts, reconstruction and visible weeks. Repair the
   shared owner and update every dependent assertion/fixture.
2. Carry truthful empty-day causes through the effective week, actual screen
   input and reopening. Preserve completed history and approved copy.
3. Refresh the existing Coach bundle and connect existing executable witnesses
   to release checks. Remove the year tool's dependency on an untracked driver.

Compared local patches at each reader with correcting shared ownership. Prefer
the latter: equipment scope belongs to fact/accepted reconstruction; exercise
classification belongs to the catalogue; empty-day reasons belong to the same
effective day as the workout. A second UI-specific interpretation would retain
the class of mismatch the audit measured.

## Starting evidence

The audit report records the exact commands: settings journey 175 assertions
passed / 8 failed; canonical slice 10615 passed / 5 failed; Coach integration
63 passed / 1 failed. These are assertion counts, not app-wide verdicts.
The limited-kit bilateral subject has four distinct male/female × missing
rack/barbell cases. The settings date-scope subject changes 31 August after an
answer for 10 August. Their root causes are under investigation.

## NOT COVERED

- Implementation and new verification are in progress.
- No physical iPhone acceptance, production data or live/paid Coach calls.
- No screenshots or recordings, following Sam's existing restriction.

## Implementation findings and receipts (in progress)

### Equipment scope and session coverage

The later-week change was a replay-input error. An exclusion accepted before a
block was created was withheld along with later, dated exclusions. Replay then
reselected an older tracked lift; the final exclusion filter removed it. The
canonical input owner now retains still-active pre-block exclusions and keeps
in-block exclusions dated. No new selection writer or exercise rule was added.
The settings journey now checks every unaffected week and every reconstructed
week, plus the accepted selection records. `test:settings-persistence`: **181
assertions passed / 0 failed** (`/private/tmp/lfa-scope-settings-final.log`).

A completed accessory-only strength session has a recorded strength component
but no tracked-main-lift log. `readBlockHistory` now counts that recorded
component, retaining the old-log fallback only for legacy records without
components. Load evidence still comes from the exact lift's own log. The load
journey distinguishes the chosen lift's recorded weight from both the outgoing
lift and its starting estimate, then crosses settings and reopening. Removed
the obsolete auto-return-to-Leg-Press expectation, which R-374 does not promise.

The four limited-kit cases were an obsolete bilateral-only expectation after
R-373's graded unilateral main lifts. They now assert one actual squat main
seat and one slot of credit; the existing leg-press and unilateral checks stay.
No catalogue rating or training prescription was changed to satisfy the test.

### Empty-day path

Typed reasons now cross the generated-week overlay and its dated merge. The
final shared week compiler attaches them to effective empty days. Removed the
Program-only attachment, so the Program and opened-date readers share one
answer. Injury recompilation respects a planner day deliberately emptied by
injury, while retaining athlete placements, safe remaining work and completed
history. Travel labels game/team-only days it actually removes.

The existing rest-day suite executes real onboarding, real injury and travel
answers, the actual `HomeRestDayReason` Text component (native Text host stub),
Program/date readers and real store rehydration/boot. It checks ordinary and
filled days, sparse overlay ownership and forward history. A mutation that
drops the overlay carrier makes that journey fail. **55 assertion invocations
passed / 0 failed** (`/private/tmp/lfa-scope-rest-final.log`). This is not a full
native screen mount or visual verification.

First travel reopen check failed on **8 timestamp field occurrences across 2
power rows**, and on no training instructions. Reused the existing annual
semantic comparator rather than adding another normaliser; it ignores only
creation/update timestamps. The initial mismatch is not evidence of a changed
travel program. `test:injury-authority`: **26/0**; `test:injury-recomposition`:
**186/0**. G7 now also bounds future main sessions instead of allowing arbitrary
excess above its no-shortfall assertion. The old I6 quarantine label and obsolete
injury-never-replans commentary were removed.

### Coach and evidence tools

The six bundled Coach reference sources were regenerated. Exact ordered
manifest/source parity, each source changed in turn, missing/extra membership
and wrong authority are checked offline. **73/0** in Coach integration. No live
provider call or deployment; the remote bundle remains unverified.

Settings, rest reasons and Coach checks are now current release witnesses.
The first release attempt rejected my server-path registration because the
existing registry accepts only `src/` paths; corrected it to the actual client
and manifest owners. Bootstrap then caught stale census assertions; updated
both the changed totals and explicit witness membership, preserving their
independent registration checks. The full release run is in progress.

The existing selection year entry now contains its driver. All three dependent
audit variants read that same tracked file instead of an untracked output file.
Their variant receipts are preserved. Existing `yearDiffTests` now run all four
consumers through a real onboarding/logging/reopening week with reads of old
output directories forbidden, plus a negative control. **14/0**. Full selection
year: **104/104 athlete-weeks across 2 distinct athletes**, with **104 successful
weekly reopen comparisons**. No approved baseline changed. Exercise distribution
deltas are under causal investigation; session and total row counts did not
change. Full eight-athlete compiler year is running separately in `/private/tmp`.

Nine changed production owners were individually reviewed in the existing writer
registry; its scan reports **0 unresolved owners**, with no new writer category.
All-scope typecheck passed with **0 errors** before the final tooling additions;
final verification will be recorded below.

## Remaining before this pass can close

- Await Sam's copy decision: the existing injury sentence names a shoulder and
  the game sentence names Saturday/Monday. Neither is valid for every input.
  Proposed generic replacements were asked through the pending text questions.
- A cooked/fatigue-only rest day still lacks its own accurate signed sentence.
  It must not be labelled a deload week. Proposed copy awaits Sam. Illness and
  deload normally retain reduced sessions; no new empty-day policy is invented.
- Full release, full compiler-year results and causal year-diff review pending.
- Final native/physical acceptance remains unperformed; no phone build,
  screenshots, recordings, real athlete data, commits or production deployment.

## Further verification

The two additional scope checks were run: `test:equipment-scopes` **20/0**.
`test:exercise-exclusions` initially **58/2**. Its two failed expectations were
not current product receipts: it expected a retired bare-name migration, and it
assumed its chosen fixed accessory (ATG Split Squat) must be a recorded rotation
seat. Its generator fixture also never explicitly recorded the initial block.
Updated both fixture generation paths to explicit author/replay intent. The
replay assertion now proves a nonempty accepted selection history remains
identical and no new exercise identity appears, instead of demanding a history
entry for a fixed accessory. Removed obsolete stored-row hiding commentary and
replaced the bare-name upgrade expectation with the current scoped-ingress
contract. Final **61/0**, `/private/tmp/lfa-scope-exclusion-scopes-final2.log`.
This remains a lower-level generation fixture; the promoted settings journey
supplies the real onboarding/action/reopening coverage.

The real two-gym-day/two-club-night travel case found a missing cause: Thursday
was empty before final display constraints, whereas Tuesday had acquired solo
mobility. The existing availability owner now marks the exact club/game anchors
it removes for travel. The scheduler carries those typed reasons, and the final
reader removes them from filled days. No travel/scheduling policy changed.
A second negative control removes that availability cause and the real team
journey fails. Rest-day suite now **72/0**, covering two travel profiles, injury,
Clear, readers, the actual text component and reopening; log
`/private/tmp/lfa-scope-rest-final-complete.log`. The vocabulary/copy limitations
above still apply. Ten capability owners were individually reviewed; the
availability adapter's change was also inspected (the census does not identify
it as a direct writer capability).

The isolated eight-athlete annual run completed: **416/416 athlete-weeks across
8 distinct athletes**, **416 successful weekly restart comparisons**, **6/6
negative-control checks**, no distinct failure keys. Report:
`/private/tmp/lfa-scope-compiler-year/index.html`. That run loaded before the last
travel-cause metadata addition; the final rest/ownership/canonical checks cover
that addition. The full release run is independently running its annual unit.

The two-athlete summary differs from the approved baseline in 7 exercise/role
counts and 7 exercise/dose counts per athlete, with no changed total rows or day
counts. Causal control: replacing only `readBlockHistory` with HEAD's old code
in an isolated male-year process restores the baseline exactly. The changed
rotation follows the corrected completed-component evidence, not the relocated
driver. No baseline edited. `test:year-diff` remains an instrument, not a rule
that decides which distribution is good. Its four real-driver smoke checks now
run in the existing release bootstrap; final bootstrap **passed**, including
**14/0** year-diff assertions (`/private/tmp/lfa-scope-bootstrap-final.log`).

## NOT COVERED (current)

- New/generic wording still needs Sam's response to the pending questions.
  Fatigue-only rest has no accurate signed reason yet; do not call all-cause
  explanations complete.
- The full native screen, keyboard, actual OS process death and physical iPhone
  have not been exercised. The Text component uses a native-host stub.
- No remote Coach deployment or live answer verification, real account data,
  screenshots, recordings, baseline edits or commits.
- The annual cases are samples, not every equipment/injury/travel/action
  permutation. The 4 driver smoke runs cover one week each; only the shared
  two-athlete driver and separate eight-athlete runner covered full years.

The settings witness now additionally mutates the recorded completion of a real
accessory-only day from full to partial, preserving its overall-day marker. In
both reached club/gym arrangements the completed-session credit drops by exactly
one while recorded-session count stays fixed. A separate assertion requires an
accessory-only day to have actually been reached. Final settings result:
**184/0**, `/private/tmp/lfa-scope-settings-component-final.log`.
Final all-scope compile check after the additional tests: **0 errors** in product,
devtools and tests (`/private/tmp/lfa-scope-compile-last.log`).

HEAD remains `8d06bbde17390aa6532b9eabb3cfbb68294e1dc5`. The pre-existing
`docs/NOW.md` diff still hashes to
`a1e5422e6d8b046d8aa6595dea173f19cd3f7971e657a6665af2a30c780ecae9`, exactly as at
entry. No other agent's source changes were deliberately changed. The full
release run has passed its separate 416-week annual unit and is now in the
canonical combined-action suite; final outcome follows below.

## Release boundary found and repaired

The first full release command stopped after **14/34 release units**. Its
canonical slice itself passed **10,624/0**, but the chained fixture transaction
suite failed its R-344 precondition: only **2 distinct matched loaded lifts**
survived in the week after a Sunday fixture, below a hardcoded minimum of 3.
That count was not a load mismatch. Corrected completion/rotation had changed
which identities matched; the actual surviving lifts kept their loads and dose.

Replaced the arbitrary minimum with the actual subject: at least one surviving
same-role lift, all matching lifts checked, and at least one earned accepted
load different from its starting estimate. Added a negative control that runs
the **same real worn-athlete fixture journey** with both accepted-load carry
entry points disabled; it must reach those entries and fail on a concrete
visible-versus-accepted load/dose difference. It does. Final fixture transaction
suite **25/0** (`/private/tmp/lfa-scope-fixture-final.log`). No load policy was
changed. Final typecheck after this test change is again **0 errors**
(`/private/tmp/lfa-scope-compile-fixture.log`).

Release continuation uses the existing `deriveReleaseGate` and `runUnits`, from
the stopped canonical unit onward, with the list still derived from the current
registry. It is rerunning that complete chained unit and all 19 following units;
no successful unchanged annual unit is repeated yet again. Receipt:
`/private/tmp/lfa-scope-release-resume.log`. The initial command's nonzero exit is
preserved, rather than relabelling it as a successful single invocation.

The canonical chain then found one more stale check in injury session
adjustment: **53/54**. The original session had 6 rows. Five were listed in
`paused`; RDLs was explicitly listed in `withdrawn` by the weekly planner.
Nothing was silently missing from the review. The assertion demanded every
original row appear in the ladder's `paused` list, ignoring the existing
planner-withdrawal category. The screen already maps `review.withdrawn` into
its `injury-review-withdrawn` rows (read at source, not native-render measured).

The revised witness independently checks safe rows and accounts for every
original row through retained work, pause, withdrawal, an actual landed
substitution, or an actual conditioning change. Hiding the reached withdrawal
makes the check fail. Final **56/56**, no new training-policy change, log
`/private/tmp/lfa-scope-injury-adjustment-final.log`; typecheck remains 0 errors.
The complete earlier canonical passes are retained. Its remaining preview and
conditioning checks plus the 19 following release units continue in
`/private/tmp/lfa-scope-release-finish.log`, using the current package/registry
command lists. This avoids repeatedly paying for unchanged passing annual and
10,624-cell runs when only a downstream test changed.

### Serious symptoms: full-gate finding and repair

The continuing release run reached `injuryFallbackJourneyTests` with **177
assertions passing / 5 failing**. The final R-377 pattern filter removed two
main rows from a serious-symptom day whose R-115 contract keeps the original
session visible but withheld. A separate process loaded the original HEAD rule,
service and reader implementations; it produced the same **177/5** result
(`/private/tmp/lfa-scope-injury-fallback-head.log`). This establishes the original
failure; the earlier progress message attributing it to this change was wrong
and was explicitly corrected to Sam.

`compileCanonicalInjuryWeek` now uses the existing serious-symptom predicate
before that removal filter. The injury source-fact merge uses the same predicate
to retain accepted rows when a second ordinary injury would otherwise empty the
day. No severity threshold or training prescription was invented. The real
fallback journey now adds the second injury, reopens, clears it, then checks the
original restore. It retains the existing exact row/load, withholding and
recording-refusal assertions. Coverage now counts main squat/hinge seats
separately from finer movement families, because R-373 allows a unilateral squat
main seat. Final **185/0**:
`/private/tmp/lfa-scope-injury-fallback-complete.log`.

Subsequent checks: injury recomposition **186/0**, injury authority **26/0**,
rest reasons **72/0**, in the respective `lfa-scope-*-last.log` files under
`/private/tmp`. Three missing result types in the added test were caught by the
all-scope compiler; they use the production `ProgramControlActionResult` now.
Final compiler: **0 errors in each of product, devtools and tests**, log
`/private/tmp/lfa-scope-compile-final.log`.

The next release stop was the legality delegation liveness guard. WC-064 named
the module `rules/weeklyScheduler`, which the guard treats as a function name.
It now names its actual called owner, `scheduleWeek`; an explicit WC-064 cell
retains that link, alongside the existing dead/live function mutation controls.
No scheduling behavior changed. Last four release units **4/4 green**, including
injury matrix, rest reasons and Coach:
`/private/tmp/lfa-scope-release-last-four.log`. Earlier remaining run passed
**11/15 units** before this stop; its red receipt remains in
`/private/tmp/lfa-scope-release-final-remainder.log`.

The final writer inventory reports **0 unresolved / 1,170 distinct capability
owners**, including callers; **2,558 direct operation occurrences in 656
distinct direct owners**. Reviewed fingerprints include the changed canonical
injury compiler. `/private/tmp/lfa-scope-writer-final.log`. The final complete
canonical chain finished in `/private/tmp/lfa-scope-canonical-last.log`: the
leading canonical slice is **10,624/0**, session adjustment **56/56**, injury
preview **194/0**, and final conditioning balance **42/0**. All intervening
commands completed successfully before the next command ran.

### Final local verification scope

All **34 distinct release units** have successful receipts across the original
run and the repaired-unit reruns; this is NOT a claim that one uninterrupted
`npm run test:release` invocation returned green. Stopped-run failures remain
in their logs. The complete canonical command was rerun after the serious-
symptom repair. The initial annual compiler result is **416/416 athlete-weeks
across 8 distinct synthetic athletes**, with **6/6 mutation controls**; it
predates that final repair and was not repeated. The final injury journeys,
canonical chain and remaining release units cover the repair instead.

Final targeted counts include settings **184/0**, rest reasons **72/0**, and
Coach integration **73/0**, all assertion counts within their named suites.
The Coach bundle matches all **6/6 distinct manifest sources**. The next defect
in these classes is checked by the real dated/reopened settings journey, the
shared-reason journey with removed-carrier negative controls, and exact Coach
source parity with changed/missing/extra/wrong-authority controls. The year
entrypoints also have real one-week execution with old output-file reads
forbidden, rather than a source-string existence assertion.

Final `git diff --check` is clean. HEAD remains `8d06bbde`; no staging or commits.
The preserved `docs/NOW.md` diff SHA256 remains
`a1e5422e6d8b046d8aa6595dea173f19cd3f7971e657a6665af2a30c780ecae9`.

### Remaining owner decision and NOT COVERED

- Sam has not yet approved replacing the shoulder-specific and Saturday/Monday
  copy, or the proposed fatigue-rest line. Current signed copy is unchanged.
  Fatigue-specific empty-day explanation remains unfinished; no new fatigue,
  illness or deload rest policy was invented.
- The actual Home explanation Text leaf, real action doors, shared Program/date
  readers and reopening were exercised. The full native Home screen, layout,
  interaction and Sam's physical iPhone have NOT been verified.
- No Coach deployment or paid/live provider calls. Only the checked-in bundle,
  source parity and offline client checks were verified.
- No screenshots, recordings, commits, push, phone build or real athlete data.
  `docs/NOW.md` and pre-existing unrelated files remain untouched.

## Approved-copy follow-up — 2026-09-06

Sam answered **"yep approve"** to the three exact proposed lines. This closes
the copy decision above; it does not author a new fatigue or injury policy.

- Injury: **No session today while you recover from injury.**
- Game preparation: **Kept clear to help you stay fresh for your game.**
- Fatigue: **No session today. Take the day to recover.**

The two old strings were replaced in the existing signed-copy table, with this
approval recorded in each entry's provenance. Fatigue has one new signed entry
and one typed reason. The reason union now derives from its runtime list, so
there is no second list to forget. The copy map remains exhaustive and the
actual Text component is checked against all six entries plus the three exact
approved sentences. No screen-specific fatigue rule was added.

Compared attaching a reason at the existing compiler removal with inferring it
again in Home. Chose the former: the existing `policy.effect === 'rest'` branch
attaches `fatigue` when it returns a null workout. The normal sparse-overlay
merge, date reader and Home component carry it unchanged. Lighter sessions and
ordinary rest do not get a fatigue-rest explanation.

### Findings and checks

The new real male/female journeys first reported **110 assertions passing / 4
failing**, with the two explanation checks failing for each athlete. Final
journeys record Monday, report moderate fatigue and then cooked on an actual
later session, compare unaffected/completed days and both readers, reopen,
clear both reports, and reopen again. A mutation dropping the reason at the
removal step makes the actual journey fail. **143/0** in
`/private/tmp/lfa-approved-rest.log`; the final repeat after deriving the type
from the shared list is `/private/tmp/lfa-approved-rest-final.log`.

`fatiguePlumbingTests` initially had **17/1** because its source pattern only
accepted a one-line `if (...) return`. Updated the surviving check to anchor
the real rest branch and require its reason plus null return; it no longer
claims that a source-text match executes removal. Behavioral removal/disclosure
is held by the real journey above. Fatigue policy **35/0** and plumbing **18/0**:
`/private/tmp/lfa-approved-fatigue-policy-final.log`.

The old copy-binding diagnostic reported **7/2**. Re-running it with HEAD's
original `projectionCopy.ts` loaded produces the same two named failures:
missing signed stale-override-banner wording, and proposed strings from retired
or changed surfaces. Its existing test-truth row already classifies it
`rewrite_test` for those issues. No unrelated copy was silently approved or
restored. Receipts: `/private/tmp/lfa-approved-copy-binding.log` and
`/private/tmp/lfa-approved-copy-original.log`. This diagnostic is NOT green.

Current copy extraction **7/0** (`/private/tmp/lfa-approved-copy-extraction.log`);
its output was redirected to `/private/tmp/lfa-approved-copy-sheet.json` rather
than overwriting the shared artifact. Coach source/client parity **73/0**
(`/private/tmp/lfa-approved-coach.log`). Test-truth and release registration
**14/0 each**. The changed compiler owner was re-reviewed: writer gate reports
**0 unresolved / 1,170 distinct capability owners**, including callers.

The full canonical chain completed with **CANONICAL_EXIT=0** in
`/private/tmp/lfa-approved-canonical.log`: canonical slice **10,624/0**, injury
session adjustment **56/56**, injury preview **194/0**, final conditioning
balance **42/0**. All intermediate commands completed successfully. The final
rest-day suite is **143/0** and all-scope typecheck reports **0 errors in each
of product, devtools and tests** (`/private/tmp/lfa-approved-compile-final.log`).

The release registration still includes this real rest-day witness. These are
the full relevant command chains for this follow-up; the entire release fleet
and annual simulations were not rerun for the approved copy/carrier addition.
No new training prescription or fatigue policy was introduced. `git diff
--check` is clean, and the pre-existing `docs/NOW.md` diff hash remains
`a1e5422e6d8b046d8aa6595dea173f19cd3f7971e657a6665af2a30c780ecae9`.

### NOT COVERED

- Full native Home mounting/layout and Sam's physical iPhone. The actual Text
  leaf runs with only its native host stubbed, alongside real doors and boot.
- Live Coach calls or deployment; no phone build, commits, pushes, screenshots,
  recordings or real athlete data.
- The older unrelated copy-binding diagnostic remains red as described above.

## Phone build and install — 2026-09-07

Sam requested the latest version on his phone. Exported the current working
files, including approved uncommitted changes, into
`/private/tmp/lfa-phone-20260907-acwydbmk/source`; no commits or source checkout
changes. Manifest: 2,576 source/input files, SHA256
`d73fd1815a4b3834177accc018f614d4fc8673019c5eb2fed004ae047c4392fb`.
Both the snapshot and working folder matched all inputs after building.

The first native build lacked generated React files in the export. Ran
`pod install --deployment` inside the copy: exit 0, no source/input changes.
The subsequent clean Release build exited 0. Signature verification with access
to system trust services exited 0. Embedded bundle: 8,602,268 bytes, SHA256
`f1698c68673869666d31391dce90afa7630ddc1cd0b5370b08beb8bc15f9ba7a`.
All three approved strings are in the binary bundle; both obsolete strings and
the debug build identity are absent. Team 66M7FZ6G37, app 1.0.0 (1),
`com.localfootyathlete.app`.

The physical-phone action matrix exited 0. I also started an unnecessary repeat
of the entire release fleet. Sam challenged the delay; I stopped that repeat
(session exit 130) and installed the completed signed build. The interrupted
repeat is NOT a completed green release run. Prior completed checks and this
turn's successful phone-action matrix remain the verification receipts.

In-place installation on Sam's previously identified iPhone 16 Pro Max
(AFA21856-881E-587B-96D5-60817FD11018) exited 0. New bundle container:
85D878DB-C98A-4588-9C8F-E3E95E6C6F56. Install receipt and logs are in the build
directory (`phone-install.json`, `phone-install.log`,
`native-release-final.log`, `signature-verification.log`). No uninstall or app
data reset. Renee's phone was not targeted.

### NOT COVERED

- Sam's visual acceptance and actual full native-screen behavior after opening.
- Live Coach/remote deployment; no screenshots or recordings.
- The interrupted redundant full release repeat is not reported as passing.


## 2026-09-07 — PDF weeks 5–7 dose investigation (awaiting Sam)

Sam requested root cause only, with no code changes before approval. Report: `docs/ROOT_CAUSE_WEEK_5_7_DOSE_2026-09-07.md`. Read attached 63-page three-day PDF and reproduced its set pattern through seven current real-journey weeks. Observer in `/private/tmp/lfa-underdose-trace.cjs` measured `hard → Sore → RPE8` plus recent-fatigue vote selecting `drop_two`; later block history qualifies and raises loads. Set-addition exclusion misses `bodyweight_progressed`, giving back extension and pull-ups both first added load and four sets. One-set main rows also measured in two-day, minimal-kit four-day and beginner profiles. A qualitative-answer-only diagnostic control removes one-set main rows but leaves the first-BW double progression. No product/test/config changes in this investigation; existing dirty work preserved.

Focused suites exited zero: block-two-progression 47 assertions, block-two-ladder 59, progression-capacity-laundering 25. These remain blind to the combined failure. Proposed shared feedback interpretation and shared earned-load predicate, dependency sweep, joined final-prescription tests and mutation controls are detailed in the report. NOT COVERED: implementation, full historical PDF rebuild, full-year/profile space, screen/physical phone, post-fix or full-release verification.


## 2026-09-07 — Sam clarification; proposed scope awaiting approval

Sam explicitly requires no soreness in the app and fatigue/deload reductions lasting one week, not a three-week block. These requests supersede the earlier report's assumption that soreness remains an active feedback input. Do not implement until Sam approves the revised plan. Preserve athlete records and necessary compatibility; do not relabel old soreness as newly reported fatigue.

Verified at source: `sessionFeedbackForm.ts` still emits soreness sections (294/326), requires soreness for affected submission paths (592), and writes it (719/731); `SessionFeedbackPanel.tsx` renders that section (1591). Existing form tests explicitly require “How sore are you?”. The actual phone surface has not been exercised in this investigation.

Standing working instructions from Sam: diagnose the real flow and exact owner first; give candidate rejection reasons where relevant; distinguish measurements from hypotheses. Report complete bounded correction, dependency sweep and tests before approval. After approval cover all callers/readers/writers/checkers/fixtures, persistence/regeneration/display; retire proven obsolete duplicates and preserve compatibility. No invented coaching, unrelated refactors/frameworks or unapproved scope expansion. Show original regression red before fixing and green afterwards; run relevant existing gates and neighbouring behaviours. Never weaken tests or change expectations/baselines for a pass; obtain approval for legitimate expectation changes. Separate verified pre-existing failures from new failures. State unperformed checks. Each response at most 300 words, plain English. Do not call a fix complete while the athlete-facing path remains broken.

## 2026-09-07 — Approved soreness retirement and one-week dose correction

Sam approved “go for it.” R-380 records the approved semantics and
LAW-progression-dose-ownership names the release witness. No commits or push;
pre-existing unrelated changes remain intact.

The accepted compiler is the sole progression writer. Numeric effort wins;
qualitative hard means seven, without inferred soreness. First bodyweight load
cannot also earn an extra set. Authored strength role wins over name heuristics.
Dated reduction history and displayed explanations use one seven-day horizon;
weeks six and seven do not inherit week five's old reduction. Historical
soreness remains readable but inactive; current form and transactions do not
write it. Actual form callbacks, full/partial saves and production boot run in
the regression. Retired progression/conditioning writers moved out of app code
or were removed where no caller remained. Coach references regenerated locally.

Original journey/form regressions failed before the correction. Final dose
matrix: 634 assertions, eight scenarios (four profiles × two effort conditions),
56 scenario-weeks; form callback/save/reload journey: 22 assertions. All five
mutation controls catch their named defect. Annual release witness: 416/416
athlete-weeks across eight athletes. Physical-phone action matrix passed
(transaction harness, not physical UI). Exact receipts are `/private/tmp/lfa-dose-*`.

Full release stopped after 24/35 successful units at obsolete soreness
expectations. Updated modifier coverage now checks refused new reports and
historical records through real persistence and Status/Day/Week readers:
235/0 lifecycle, 11/0 Status, 11/0 Program, 4/0 wording. Surviving restriction
assertions retained. All-scope compilation now reports zero errors.
Remaining release-unit continuation and phone install receipts follow below.

NOT COVERED: native layout/keyboard and Sam's device acceptance; live Coach
provider/deployment. Old diagnostics remain red with matching baseline failures:
extra-session 3, session-outcome 3, accepted-state 14, conditioning adapter 1;
old law registry retains 21 UNENFORCED rows. These are not release witnesses.

### Final release and in-place phone installation

All **35/35 distinct release units** now have successful receipts: the initial
24, the repaired modifier chain, and all ten remaining units. This is not a
claim that the interrupted first invocation passed. Exact machine-readable
summary: `/private/tmp/lfa-dose-release-final-summary.json`; remainder exited
zero in `/private/tmp/lfa-dose-release-remainder.json`. Final compile is zero
in all three scopes. Final dose witness again reports 634/0, form 22/0, and
all five mutations caught. `git diff --check` is clean.

Clean native Release build and deep signature verification both exited zero.
Snapshot `/private/tmp/lfa-dose-phone-xt8z3cgx/source` matches 2,585/2,585
manifest files. Shipped app code still matches the working folder exactly;
only two test files and this status report changed after the native build.
Artifact receipt: `native-artifact-receipt.json` in that build directory.
Embedded main.jsbundle: 8,534,084 bytes, SHA256
`054f160e50172044eba772a5a7d203a86323519fed25a75591c05f7538bd5490`.

Installation over the existing app on Sam's iPhone 16 Pro Max
`AFA21856-881E-587B-96D5-60817FD11018` succeeded, confirmed by
`phone-install.json` in the same directory. Bundle `com.localfootyathlete.app`,
version/build 1.0.0 (1). No uninstall or data reset.

Sam should check full and partial feedback contain no soreness question, and
that the old week-five reduction and notice do not remain in weeks six/seven
without a new report. Gates green, awaiting Sam device acceptance.

NOT COVERED: full native interaction/layout, Sam's acceptance, live Coach
provider/deployment; earlier baseline-matched diagnostic failures remain open.
No screenshots, recordings, commits or push.

## 2026-09-07 — Week-three preparation diagnosis; awaiting approval

Sam requests diagnosis/plan only. No product, test or config changes this turn.
Current three-day four-week journey reproduces Broad Jumps, Medicine-Ball Slam,
tempo running and Saturday conditioning; four exact visible-signature restarts
pass. Report: `docs/ROOT_CAUSE_EARLY_OFFSEASON_2026-09-07.md`.
Receipts: `/private/tmp/lfa-early-offseason-diagnosis/female-year.json`,
`/private/tmp/lfa-early-offseason-selector-trace.json`, and filtered first
selection decisions `/private/tmp/lfa-early-offseason-candidates.json`.
The bundled trace export was empty, so a temporary read-only module observer
captured the actual returned selector traces; no source behaviour was replaced.

Verified: dated clock switches early to mid at week three (R-173). Power policy
and V2 contract permit primers there. WC-131 starts running/conditioning floors;
WC-060 reserves a second run outside the gym days. Current policy tests protect
this: offseason policy 35/0, power policy 57/0, delivery 48/48. Logs under
`/private/tmp/lfa-early-*-existing.log`. These are proof of the expectation gap,
not proof that Sam's intended preparation rule holds. No hard-interval selection
was measured; tempo is not reported as hard merely because it is running.

Proposed first-four-week protection needs approval; do not silently extend the
optional period or invent a later power start. Follow the user's full diagnosis,
complete-owner, regression-first, persistence/display and honest-reporting rules.
NOT COVERED: implementation, native UI, other profiles, historical PDF rebuild.

## 2026-09-07 — Approved four-week preparation correction

Sam approved the diagnosis with “yep”. R-381 records the first-four-week
prescription; participation remains optional only in weeks one/two. One owner,
`OFFSEASON_PREPARATION`, supplies power, conditioning, running and strength
prescriptions. Scheduling, both contracts, validation, canonical regeneration,
sprint eligibility and Coach descriptions now consume it. The obsolete mid
contrast-to-primer branch and retired policy variants are removed. Phase clock,
deload timing and storage formats remain unchanged.

Regression: the real 2/3/4-day journey failed 22 assertions before correction;
final witness passes 403 assertions, including visible session models and
weekly save/reopen. All five deliberately broken preparation rules are caught.
Receipts: `/private/tmp/lfa-preparation-before-final.log` and
`/private/tmp/lfa-preparation-complete-final.log`. The prior dose witness and its five
mutation controls also pass. Previous phone-build input with durable logged
sets/reps upgrades through current boot twice, preserving session feedback and
weight overrides exactly: `/private/tmp/lfa-preparation-upgrade-receipt.json`.

Final targeted power policy/delivery, offseason policy, Section18, block-two
progression/ladder and compilation pass. Reviewed writer inventory reports zero
unresolved owners. Coach reference bundle regenerated locally. Release gate
currently running; clean native Release build/signature verification succeeded,
not installed yet. Build: `/private/tmp/lfa-preparation-phone-kmr31j5z`.

Baseline comparisons: old phase-clock diagnostic retains two failures; workout
canonicalisation retains four. Focused old Bible conformance retains the exact
five invariant failures across four scenarios on both versions. Its extended
wrapper timed out; it is not a pass. These receipts remain under
`/private/tmp/lfa-preparation-*`. No assertions weakened to hide them.

NOT COVERED: completed release run, native interaction, Sam's acceptance,
live Coach deployment/provider, historical PDF rebuild. No screenshots,
recordings, commits or push.

### Release stopped: injury integration needs scope approval

Preparation witness: 403/0 and five mutations caught. Year acceptance:
416/416 athlete-weeks, eight distinct athletes. Compilation: zero errors in all
three scopes. Across the stopped full run and separately executed remainder,
35/36 distinct release units pass. The full invocation itself failed; it is
not reported as green. Summary: `/private/tmp/lfa-preparation-release-final-summary.json`.

The remaining unit is `test:canonical-weekly-compiler`. Its main suite passes
10650/0; injury review fails three assertions and injury preview fails one.
Their previous-phone-build counterparts pass 80/0 and 194/0 respectively.
All four new failures remain intact. The subsequent conditioning repair passes
42/0. No injury production code was changed in response.

Read-only executable observers established three shapes: a selected B-Stance
RDL is removed by the later prohibited-pattern filter; a fully paused second
injury returns the prior workout/summary; an edit to injury-added Weighted Dead
Bug cannot find its target during earlier exercise replay and rolls back.
In the fully paused case, final remaining rows ARE marked injury-unavailable;
do not claim the athlete was told they were safe. Exact diagnosis and proposed
bounded correction: `docs/ROOT_CAUSE_PREPARATION_INJURY_INTEGRATION_2026-09-07.md`.
Receipts: `/private/tmp/lfa-preparation-injury-minimal.log`,
`lfa-preparation-injury-observed.log`, `lfa-preparation-injury-target-observed.log`
in the same directory. Observers only log; no source rules were changed.

Other updated fixture coverage passes 9744/0. Eight neighboring athlete-weeks
match the previous build exactly; later COD drill rows are counted separately
from their three session prescriptions. Existing R-337 placement rules govern
the newly added late-week checks.

Native Release build/signature verification succeeded; paired iPhone reachable.
NOT INSTALLED: the injury checks block release. App code matches the frozen
snapshot; subsequent changes are tests/reports only. No commit/push/reset.

NOT COVERED: injury correction, fully green release, native interaction,
Sam's acceptance, live Coach deployment, historical PDF rebuild.


## 2026-09-07 — Approved injury integration implementation

Sam approved the bounded report above with “approve”. The weekly injury owner
now supplies its prohibited patterns before ladder planning; fully paused
sessions carry the current summary. Accepted injury-era row edits continue
through the existing edit compiler after injury composition. Earlier edits
keep their base ordering; absent exact targets remain deferred, never guessed
by display name. Continuation is derived input, excluded from persistence.

The repeated-edit witness exposed an additional part of the same ownership
boundary: a retained-row strength edit published a whole healthy date override,
reclaiming unrelated conditioning. This first edit changed Steady 5 min Blocks
to Warm-up + Fly 20; the second then failed the conditioning contract. Observer:
`/private/tmp/lfa-injury-repeat-observed.log`. Deferring injury-era row edits
removes the accidental whole-day ownership. No conditioning rule changed.

Receipts: original injury review 77/3 and preview 193/1 in
`/private/tmp/lfa-injury-integration-before-{review,preview}.log`.
Review now 80/0; expanded preview 250/0, including requested dose, second edit,
Undo, conditioning conservation, improvement, earlier edits, Clear and multiple
reopens. Current all-scope typecheck passes. Eleven changed compiler/boundary
owners reviewed; the writer inventory has zero unresolved of 1163 distinct
capability owners. Mutation controls and full release are running.

NOT COVERED: full release completion, native interaction, phone installation,
Sam device acceptance, live Coach deployment, historical PDF regeneration.


### Accumulated checks and mutation liveness

The original fully-paused mutation initially survived: the old paired-injury
fixture no longer entered that branch after the pattern correction. A read-only
observer located a real knee-then-lower-back case. Added actual acceptance and
reopening assertions; the old early return now fails those two checks for the
right stale summary. The current session reader is also asserted to receive the
current summary and unavailable rows. The obsolete test requiring an unexplained
planner withdrawal was replaced within the approved reconciliation: every actual
paused/withdrawn removal must be explained, and hiding any explanation must fail.
No removal-coverage assertion was weakened; the category is no longer forced.

Expanded preview now 274/0 across three athletes, current/future dates,
injury improvement, repeated edits/Undo, Clear and reopening; two earlier-edit
worlds retain their exact accepted sessions after injury/Clear. A substituted
row is identified by its original component, not its changed display name; an
athlete's accepted edit to that slot persists through Clear. Genuinely added
injury rows cease when their injury is cleared.

The program-action matrix passes. Final all-scope compilation has zero errors.
The signed clean Release build succeeded in
`/private/tmp/lfa-injury-phone-l4r9xkgh`; signature verification passed. All 2588
frozen input files match their manifest. Only four test/mutation files currently
differ from the working source; product files match exactly. Previous-build
logged inputs upgrade through this frozen source twice without changing recorded
sets/reps/feedback/weight overrides, while the unwanted preparation work disappears.
Receipt: `/private/tmp/lfa-injury-integration-upgrade-receipt.json`.

NOT COVERED: completed release invocation, installation, native interaction,
Sam's acceptance, live Coach deployment, historical PDF regeneration.


### Final release and in-place phone installation — 2026-09-07

The complete `npm run test:release` invocation exited zero: **36/36 distinct
release units green**, not a combination of interrupted runs. Final receipts:
`/private/tmp/lfa-injury-integration-release.log` and
`/private/tmp/lfa-injury-integration-final-summary.json`.
The full-year witness passes 416/416 athlete-weeks for eight distinct athletes.
Injury review 84/0 (including the actual session reader and fully paused
reopening); injury adjustment 56/0; expanded injury preview 274/0; four injury
mutation controls applied and caught. Preparation 403/0 plus five mutations;
previous dose 634/0 plus five mutations. Final typecheck has zero errors in all
three scopes. Program-action matrix and upgrade-history witnesses passed.
`git diff --check` is clean. No validation inputs drifted during final checking.

Signed clean Release from `/private/tmp/lfa-injury-phone-l4r9xkgh/source`
installed successfully over the existing app on Sam’s iPhone 16 Pro Max,
AFA21856-881E-587B-96D5-60817FD11018. Native build, deep signature verification,
and installation each exited zero. `phone-install.json` confirms success;
`native-artifact-receipt.json` records bundle `com.localfootyathlete.app`,
version/build 1.0.0 (1), main.jsbundle SHA256
`feefe5da8d64fa99c6f937003fb3d25bbd90e7eac734e2d7d324908a7841f6a4`.
No uninstall, reset, screenshots, recordings, commits or push.

Sam should check the three-day athlete’s weeks three/four: preparation lifting,
without automatically added power, running or hard conditioning. If checking an
injury-adjusted session, verify its summary and a saved set edit after reopening.
Gates green, awaiting Sam device acceptance.

NOT COVERED: physical native interaction/layout and Sam’s confirmation; live
Coach deployment/provider; historical PDF regeneration. Earlier diagnostic
failures documented above were outside this release invocation and were not
rerun after the injury follow-up; no blanket whole-app pass is claimed.


## 2026-09-07 — Week-eight club input diagnosis; awaiting Sam

Sam cannot test this audit athlete on his phone and explicitly wants the full
yearly program rerun as verification, after reviewing the new input question.
Do not ask him to verify these audit-program issues on his phone.

Attached PDF SHA256 5304fae988310a83de92cc25d5f65c8dafeebaa7c7be70fad2e4d6afee6b435a
has 63 pages. Read profile and complete week-eight pages (1/2/10/11). Header
shows initial Off-season zero team days; week eight shows Monday 16 November
and Wednesday 18 November club sessions. The PDF's revision 709bf140 contains
the same cohort-runner injection: week eight changes phase and explicitly
supplies Monday/Wednesday via the real profile transaction. This is an audit
scenario input, not an automatic app phase rule. Existing annual checkers
expect the same hard-coded phase schedules, so they do not reject this assumption.

Read-only diagnostic in `/private/tmp/lfa-team-input-probe.cjs` executed two real
seven-logged-week journeys, then the week-eight phase transaction and reopening.
Empty team days: zero club sessions before/after reopen; Monday/Wednesday: two
club dates before/after reopen, matching the PDF dates. Both inputs accepted.
Receipt `/private/tmp/lfa-team-input-probe.json`; no production/test/config edits.
App sheet allows blank club days. Bible says actual team-training nights are
inputs and must not be assumed. Intended athlete scenario still requires Sam.

Plan/report: `docs/ROOT_CAUSE_YEARLY_CLUB_INPUT_2026-09-07.md`.
NOT COVERED: intended club schedule confirmation, exact original 63-page
exporter, full current 52-week rerun and replacement PDF. Await approval before
changing the runners, shared audit inputs, checkers or report behaviour.

## 2026-09-07 — Requested two-day / three-day full-year rerun

Sam clarified that the annual scenario intentionally changes circumstances,
including club sessions from pre-season; he did not request an all-year zero
club scenario. Requested both existing presets rerun from the current app.
No new app/runner/test/config edits, commits, phone actions or native captures.

Ran `scripts/athlete-cohort-year.cjs --preset=<two-day|three-day> --weeks=52`.
Both processes exited zero, each with 52 weeks, 364 distinct dates, 52/52 weekly
reopens and zero checkFailures. Results/receipts:
`output/year-review-2026-09-07/README.md` and adjacent raw run directories.
Source manifest captured 1,400 files; no source hash drift. Includes existing
uncommitted fixes at base 8d06bbde, not falsely represented as a clean revision.

New PDFs: `output/pdf/year-review-2026-09-07/`, 117 and 124 pages. Exact source
row inventories and per-date extracted values checked, all pages rendered and
visually inspected via contact sheets plus representative full pages. PDF-only
builder/QA tools: `tmp/pdfs/year-review-2026-09-07/`.

Audit projection 9/9, row summary 6/6, audit rules 6/6. Annual checks remain red:
older scenario/receipt assumptions, empty selector traces, five Christmas-break
club dates per athlete, and two-day Single-Arm DB Row repeated in week 41.
Combined budget required movement coverage passes; selection fails for repeat.
No silent repairs; no expectation changes. Full diagnosis awaits a separate request.

NOT COVERED: native/phone UI, clinical validation, selector rejection evidence,
full release rerun, root cause or baseline classification of findings. Sam’s
requested annual reports are available without requiring a phone check.


## 2026-09-07 — Authorized Christmas audit correction and regenerated reports

Sam requested the club break cover 18 December 2026 through 30 January 2027
inclusive. Verified cohort action used unrecognized teamTrainingBreak and a
nonexistent span.to; production consumes noTeamTrainingSpan with from/until.
Both yearly runners now use shared auditChristmasBreakAction. Shared dates,
anchor fixtures and existing audit-rule command updated; no production rules,
athlete data, commits or phone actions changed.

New annualClubBreakJourneyTests failed against prior genuine output (ten club
dates in the interval across two athletes), then passed three fresh 19-week
journeys and both fresh 52-week journeys. Each covers 44 distinct closed dates,
lifting retained, February club return and weekly reopening. Four deliberate
artifact mutations failed for the intended reasons. The two full-year reports
contain 364 distinct dates each, 104/104 reopens and zero journey checkFailures.
Full-year team anchor checks: zero findings per athlete.

Audit rules 6/6, projection 9/9, row summary 6/6; broader compiler-year 416/416
athlete-weeks across eight distinct athletes, exit zero. Existing Christmas
diagnostic remains red (normal-week fixture and retired helper), already
classified rewrite_test. Budget audit remains red: repeated Single-Arm DB Row
on July 5/8 in two-day week 41, plus newly exposed transverse coverage flag in
week 16. No selection repair or cause claim.

Evidence: output/year-review-2026-09-07-christmas/verification-receipt.json.
PDFs in output/pdf/year-review-2026-09-07-christmas/ contain 127/133 pages;
all rendered contact sheets and representative full pages reviewed; source
values checked per date. 1,401 source hashes unchanged across the runs.

NOT COVERED: native UI, phone, clinical sign-off, remaining selector/coverage
root causes, full release gate. Sam can review the full-year PDFs directly.

## 2026-09-07 — Conditioning return and fourth gym-day diagnosis

Sam asked why weeks 3–4 have no easy base conditioning, three-day week five
adds Saturday bike sprints, and first running is COD/Speed. Investigation only.
No app/rule/test edits; 1,401 source hashes still match the last annual run.

An external read-only module observer traced the genuine cohort three-day
onboarding/logging/rollover/reopening path for eight weeks. All eight reopens
succeeded; conditioning names, modalities and doses match the delivered PDF's
underlying output for all eight weeks. Evidence:
`output/conditioning-diagnosis-2026-09-07/` (trace, candidates, scheduler,
reproduction, dependency and verification inventories).

Verified: optional aerobic default/minimum zero means no selector invocation in
weeks 3–4. Preparation assertions use every over an empty list. The preparation
gate nevertheless passes 403 assertions plus five mutation arms. Availability
supplies owned machines seven days; Saturday scheduled as running reaches
selection with machines available and runOnly false, so a machine-only template
wins. Both prior annual presets show four distinct off-gym machine dates across
weeks 1–8 (eight conditioning row occurrences each, including warm-ups).

Week-five normal-build target jumps to four stimuli; mini-cycle two rotates to
glycolytic. COD week-five cadence replaces the first easy/moderate receiver;
20 m acceleration is an explicit speed preference. These are existing R-311,
R-329/R-331/R-337 prescriptions; no invented later entry week or new dose.

COD gate 19/19. Existing modality-by-day gate fails before any edit: four pass,
off-season fixture reports no attached conditioning. Historical origin not
established. No assertion weakened. Four diagnostic symptom assertions are red.

Plan: docs/ROOT_CAUSE_CONDITIONING_RETURN_2026-09-07.md. Complete dependency and
verification inventory in evidence directory; awaiting approval and conditioning
progression resolution before implementing.

NOT COVERED: implementation, native UI, new progression prescription, full years
after a fix, all profile/equipment combinations.

## 2026-09-07 — Approved conditioning correction and annual reports

Sam approved the diagnosed correction. One dated equipment owner now supplies
weekly generation, the display adapter (including its fixture fallback) and
injury conditioning. Weeks 3–4 offer the existing optional light finishers on
selected gym dates. COD exchanges tempo before the last base receiver; existing
week-five timing and authored prescriptions remain unchanged. Session rendering,
completion and both annual exporters use the same optional flag. Coach references,
preparation sheet, surviving fixtures, registry and release witnesses were updated.

Fail-before/pass-after receipts cover absent offers, off-gym machines, lost base,
optional display/completion and fixture fallback. Preparation: 457 assertions,
eight mutations. Dated access: 105 profile/date cases plus fallback, four mutations.
Two annual runs: 52 weeks and 52 successful reopens each; all 728 distinct
athlete/date records checked. Broader compiler year: 416/416 athlete-weeks, exit 0.
Two PDFs: 127/134 pages, every generated row retained, visual/layout QA complete.

Release exposed obsolete zero-conditioning expectations and a fully-paused injury
fixture that now retained the new optional aerobic offer. The former now checks
actual optional history; the latter reaches week one through onboarding, preserving
all full-stop assertions. Injury policy was not changed. Normal injury review:
84/84. Its pattern-guard mutation now binds to the actual fully-paused failure
instead of the changed week-three pairing. Complete canonical group and remaining
release units are still running; no aggregate release pass claimed yet.

Receipt: output/conditioning-correction-2026-09-07/verification-receipt.json.
Boundary: docs/BOUNDARY_CONDITIONING_RETURN_2026-09-07.md.

NOT COVERED: native phone/install, live Coach deployment, alternative speed/COD
entry timing, unrelated existing repeated-row/required-coverage findings and
pre-existing diagnostic failures. No commit or device build performed.

Final verification: complete `test:canonical-weekly-compiler` rerun exits 0,
including 10,654 main assertions, 84 injury-review assertions and all four injury
mutations. All remaining 22 release units exit 0. Bootstrap and full typecheck
rerun exit 0. All 37 current release groups are verified across recorded runs;
the first aggregate command's failure remains recorded, not relabelled as a pass.
`output/conditioning-correction-2026-09-07/release-verified.json` maps each group
to its receipt. All app/simulator source hashes match the final annual run;
only the documented test mutation matcher changed afterward. No process remains
running for this work. Existing diagnostic/annual findings and NOT COVERED above
remain unchanged.
