# COWORK SEAT HANDOFF — 2026-08-09 late night, COACH BUILD ERA

For the next Cowork review seat (Opus — Sam switched for the
build-verify loop; Fable returns for design moments only). READ ORDER:
this doc, then COWORK_SEAT_HANDOFF_2026-08-09_UNDO_COACH_ERA.md §2-§5
(liveness law + methods + device facts), then
COWORK_SEAT_HANDOFF_2026-08-06_REBUILD_ERA.md §1-§8 (all seat laws),
then COWORK_SEAT_HANDOFF_2026-08-06.md §1-§3 (base communication).
Then docs/NOW.md + docs/SEAT_INBOX.md. Verify EVERYTHING against git
before telling Sam it is true.

## §0 THE TWO LAWS SAM FORCED ON THIS SEAT TONIGHT (read first)

- SAM CHAT RULE (memory: sam-chat-simplicity-rule): every reply to Sam
  is three parts max — WHAT HAPPENED / WHAT'S NEXT / WHAT TO SEND —
  12-year-old simple, zero jargon, zero file names or commit ids in
  chat. NO file cards for repo plumbing: write inbox/ruling docs to
  the repo via device_bash (python heredoc), never SendUserFile.
  SendUserFile ONLY for things Sam must open (mocks, sheets).
- LIVENESS LAW (previous handoff §2): a terminal is STOPPED after any
  report until typed at. Never say "working/building" without a
  commit/mtime receipt checked at the moment of speaking.

## §1 STATE (2026-08-09 ~9pm)

- UNDO UNIT: CLOSED ON DEVICE. One-step undo (append reversal +
  re-derive, zero new stored state); toast = the only surface (Sam
  killed the bar/sheet mock: "the coach should be its own tab and the
  athlete just talks to it"). Boot was fixed TWICE on his phone the
  same night: total ledger readers + phase wrap (1682f6fd), then
  anchor recovery for pre-ruling worlds (e513365a — the anchor was
  never stored; recovered from world evidence, NEVER the clock).
  PARKED (docs/PARKED_QUESTIONS/UNDO_2026-08-09.md): toast ~6s dwell
  ("forget about that for now"), batch 29 words, coach-undo question.
- COACH REBUILD RUNNING. Kickoff Sam-signed via mock
  (docs/COACH_REBUILD_KICKOFF_2026-08-09.md; mock
  docs/COACH_TAB_MOCK_2026-08-09.html — approved with 3 rulings):
  L-C1 BRAIN LAW (coach knowledge = Bible + rulings, never invents
  policy, asks when no rule answers — "I basically want it to be my
  brain"); L-C2 THE CHANGE CARD (no mutation without it; renders from
  the ProgramControlAction itself); L-C3 NIKE BAR (keyboard/dead-zone
  failures are GATE failures).
- S1 LANDED: tab talks; greeting signed SAM-VERBATIM (batch 30):
  "G'day, I'm your S&C coach. I can answer fitness questions and make
  changes to your program." — ships ahead of the ability BY SAM'S OWN
  RULING (no users yet; if a beta gate arrives before S3, re-check).
- S2 LANDED: answers from the week through the same projection the
  Program tab renders; truth gate on the reply exit; the two-marker
  resolver bug found by hand-probe, fixed, class named for S3.
  Sam device-tested: answers RIGHT in bucket vocabulary.
- S3 ORDERED (in inbox, may be mid-build): changes behind the change
  card, kind by kind, chips return. Order 0 URGENT sits above it:
  KEYBOARD SCROLL DEFECT from Sam's device — history hidden behind
  keypad, list doesn't follow to newest. L-C3 gate failure.
- Architecture settled (docs/COACH_ARCHITECTURE_REASSESSMENT_2026-08-09
  §0): coach output = ProgramControlAction → athlete's door → ledger
  verbatim. No coach writer ever. Salvage layer re-pointed, not
  rewritten; frozen CoachScreen root retires by supersession.

## §2 THE SEAT'S OWN FAILURE TONIGHT — DO NOT REPEAT

L-C3 said keyboard cases are gate cases from slice 1. Slices 1 AND 2
shipped with "no keyboard case exercised" IN THE BOUNDARY, and the
seat waved both through; Sam's phone then hit exactly the predicted
defect ("so much for nike style UI - obviously this was going to be an
issue"). A boundary that names an unmet law is a SEND-BACK, not a
footnote. Bounce it.

## §3 QUEUE

Sam: verify keyboard fix on device; S3 device pass (change card, with
keyboard up); ONE signing sitting when S3 settles (batch 31 five
strings PROPOSED + S3's strings — batch rule, no drip). After S3: the
Bible-grounded "why" layer (Sam ruled (a): changes first), then S4
journal-context (notes stay out — privacy-clean cut; opt-in parked).
Wow-ideas file parked (docs/PARKED_QUESTIONS/COACH_WOW_IDEAS_2026-08-09.md);
Sam's picks unstated, seat recommended 1/3/4.
Fable moments to call back for: the why-layer design, S4 shaping, the
quality pass (COACHING_QUALITY_EXHIBITS), any reassessment.

## §4 MEMORY UPKEEP

Running history: memory lfa-walker-and-post-merge-queue-2026-07-29
(updated through kickoff signing — update after S3 lands). Judgment
ledger: coach-tab call + "why would you assume any are working" both
recorded. Keep MEMORY.md's session-start line pointing at THIS doc.
