# SURFACES-CONTEXT RULING — 2026-08-06

Rules the sixth condition-1 stop (probe 9699f783). Supersedes the
mechanism of docs/CONSTRAINT_VISIBILITY_PRECONDITION_RULING_2026-08-06.md
(its aim stands — the forgettable class dies; its mechanism, a
canonical persisted resolver, is refuted by the PROPOSAL class, 8.6x
the class it targeted). Ruled by the review seat per RULE-DON'T-ASK;
Sam holds a veto.

## Ruling — the gateway takes the SURFACES, not a bare array

The gateway's constraint input becomes the evaluation context:
AcceptedEffectiveWeekSurfaces, REQUIRED, not optional. The truth is
"the world under evaluation" — sometimes the persisted store, sometimes
the world a transaction is composing (a proposal is a decision in
flight, and decisions-in-flight are decisions). The
default-to-live-store exists in EXACTLY ONE composer
(buildFixtureProjection's args.sourceSurfaces ?? liveState — the line
that already models this correctly); every other site passes the
surfaces it means. A required parameter cannot be forgotten — the
class dies structurally, and the delete-vs-move dual-set case
(acceptedStateTransaction.ts:2785-2791) expresses naturally as two
surfaces objects, exactly as signed.

## Conditions

1. The probe's taxonomy is the unit's proof, re-run after the build:
   FORGOTTEN 328 → 0; PROPOSAL 2,827 preserved byte-for-byte;
   agree/absent unchanged.
2. STATUS-DIFF (54) and STALE (3) are triaged in this unit — each is
   diagnosed to a named cause or declared with a payer. Unexplained
   disagreement does not ship silently.
3. programStore.ts:902 (never met a non-empty store in the witness
   set) is priced during the build, not left unnamed.
4. The 146-stamp clock fix (one injected todayISO) remains owed in the
   main build — tracked.
5. Then the final pricing re-runs on the full witness set. Green →
   build → run home (unpiped bible, condition 4 markedDays proof,
   merge, post-merge unpiped bible, R5.7, remaining batches). Not
   green → STOP with the residual named.
