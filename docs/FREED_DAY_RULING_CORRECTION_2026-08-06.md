# FREED-DAY RULING — CORRECTION, 2026-08-06

Corrects docs/FREED_DAY_RULING_2026-08-06.md on measurement (the
option-2 probe at 11ba8cb7). Ruled by the review seat; Sam holds a veto.

## What the measurement showed

The ruling's ground "§18 passing with ZERO shortfall on the derived
week" was computed against a STALE contract: the stored contract never
learned the fixture change, and section18EffectiveWeekEvaluator:526
credited the cancelled game's anchors, so the phantom game paid the
week's third core-conditioning credit. Reconciled to the true facts,
the mode derives in_season_bye_build and §18 BLOCKS with
required_minimum_shortfall — the freed Saturday is owed a session.

## Corrections

1. The SUPERSESSION of 1b ruling 2's baseline sentence is REVOKED. The
   original sentence ("rebuilt bye week: Saturday hard conditioning
   built") was right; cell 8's freedRole === 'required_core' half
   STANDS and is not rewritten.
2. The freed-day ruling itself survives ONLY as its carve-out already
   states: a freed day is empty when the mode's authored policy demands
   nothing there, and BUILT when it does. The measured world demands.
3. The totals-asserted-never-mirrored guard stands and is what caught
   this.

## Approved on the same measurement

- OPTION 2 (contract-to-fact reconciliation): the contract derives from
  the current facts — no stored contract outlives a fixture decision.
  Priced cheap; the one assertion it inverts is re-inverted with this
  dated doc as citation. Necessary, not sufficient.
- The residual defect (the derived bye week under-produces against its
  own corrected contract — the builder does not place the owed
  Saturday session) is INSTRUMENTED BEFORE FIXING: attribute why the
  gateway's repair never reaches the read, then fix at the owner it
  names. Three witnesses already agree the session is owed (§18's
  shortfall, cell 8's role=null, the deletion suite's 16 seed
  refusals) — the fix must green all three without editing any.
