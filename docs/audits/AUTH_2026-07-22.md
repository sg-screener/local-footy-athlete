# Auth Area Audit — 2026-07-22

Seed: `standard-in-season-week` (anchor Mon 2026-07-13), reset again after the
Full-reset probe below. Device: iPhone 17 Pro simulator, iOS 26.3. Checkout:
`local-footy-athlete` @ `main` (c61aceb). Scope: whatever the live app
exposes as sign-in / sign-up / sign-out / account per `QA_RUNBOOK.md`'s audit
order. Not covered by `SUPPORTED_ATHLETE_ACTIONS.md` directly. Tap-record-
move-on only — no diagnosis, no harness changes, no code changes.

---

## Row-by-row

| # | Screen / Action | Expected | Actual | Result |
|---|---|---|---|---|
| — | App launch (seeded state) | Some indication of an authenticated session (account entry point, sign-out control, etc.) | Lands straight on the Program tab. No account/session indicator anywhere on Program, Coach, or Profile tabs. | Confirmed no visible session UI |
| — | Profile tab, scrolled top to bottom | A Sign Out / Account / Session control somewhere in Profile's sections | Full scroll through every section — Program Setup, Coach Adjustments, Learn/FAQ, Developer Tools, Support, Legal, Danger Zone — has no Sign Out, no Account row, no email/session display anywhere. Danger Zone's only entries are "Full reset" (no "Delete Account" row either). | FAIL (no reachable sign-out/account control) |
| — | Danger Zone → "Full reset" → confirm | Per its own label: "Wipes profile, program, calendar and coach history. Returns to onboarding." | Native "Full reset?" confirm alert showed correctly with the same copy. Tapped "Reset everything." | PASS (confirm step itself) |
| — | Post-Full-reset landing screen | Returns to onboarding, per the confirm copy — checked specifically for any sign-in/sign-up/"already have an account" link at this fresh-install-equivalent point | Landed on the "BUILT FOR FOOTY." welcome screen with "Build My Program →" and "Skip onboarding (dev)." **No sign-in, sign-up, log-in, or "already have an account" control anywhere on this screen or, from what's visible, anywhere before it.** This is the one point in the app most likely to show an auth gate, and it doesn't. | FAIL (no reachable sign-in/sign-up entry point) |

No further rows are possible: with no sign-in, sign-up, or sign-out control
reachable from app launch, the Profile tab, or the post-reset onboarding
landing screen, there is nothing left to tap-test per the runbook's
tap-record-move-on method.

---

## Grouped by failure category (per QA_RUNBOOK)

**Dead-control / unreachable feature (1, area-wide):** same class of finding
as `JOURNAL_2026-07-22.md` — the entire Auth area (sign in, sign up, forgot
password, sign out, account) has no reachable entry point anywhere in the
current build. Checked every tab, the full Profile scroll, and the one
fresh-install-equivalent screen (post-Full-reset onboarding landing) where a
gate would most plausibly appear. None of them offer a path to it.

## Screenshots

None — the finding is an absence (no control exists to tap), not a visual
defect to reproduce. The Full-reset confirm alert and post-reset landing
screen were both plain, expected UI with nothing anomalous to document
visually beyond what's described above.
