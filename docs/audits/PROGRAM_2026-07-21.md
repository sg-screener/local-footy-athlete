# Program Area Audit — 2026-07-21

Seed: `standard-in-season-week` (anchor Mon 2026-07-13). Device: iPhone 17 Pro
simulator, iOS 26.3. Checkout: `local-footy-athlete` @ `main` (1fa4cc0).
Scope: Group 1 (This session) and Group 4 (Games) from
`docs/SUPPORTED_ATHLETE_ACTIONS.md`. Tap-record-move-on only — no diagnosis,
no harness changes.

Note: the App.tsx fix for the RN DevTools warnings banner
(`fix(e2e): suppress Fusebox warnings banner that blocks the tab bar`) was
committed this session to `codex/final-verification-unblock` in a separate
worktree, not to `main`. This checkout still runs without that fix, which is
why the banner appears below as a live defect rather than a closed one.

---

## Row-by-row

| # | Screen / Action | Expected | Actual | Result |
|---|---|---|---|---|
| — | Program screen, cold start after seed reset | Clean screen, no overlay | RN DevTools "Open debugger to view warnings." banner rendered over the bottom of the screen, overlapping the tab bar / sheet action buttons. **Re-check 2026-07-21 (post-fix, commit 511ceff): RESOLVED.** After applying the App.tsx `LogBox.ignoreLogs` fix and reseeding, the banner no longer renders — tab bar (Program/Coach/Profile) and the Swap/Add/Move/Bin sheet action buttons are fully visible and reachable. Screenshot: `screenshots/PROGRAM_2026-07-21_banner-gone-recheck.png`. Banner artifact, now cleared. | ~~FAIL (dead-control)~~ → PASS (fixed) |
| 1.1 | MON Lower Body Strength → Swap → Conditioning → Light | Preview/apply change or advisory+override | Refused: "That change isn't possible here (section18_week_rejected)." Internal error code shown to user. No plan change. | FAIL (rules-engine) |
| 1.2 | MON → Swap → Strength → Upper body | Preview/apply change | Same refusal: `section18_week_rejected` | FAIL (rules-engine) |
| 1.3 | MON → Swap → Recovery | Preview/apply change | Same refusal: `section18_week_rejected`. Reproduced a 5th time later in session on the same day (screenshot below) with identical result. | FAIL (rules-engine) |
| 1.4 | Swap session → Rest day | Dedicated swap-to-Rest control with confirm step | No "Rest" option exists in the Swap-To list on any day tested (only Conditioning/Strength/Recovery). The only path to a Rest outcome is Bin (1.6), which has different UX (scope + destructive confirm) than the "Confirm step, then rest" described for 1.4. | FAIL (missing control) |
| 1.5 | MON Lower Body Strength → Move → Wed (occupied, Lower Body Strength → atomic swap) | Only legal days offered; occupied destination swaps atomically | Correct: MON and WED swapped sessions cleanly, no other days touched. Confirmation message: "Done. 2026-07-13 and 2026-07-15 swapped sessions." — raw ISO dates instead of day names/labels. | PASS, with visual defect |
| 1.6 | TUE Team Training + Upper Pull → Bin → "Just the gym session" → confirm | Recurring team schedule untouched; no re-injection | Scope choice and confirm step worked correctly. But: (a) result message read "Upper Pull was removed. Pulling work was added to Wednesday." — the exact removed session type was silently re-injected onto Wednesday (previously Rest); (b) Friday's unrelated derived "Gunshow" session also disappeared (day became Rest), never mentioned in the result message. | FAIL (rules-engine + silent side effect) |
| 1.7 | FRI (Rest) → Add optional session → Conditioning → Light | Legal add succeeds, or advisory+override if risky | Refused: `section18_week_rejected` | FAIL (rules-engine) |
| 1.7 (retest, different category) | FRI (Rest) → Add optional session → Strength → Full body | Legal add succeeds, or advisory+override | Same refusal: `section18_week_rejected`. Confirms the block isn't Conditioning-specific — it hit both Add categories tried. | FAIL (rules-engine) |
| 1.8 | SUN Recovery Session (occupied, day after Sat game) → Add → Conditioning → Hard | Combined block materializes, or advisory+override if risky | Refused: "I couldn't safely make that change, so the plan is untouched." Different message, no internal code, but also **no advisory+override choice** — a flat block. Contradicts the contract's Override Principle ("nothing legal is hidden or hard-blocked; risky choices get advisory + proceed anyway"). | FAIL (rules-engine) |
| — | FRI Gunshow ("OPTIONAL" session) → "Want to change something?" sheet, all levels | Same accessible sheet as every other day | Accessibility tree came back completely empty (no ids/labels at any level: top menu, Swap-To submenu) — the only day of six tested with this gap. Controls were still functionally tappable via raw screen-coordinate taps, confirmed by screenshot, so this is an automation/accessibility-tree gap, not a dead button. | FAIL (accessibility gap) |
| — | Accidental: swipe-to-dismiss on FRI's inaccessible sheet landed on "Something else - ask the coach" instead, opening Coach tab with a pre-filled draft "About Friday 17 Jul: " | N/A (audit navigation error, not a product test) | Draft was never sent; navigated back to Program without submitting. Logged for transparency only. | N/A |
| 4.1 | SAT Game Day → Move or remove game day → Move → Sun | G−1/G+1 re-derive automatically | Correct: game moved Sat→Sun, Saturday auto-became a light session ("Gunshow"), confirmation message named both days plainly: "Game moved from Saturday to Sunday. Saturday was kept light to protect game day." | PASS |
| 4.1 | SUN Game Day → Move or remove game day → Remove | G−1/G+1 re-derive automatically | Correct: message named both affected days explicitly — "Removed Sunday game. Saturday changed from Gunshow to Hard Conditioning. Sunday changed from Game Day to Off." Reversible via an explicit "restore" action in the coach-notes card. | PASS |
| 4.2 | Bye this week → unlocks hard-conditioning menu | Dedicated "bye week" declaration control | No dedicated "Bye this week" control was found anywhere in the Program area. However, removing the week's only game (above) put the week into a de-facto bye state and Saturday did surface "Hard Conditioning" per the contract's test note. Marking as inferred-PASS via an indirect path; the dedicated entry point itself is unverified/possibly absent. | PASS (inferred), missing dedicated entry point |

---

## Grouped by failure category (per QA_RUNBOOK)

**Rules-engine (5):** 1.1, 1.2, 1.3, 1.7×2, 1.8 all blocked by the same
`section18_week_rejected` gate or its unlabeled variant, across Swap and Add
categories, on both a "TODAY" day and non-today days, and on both empty and
occupied destinations. Move (1.5) and Bin (1.6) were not blocked by this gate
— only Swap and Add were, in every instance tried this session. 1.8's block
used a different, code-free message but had the same missing-override defect.
This directly matches the escalation trigger already named in
`SUPPORTED_ATHLETE_ACTIONS.md` Group 6 ("§18 preview-gate refusal of a benign
edit") — recommend treating this as the trigger it's already flagged as,
per `CLAUDE.md`'s Coach Architecture Escalation Rule, rather than a per-item
patch.

**Rules-engine / silent side effects (1):** 1.6's Bin action re-injected the
removed session type onto a different day and silently emptied a third,
unrelated day — both undisclosed in the result message.

**Missing control (1):** 1.4 has no distinct affordance from 1.6.

**Dead-control / navigation (1):** RN DevTools banner overlays sheet buttons.
~~fix exists but hasn't reached this checkout's branch~~ — **RESOLVED
2026-07-21:** the App.tsx `LogBox.ignoreLogs` fix was applied to this branch
(commit 511ceff) and re-verified in the simulator after reseeding. Banner no
longer renders; tab bar and Swap/Add/Move/Bin action buttons are fully visible
and reachable. This was a genuine banner artifact.

**Accessibility (1):** FRI's "OPTIONAL" session sheet exposes no accessibility
tree at any level, unlike every other day type tested.

**Visual (1):** 1.5's confirmation message shows raw ISO dates instead of
day names.

**Known pre-existing gaps possibly touched:** none of the above duplicate the
gaps already logged in `FLOW_AUDIT_2026-07-07.md`.

**Not tested:** pass criterion #5 ("survives app relaunch") — skipped per the
documented environment fact that plain relaunch after a seed session
white-screens; not rediscovering that known issue.

---

## Re-check after banner fix (2026-07-21, post-fix)

Scope: the QA_RUNBOOK-style re-check was limited to the finding marked
**dead-control** (the RN DevTools banner row). No rows were marked BLOCKED.
Steps: applied the App.tsx `LogBox.ignoreLogs(['Open debugger to view
warnings.'])` fix to `main` (commit 511ceff), reseeded
`standard-in-season-week`, and re-tapped in the iPhone 17 Pro simulator.

- **Banner / dead-control row → resolved.** Banner gone on the Program screen
  and through the day-action sheet down to the Swap/Add/Move/Bin buttons. It
  was a banner artifact.
- **Rules-engine rows (1.1–1.3, 1.7×2, 1.8) → NOT banner artifacts, unchanged.**
  Each produced a genuine product refusal message (`section18_week_rejected`
  or its unlabeled variant) at audit time, which means the taps landed and the
  app responded — a banner that swallows taps produces no response. These are
  real rules-engine failures, out of scope for this re-check, and left as-is.

## Screenshots

- `screenshots/PROGRAM_2026-07-21_1.1-section18-rejected.png` — Monday swap
  rejection, reproduced for evidence (5th occurrence this session, same
  message/code as 1.1–1.3, 1.7×2).
- `screenshots/PROGRAM_2026-07-21_banner-gone-recheck.png` — post-fix re-check:
  MON "Edit this session" sheet showing Swap/Add/Move/Bin buttons fully visible
  with no RN DevTools banner overlay.
