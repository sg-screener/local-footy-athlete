# Handoff — Saturday 4 July 2026 (for Codex)

Previous session hit its limits; you're picking up mid-stride. Mount the
parent folder `/Users/samgeurts/Documents/local-footy-athlete` (the LFA
subfolder is empty). **Read `AGENTS.md` and `CLAUDE.md` before changing
anything** — the no-phrase-patches / architecture-reassessment rules are
hard requirements, not suggestions.

## Where the project is

A very large 24 hours. Everything below is COMMITTED and test-green;
most of it is live-verified in the Simulator up to about commit
`380612d`, the last few commits are NOT yet live-verified (see gaps).

**Sheet v2 — the tap-first change menu is COMPLETE** (design:
`ATHLETE_CHANGE_VOCABULARY.md`, all decisions recorded there):

- Day rows + inside-session both have the "Want to change something?"
  door (PlanChangeSheet, shared).
- Swap/Add → russian dolls: Conditioning (Light/Hard) / Strength
  (Upper / Lower / Full / Accessories) / Recovery / Rest. Producer picks
  the concrete session deterministically (variety + date-seed); Strength
  and Accessories are ENGINE-GENERATED dynamic registry templates
  (buildTagAwareSession / buildDerivedSession) via the one context seam
  `coachRevisionTemplateContext.ts`.
- ATHLETE OVERRIDE PRINCIPLE (signed off): nothing is policy-blocked;
  warnings instead (game-week freshness, burnout volume). Validator
  byeOnly mechanism kept but fed an empty list. Free-form content still
  rejected — override covers choice, not arbitrary content.
- Move = swap: any non-game day is a destination; occupied days exchange
  atomically (conservation-validated). Partial merges still forbidden.
- Bin scopes: multi-session days bin per part (team training / gym
  session / conditioning / whole day). Team-training bin = that date
  only.
- Add-on-top: conditioning stacks onto strength/team days (writer mixed
  materialization). Strength/recovery stacking NOT supported (guarded).
- "I'm not 100%": tired (spark/cooked → readiness signal for today),
  sick (sniffle → today becomes Recovery Flow; rough → coach pre-loaded;
  bed-ridden → confirm → shutdown_week clears rest of week, games
  untouched), injured → coach pre-loaded.

**UI overhaul (Program tab):** hero card gone — one week list, selected
day emphasized (defaults to today; nothing selected on other weeks);
real dates ("FRI 3/7") everywhere; This/Next/Last week badges;
quick-action chips removed; bin has are-you-sure.

**Calendar tab REMOVED** (`fc15e69`): Program tab covers all of it;
calendarStore + coach setRestDay machinery kept; initialDate deep-link
kept.

**Weekly taxonomy (signed off, `32fa31a`):** the week list speaks in
categories — strength splits canonical; Recovery; Accessories;
conditioning shows Aerobic Base / Flush Out / Sprint Work / Hard
Conditioning (real session name inside the day). Single owner:
`src/utils/weeklyPlanDisplay.ts` + 42-test suite pinning every inventory
session to its category.

## IMMEDIATE FIRST JOBS

1. **Remind Sam to `git push`** — ~26 unpushed commits on main.
2. **Live-verify the tail commits** (Metro restart first): weekly
   category labels on the Program tab, calendar tab removal, sheet copy
   pass, strength swaps end-to-end on the phone (add Upper/Lower/
   Accessories, open the session, check exercises/loads look sane),
   "I'm not 100%" all five paths.
3. **Then do the audit Sam asked for** (below).

## THE AUDIT (Sam's explicit request)

Analyse the current state of things and find gaps — audit the code for
holes in the flows, then tell Sam what to work on next. Places the
previous session already suspects (start here, don't stop here):

- **Engine one-off prescriptions**: buildTagAwareSession uses placeholder
  rx (3×8-10, 90s) for sheet-added strength sessions. Weekly generation
  applies real prescriptions + load estimates. Athletes WILL notice.
- **Readiness-signal loop**: the tired flows write a signal for todayISO;
  verify the resolver visibly eases the day in the live app, and that
  the signal clears/expires sanely (what happens tomorrow? what if they
  tap it twice? is there any UI showing it's active / undoing it?).
- **shutdown_week + regeneration**: after a bed-ridden clear, what does
  next week's generation do? Does feedback/readiness see a week of
  Rest overrides and react badly?
- **Swapped/added sessions vs weekly caps & running rules**: sheet-added
  sessions bypass generation-time weekly caps (4-run cap, category
  caps). The warning system covers hard-conditioning volume only.
  RUNNING_RULES_PLAN.md is still BLOCKED on Sam's section-5 answers —
  sprint category waits on it.
- **Move-swap ripple**: swapping a G-1 lightened session onto a far day
  keeps its G-1 content but not its context. Is that acceptable
  (athlete override) or does it need a warning?
- **Dead code**: Classic render layers (DESIGN_VERSION hardcoded 'v2' in
  HomeScreen.tsx + DayWorkoutScreen.tsx), WeekViewCard, QUICK_ACTIONS,
  useMonthIndicators/useBlockBounds (calendar-era hooks). Deleting is a
  Stage-4/5-style cleanup — propose, don't surprise.
- **PlanChangeSheet size**: it now owns ~10 steps. Still readable, but
  consider whether step components should split before it grows again.
- **Move-destination labels** show real session names ("Swap with Upper
  Pull") — fine, but check consistency with the new category taxonomy
  everywhere else weekly names appear (coach chat replies, diffs,
  verification messages still use real names — probably correct, but
  confirm Sam agrees).
- **Team-only day snapshots**: team-only days with zero exercise rows
  produce section-less snapshots; add-on-top is guarded off for them
  (day_not_stackable). Verify a team-only day's bin/swap flows live.

Produce: a findings list ranked by athlete impact, with a recommended
next-work order, and get Sam's sign-off before building.

## Workflow facts (hard-won, do not relearn)

- **Metro NEVER sees file edits.** Batch edits + tests, then ask Sam to
  restart Metro (`Ctrl+C`, `npx expo start --clear`) — one restart per
  verification round. Check `buildFingerprint` in turn_start logs before
  trusting live results.
- Simulator = full computer-use tier; Terminal = click/read-only. Type
  into the Simulator ONLY via write_clipboard + cmd+V.
- Supabase deploys + Metro restarts need Sam's terminal.
- Tests: `npx sucrase-node src/__tests__/<file>.ts` (ts-node broken).
  Key suites: planChangeProducer 154, proposal 92, writer 34,
  controller, matrix, weeklyPlanDisplay 42, smoke preflight 623.
  Typecheck: `npx tsc --noEmit`.
- Git: commit with `-c user.name="Sam Geurts"
  -c user.email="geursamuel@gmail.com"`; stale `.git/*.lock` files appear
  after sandbox commits — `rm -f .git/*.lock` (file-delete permission
  already granted this machine).
- Session-end habit: append a plain-English entry to `DEV_DIARY.md`
  (Renee-readable). Today's entries are in; the consolidated wrap-up in
  chat was offered but not yet swapped in.

## Guardrails (standing)

- No phrase/regex patches — systemic fixes only; architecture
  reassessment if the same bug class appears twice (repo CLAUDE.md).
- The producer invariant is sacred: the menu may NEVER offer what the
  validator would reject (test-enforced).
- Registry principle: advertised snapshot, validation signature and
  written workout must agree byte-exactly (round-trip checked).
- Don't touch program generation / coaching philosophy without a
  signed-off plan (main lifts stay on team training days).
- Reliability > latency. A safe refusal beats a confident wrong edit.
- No API keys in code or chat.

## Key docs

`ATHLETE_CHANGE_VOCABULARY.md` (the vocabulary + every signed-off
decision incl. athlete override and the taxonomy),
`RUNNING_RULES_PLAN.md` (BLOCKED on Sam's answers — unblocks sprint),
`CONDITIONING_SESSION_INVENTORY.md` (session catalogue),
`DEV_DIARY.md` (Renee-readable log), `HANDOFF_2026-07-03.md` (yesterday).
