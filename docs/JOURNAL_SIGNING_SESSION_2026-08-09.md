# JOURNAL SIGNING SESSION — SAM SIGNED (2026-08-09)

Sam, on the seat's compiled sheet: "yeah i think that all looks good" —
all items signed AS RECOMMENDED. Commit as authored. The sheet's
recommendations, now his rulings:

## A — WORDS: batches 15-26 SIGNED WHOLESALE, as amended by batch 26's
withdrawals/replacement. Update every batch STATUS line PROPOSED →
SIGNED 2026-08-09 (equality-bound where the binder reaches; the binder
extension stays named debt). Any later wording change is a new ruling.

## B — CONSTANTS: ALL EIGHT SIGNED AS PROPOSED. In
src/rules/journalLoad.ts JOURNAL_LOAD_CONSTANTS, flip provenance
'proposed' → 'signed' with source "Sam 2026-08-09, signing session"
for: streamWeighting (50/50), sweetSpotBand (0.8-1.3),
tonnageModulatedByEffort (OFF — signed as off), regionNormalWindowWeeks
(4), regionSecondaryShare (0.5), regionHotRatio (1.15),
patternDriftThreshold (0.25), minimumWeekCoverage (0.5). This LIGHTS
the load band, Load tile and both attention cards — the signing IS the
switch; expect no other behaviour change, and the boundary report
states what appeared.

## C — DECISIONS, as recommended:
- C1 SIGNED: body-feel words Empty / Heavy / Okay / Good / Flying.
- C2 RULED (a): the after-session question becomes "How did that go?"
  — replaces "How did it compare with what was planned?"; answers
  unchanged. New signed wording, record in the copy sheet.
- C3 RULED: RESTORE "No lifts recorded with a weight this week." —
  the one exception-rule withdrawal that cost honesty comes back
  (un-withdraw in the sheet, one line in the surface).
- C4 OPEN — bar colours (app tokens vs calm ramp): Sam judges on the
  phone after this lands; no change now.
- C5 RULED: "since {date}" renders the MONTH WORD ("since March") —
  the closed-twelve month table already exists on the screen; no
  locale dependence.
- C6 RULED: BUILD THE MONDAY NOTIFICATION — its own slice, after the
  flip lands: expo-notifications dependency, permission asked politely
  and refusable (a refusal is a fact, not an error), Monday-morning
  local schedule, opens the Journal tab. Notification sentence comes
  to Sam PROPOSED before it ever fires. Native module = Sam rebuilds
  with pods; the slice's report says so in plain words.
- MINOR PARKED, all leave-as-built confirmed: tap optional, no region
  on notes, anchor lifts = main lifts, flat = exact, 2/1/0 stays out
  of ratio space.
