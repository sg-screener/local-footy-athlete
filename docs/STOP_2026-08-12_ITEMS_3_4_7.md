# STOP — ITEM 4 BUILT, ITEM 3 DIAGNOSED, ITEM 7 MEASURED AND BLOCKED ON SAM

**LOOP CHECK:** *a cell that cannot catch the defect it was written for* —
**sighting 5** (the vacuous bind, the empty gate, the heading-watcher, the
section-wide string, and today the stress scan that missed its own founding
case). **Disposition: ITERATE, and the practice that catches it every time is the
one that caught it again — MUTATE THE REAL DEFECT BACK IN.** Three of this pass's
cells were wrong on first write and all three were caught the same way, not by
reading.

**HEAD:** `70eb0125` on `main`. Branch verified before every commit. Sam is
asleep; per item 0 this pass did not stop to report.

---

## 1. ITEM 4 — BUILT

**The moderate half of Sam's shape now speaks.** `preferredModerateDayRange` on
the contract and a `moderate_day_missing` finding that is **advisory and can
never be anything else**. `achievedModerateDayCount` had been written on every
assessment since Contract v2 shipped and read by nothing.

**THE MUTATION DEMONSTRATES THE RULING INSTEAD OF ASSERTING IT:** switching the
severity to `blocking` throws `Section18WeekAcceptanceError` and takes the suite
down — the exact outage "advisory, never blocking" exists to prevent.

**AND THE SECOND CAUSE IS THE REAL FIND.** `section18OfferPlacement.ts:508` wrote
`stressLevel: 'moderate'` — **a fourth word**, outside the union
`high | medium | low`, inside an array cast `as never` so the compiler was blind
by construction. The ledger counts a moderate day by `=== 'medium'`, so **every
offer the app marked as the easier option was invisible to the count that decides
whether Sam's shape was met.**

**THE FIX CHANGED NOTHING MEASURABLE AND THAT IS REPORTED, NOT HIDDEN:** all 17
scenarios are identical before and after — 4 of 17 meet both preferences in both
arms — because that placer does not run in them. **12 of 17 still have zero
moderate days.**

Guarded by a source scan, because a type cannot reach past a cast. **It was wrong
twice**: it first matched only a bare literal and missed its own founding case (a
ternary), then flagged a comparison operand for the word it TESTS. Both
corrections carry their own probe.

**NOT BUILT: the generation target toward 4+1** — the item says nothing about
which session becomes moderate or at whose expense. **Sam's call, and it is all
that remains here.**

## 2. ITEM 3 — DIAGNOSED, NOT FIXED

Two measurement passes, both by order rather than by choice, and the answer is
the opposite of what three attempts assumed.

**The chain, every hop measured in BOTH arms:**

    resolver mints the link ......... 30x without the ±7, 23x with it
      -> materialiser sees it ....... 4x without, 2x with
        -> rolling repair projects it  both arms
          -> the proposal carries it   both arms, IDENTICAL
            -> THE COMMIT KEEPS IT ONLY WHEN THE PHANTOM EXISTS   <- the defect

Five candidate causes are dead, each by a trace that printed nothing or printed
the same thing twice. **§9's "one line left" was itself named from a grep and was
WRONG** — that loop never runs on this path either, and the correction is in the
census rather than quietly swapped.

**Attempt 3 is one question:** which line inside the accepted-state transaction
replaces the proposed Monday. **The finding is already bigger than the ±7** — a
commit that re-derives a week can silently discard provenance the proposal
carried, and today an invented fixture is the only thing hiding it.

## 3. ITEM 7 — MEASURED, AND SAM'S OWN CASE CANNOT BE EXPRESSED

- **The premise is confirmed:** strength is capped at `selected.length`, **the
  game day included** — the only allocation whose capacity counts a day it can
  never be placed on.
- **Team days are NOT the defect:** strength legitimately stacks on a team night
  ("Team Training + Upper Pull" is a real generated session). The item's phrasing
  on that point is too strong.
- **`"2 games and 2 team trainings"` HAS NOWHERE TO LIVE.** The profile has one
  game field. The case that motivates the item cannot be built or tested until a
  second-game field exists — the same blocker item 2 records.
- **The copy is one unconditional template**, with no branch for a shortfall the
  fixtures caused. The second sentence is **signed copy and Sam's to sign**.

**Nothing was built.** The capacity change is generation behaviour and must land
against the committed preference baseline, which now exists for exactly this.

## 4. NORTH STAR

**Toward it.** Item 4 gives a written-and-never-read counter its first reader.
Item 3's finding is that provenance survives only because something invented
exists — stored-shaped behaviour propping up a derived one.

## 5. NOT COVERED

- **NO SWEEP.** Eleven suites were run by name and are green; the chain was not
  swept end to end.
- **THE ±7 IS STILL THERE**, and item 4's generation target and item 7's two
  changes are not built.
- **AWAITING SAM, and both are genuinely his:** a second-game field (or a ruling
  that a two-game week is out of scope), and the words for a fixture-caused
  shortfall.
- **A RED THAT IS NOT MINE:** `test:compile` on the other agent's untracked
  `fixtureSettleAfterSetupTests.ts`, unchanged all day.
