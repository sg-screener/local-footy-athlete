# DERIVED DECLARATION RULING — 2026-08-06

Rules the stop at 6ab6ca1d (fixture-identity cells 3/5/6, bisected to
option 2). Ruled by the review seat per RULE-DON'T-ASK; Sam holds a
veto.

## Ruling — the declaration DERIVES; it is not published

Option (b). The fixture door stops publishing a declaration artifact;
the declaration is derived at read from the ledger and the facts —
the same shape as the boot-order ruling one layer down, and the
north-star answer (a published declaration is a stored output). Option
(a) — revoking the stale-contract correction — is REJECTED: two
standing rulings hold it, and (b) subsumes it by construction (a
derived declaration cannot outlive its facts).

## Conditions

1. Entry gate: enumerate every reader of the published declaration
   BEFORE reshaping — each reader is routed to the derivation or named
   as retired. Measured, not assumed.
2. The witness set decides, and none of it is edited: fixture-identity
   3/5/6 green; the L16 slice stays 20/20 (walker and deep); option
   2's greens hold (accepted-state, phase-structure 11/11, deletion
   suite); the §18 stale-contract measurements stay correct.
3. The merge-gating bible run is captured UNPIPED
   (npm run test:bible; echo "BIBLE_TRUE_EXIT=$?") — the literal exit
   line on record, per the declared method gap. Notifications remain
   untrusted (four sightings).
4. Then, in order: condition 4 with the markedDays proof, merge to
   feat/stage-b-stage2, post-merge bible, R5.7 whole-or-none,
   remaining batches. STOP at R5 close.
