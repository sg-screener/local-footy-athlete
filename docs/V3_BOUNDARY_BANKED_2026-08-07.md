# V3 — BANKED AT THE ONE-DOOR BOUNDARY (2026-08-07)

**Sam's ruling: OPTION B.** V3 banks where it stands and the work moves to R5.
The remainder is filed as the **LR-29 replay unit**.

**THE WORDS, and they are deliberate: V3 IS BANKED AT THE ONE-DOOR BOUNDARY;
THE REMAINDER IS THE LR-29 REPLAY UNIT.** Not "closed". Nothing in this document
says closed, because the measurements do not support it and the last four passes
each ended on that same sentence.

## THE NORTH-STAR CONVERGENCE ANSWER, HONESTLY

`docs/NORTH_STAR.md`: *store only decisions, derive everything else.*

**CONVERGED — content and derivation.** What the athlete SEES is derived. The
tier-4 deriver owns the visible week; the week's contract is derived from the
athlete's facts at all three contract-selection lines; reduction OWNERSHIP
derives from the decision that authorised it (`08212473`, equality-bound, 45
consumer calls / DIVERGENT 0); the stored `linkedTypedReductions` mirror is
behaviourally write-only. Representations went DOWN, measurably: ten longhand
copies of the declaration precedence collapsed to one door (`e97c9a49`), and the
four-copy covering-week predicate collapsed with them.

**NOT CONVERGED — decision-accumulated identity.** The stored declaration is
still the READ authority for one thing: state that DECISION doors accumulate
onto it — `authorisedReductions`, and the decision-authored identity
(bye-build / practice-match table selection, illness authoring, in-week
authorised changes). That is stored OUTPUT, and it is exactly what the north
star forbids. It stays until the replay unit.

**So: the derivation half of V3 landed and the accumulation half did not.** The
app is behaviourally sound at this boundary — undo works, nothing ships broken —
and it is not yet convergent. Both halves of that sentence are true and neither
is the headline on its own.

## THE BANKED STATE

| | |
|---|---|
| branch | `feat/r53-v3-switchover`, **UNMERGED** |
| declared failure set | **2 of 156** — `program-control-durable`, `fixture-identity` |
| `test:compile` | EXIT 0, no file regressed |
| the one read door | **LANDED, unflagged** (`src/rules/storedWeekDeclaration.ts`) |
| the flip | **PARKED INERT** — `LFA_FLIP_DOOR=1|2`, never set in any gate |
| the stored declaration | **still the read authority** until the replay unit |
| parity gate | 16/16, its declared entry still firing and still owed |

**Nothing was landed to bank this.** The boundary is the state the branch was
already in; this document names it rather than moving it.

## WHAT LANDED IN V3, IN ORDER

- the switchover boot order and the surfaces context (`6ab6ca1d`, `dc8e8aef`);
- the week's identity derived from the athlete's facts — the leg (v) READ half,
  measured FREE, control set exactly (`8ca5ae24`);
- reduction ownership derived at read, equality-bound (`08212473`);
- the landing set, the parity gate and its classifier, the census instruments;
- **the one read door** — ten longhand selection sites onto one owner, measured
  FREE (`e97c9a49`).

## WHAT DID NOT, AND WHY — THE FOUR REFUTED ROUTES

Every route to making derived the sole authority was measured, and each was
refuted by measurement rather than abandoned by fatigue. This is the section the
replay unit starts from.

| # | route | measured result |
|---|---|---|
| 1 | **Retire the writer** (leg (v) writer half) | 8 new / 1 fixed; retiring it leaves the Restore path's typed-reduction ownership VACUOUS |
| 2 | **Fill the declaration at the writer** (the flip, move (i)) | Four publication sites taught to write `derive()`; the stored declaration moved **0 leaves**. The author is a FIFTH site (`programStore.ts:1449-1462`) in no census — and the mechanism was the PAYLOAD, not the declaration |
| 3 | **Drop the overlay rung** (flip arm 1) | 9 of 156 — 8 new, `fixture-identity` FIXED. The failure SET is **byte-identical to route 1's**, so the class belongs to "the declaration stops answering at read", not to either migration |
| 4 | **Replay identity from facts at the door** (flip arm 2) | 14 of 156 — **paid ZERO of the eight, cost five more.** Proves the missing state is not fact-derivable, and that the door is the wrong place to derive at all |

**§8 SECOND-WALL LAW fired against route 2 at attempt four** and produced route
3 — the alternative that made the writer hunt unnecessary. It is recorded because
the law working is the reason this boundary is a boundary and not a fifth
attempt.

## THE REMAINDER, NAMED

**The LR-29 replay unit.** Filed separately —
`docs/REPLAY_UNIT_KICKOFF_2026-08-07.md` — carrying the class sentence, the four
routes above, the sizing measurements, and the parked scaffolds.

## DEVICE FLAGS AT THIS BOUNDARY

- **completed-day display — LANDED.**
- **Vertical Jump Monday — NOT LANDED, and struck from the flags.** It returns
  with the replay unit, not with this branch. It is measured (`Lower Squat|8`,
  byte-equal in all three parity worlds) only under the parked flip arm, and a
  measurement under an inert flag is not a thing the athlete has. Nothing
  promised that isn't real.

## WHAT IS MEASURED AND WHAT IS NOT

**MEASURED.** Every figure above is from a run recorded in its own dated report:
the 2-of-156 declared set (sweep, world-identity preamble printed); the door's
freeness; the four routes' prices, all set-diffed rather than counted.

**NOT MEASURED.** The banked state has no device evidence — Sam's combined
device pass is the merge gate and has not run. The replay unit is not priced;
its first act is a measured dependency list, by its own opening law. Class B of
the eight (`phase-structure` 8, the lighter-day walker cells) remains
characterised by failure text only, in three passes.
