# Programming remediation — 28 August 2026

Owner: programming-remedy. Candidate: `8dbcbc00c2bcccc5ad221eed6312de3ac78563aa`.
Baseline preserved: `0bcc3353`. No physical install, launch, reset or wipe.
The following report-save checkpoint changes documentation only; verification
and the Release binary remain pinned to the candidate above.

## Outcome

P13's painful-trigger handling is repaired inside the existing compiler/safety
owners. An explicit painful press excludes the whole authored push family,
including power, at every active severity boundary; the existing risk policy
still decides other exercises. It is not a blanket permission to pull through
shoulder pain. Injury replacements respect already-reduced lifting sets.

The accumulated real-onboarding journey covers seven logged weeks, week-eight
deload plus readiness plus shoulder 4/10/pressing, restart, Clear, then re-entry,
block rollover, another restart and Clear. Named witness:
`test:injury-recomposition` / `accumulatedInjuryConditioning`: 186 assertions
green in the focused run. Manual Add plus injury/equipment/Clear/restart is
covered by `specialSessionInputTruth`. Final consolidated receipts follow below.

Other fixes share the existing owners: current/day-specific equipment,
conditioning quality and accepted history, whole-week receiver spacing, typed
Mobility/additional parts, authored special-session equipment, typed modality,
and final presentation. No new program writer, AI generation, alternative
progression engine, or relaxed gate budget was added.

## Item ledger

“Implemented” below means code plus named automated evidence, not physical-device
acceptance. No athlete-facing item is called device-accepted.

| Item | State and evidence / remaining decision |
| --- | --- |
| P01 | Implemented structured projection checks over all 55 authored conditioning templates: work/recovery, unique labels, no ratio-title/Heart-rate/alternative-dose clutter. `programmingInputTruth`; native 4×4 flow. Authored recovery exceptions remain P07. |
| P02 | Partial: current kit, whole-week placement and selection are repaired. No-rack bilateral-versus-unilateral slot policy still requires Sam's decision; see P16. |
| P03 | Implemented precise automatic Gunshow/female-Primer withdrawal outside the existing single-fixture G-1 opportunity. Manual Add and Mobility remain. `spareCapacityOfferTests` and accumulated Add/Undo witnesses. R-237 amended. |
| P04 | Existing screen rule preserved; revised year reports hide power rest while retaining domain rest. `programmingInputTruth` and final report audit. |
| P05 | Implemented full-week conditioning receiver ranking plus quality/seat history and in-run selections. Weeks 3/4 and restart checked by `programmingInputTruth`; before/after reports show delivered results. |
| P06 | Question: retain Leg Press/conventional Deadlift for manual/fallback access, and which exact automatic preference should supersede the existing slots? No catalogue deletion or silent hinge-role substitution. |
| P07 | All-template wording reviewed; 4×4 complete rest preserved. Explicitly authored easy spin/jog/walk-back exceptions remain real active recovery, not relabelled rest. Revising those signed exceptions is an optional content decision, not an implemented change. |
| P08 | Implemented loaded curl preference for advanced automatic selection, preserving band-only fallback and novice availability. `programmingInputTruth`, including mutation. |
| P09 | Implemented typed running/Bike/RowErg/SkiErg/mixed labels and stable authored template names. Mode changes update the option, not a title pretending to be a new exercise. Standalone and combined paths share identity; `conditioningModalityExposure`, accumulated projection and native conditioning flow. |
| P10 | Implemented cyclic whole-week spacing preferences within fixture/availability/safety rules; WC-101 club-night upper pairing retains priority. Constrained 2–6-day/phase/fixture worlds are exercised in canonical/year gates. Adjacency counts include forced pairs and do not prove global optimality. |
| P11 | Existing rcsteps strength Row→Bike identity fix retained; accumulated mixed-day modality checks preserve every non-conditioning row/dose. |
| P12 | Same class as P11. Full signed projection is now exercised at rollover and in every measured annual week. No corrupted-name alias was registered. |
| P13 | Implemented trigger-family policy, reduced replacement dose ceiling, shared open injury horizon and rollover fact compilation. Clear and restart held by the accumulated and special-session witnesses above. |
| P14 | Implemented removal of machine-count preference. Five usable off-leg aerobic templates remain eligible; sparse opportunities rotate quality before template. `programmingInputTruth`. |
| P15 | Partial: typed acceleration/top-end/repeat-sprint selection/history is preserved and exercised. Club credit versus separate top-end exposure conflicts between R-079 and the no-extra-app-sprint floor rule need Sam's ruling; no invented club content or in-season microdose. |
| P16 | Question: should unilateral work replace the second bilateral squat seat without rack/barbell, or lead while the required squat remains? R-014/Bible movement coverage currently requires squat, hinge, single-leg knee and single-leg hip. A lunge was not relabelled as a squat. |
| P17 | Partial: current kit reaches power/Primer; unavailable Box/Trap Bar/Bench choices are excluded and temporary kit survives restart/Clear. Box/Broad/Jump Squat additions to the signed dedicated power pool still need phase/age/impact/dose approval. |
| P18 | COD remains deliberately unforced. Optional flush examples/doses exist, but an automatic need threshold is not signed. Bible §20.1 forbids spare-day filling; the optional-placement ruling expressly reserves need computation for Sam. Which recorded need should enable it? |
| P19 | Answered: present and retained as vertical-push strength; its earlier absence was not established. Promoting Explosive Landmine Press into the dedicated upper-power pool would be a separate role/dose decision, not necessary to answer its presence. |
| P20 | Cross-cutting input/history/final-row fixes implemented; original-partial and corrected-commercial worlds are kept separate. Complete acceptance receipts and remaining limits are below. |
| P21 | Implemented replacement of weekday-first budget truncation with feasible receiver-combination ranking. Original weeks 3/4 move from Mon/Tue/Wed to Tue/Thu/Sat in both athlete examples. No unavailable date is introduced. |
| P22 | Implemented actual Mobility + Strength, Conditioning, Gunshow, Primer and Accessories; typed rows preserve both components, names, Remove, component Move, Undo and restart. Ten real journeys/150 assertions. Native Mobility+Gunshow → drag → Undo → drag again → Save → restart passed on this candidate. The covered Undo toast is fixed and guarded (28/28 Undo assertions). Second Mobility/Recovery merge versus separate part remains a product question; physical acceptance is still owed. |

## Verification strategy and findings

The guards target classes, not the two reported bench names. Trigger tests read
the independent authored movement map; equipment tests conflict the legacy and
current answers; title metamorphisms cover every authored template; real journeys
carry logs, facts and edits through restart and Clear. Mutation tests restore
the faulty policy/identity/spacing branches and must fail for the intended reason.
The annual gate now calls the signed final projection, catching a valid compiler
row whose displayed identity cannot actually render.

The first broader runs found and corrected additional defects:

- Conditioning minutes consumed a lifting-set ceiling, suppressing safe injury
  replacements. The sum now uses typed lifting components.
- A typed RowErg sprint title was classified as running and removed. Typed mode
  now owns exposure classification; actual running remains a negative control.
- Active injuries stopped at the original recorded week in the fact compiler,
  while the modifier stayed active. The shared horizon and staged rollover
  compilation now match restart.
- Strength added to an energy-only container inherited conditioning presentation
  and lost its displayed lifting dose. Typed row roles now determine that split.
- A mode change invented `Continuous Aerobic Bike`, breaking signed projection.
  Authored identity is now stable; typed modality changes separately.
- The old exporter hid unavailable/Skip metadata, making a withheld press look
  trainable. Revised output carries that distinction and counts it separately.
- The broader year gate caught conditioning warm-ups stamped as strength
  accessories. Their authored conditioning role now wins inside the existing
  evidence owner. Both rows of all 55 templates are checked; all 55 warm-ups
  were red before the fix. The annual presentation assertion was not narrowed.
- Native drag found Save changes covering the shared Undo toast. The accessible
  Undo node existed behind Save, so its tap hit Save instead. The existing toast
  now paints above the board control; its transaction, duration and copy are
  unchanged. `test:undo-reversal` adds an anchored layer comparison and a
  mutation that lowers Undo behind Save. Actual Undo restored Mobility beside
  Gunshow and emptied its destination; complete final receipts are below.

Superseded runs are not acceptance: `3c2455c5` had an empty-list test wiring
failure corrected in `9d8f0f7a`; `fdbf7e42` reached the four years but failed signed
projection on four November dates per athlete/world (16 occurrences, four
distinct dates). Interrupted release/mutation logs are retained as diagnostics.
`fc242404` is also superseded: its eight-profile year found the warm-up evidence
defect above. Its 23-mutation run matched 21 expected witnesses, not 23: missing
standalone mode was caught by a different compiler-boundary witness, and the
injury-horizon mutation genuinely survived visible-output checks because another
accepted injury input still protected the output. The former check now includes
the actual eight-week fixture journey; the latter also inspects the real accepted
episode's open horizon before rollover. Final mutation receipts are separate.
The first isolated native build lacked generated dependency sources; a further
attempt hit dependency-symlink resolution. The clean local-dependency build at
fc242404 succeeded and its signature verified. It is not the revised binary.
The 76e9a802 run caught all 24 domain mutants and regenerated all four years,
but the newly measured covered Undo control required the last UI fix. Its gate
was deliberately stopped during the sixth annual athlete; the fresh full gate
below runs on 8dbcbc00. No interrupted run is counted as acceptance.

Diagnostic fleet, not hidden or weakened: `test:week-board` is 80/81 on both
the untouched `0bcc3353` source and this candidate. Its failing expectation asks
the adapter to author strength intent, forbidden by the current composer-owned
field boundary. `test:block-rollover` stops before generation on incomplete
hand-built onboarding. Those diagnostics are not presented as green; the
current release contracts and actual-onboarding journeys are the acceptance
surface. Native drag/Undo/save/restart now has a passing simulator receipt.

## Exact candidate receipts

`npm run test:release` completed with **20/20 units green, exit 0**, on the
exact candidate above. Its scope is 23 validated current product contracts,
test-truth bootstrap and real all-scope typecheck. The isolated gate checkout
matched all 2,337 tracked candidate files before and after the run. The native
build checkout has the same source manifest. The simulator's shared checkout
matches the candidate except for the pre-existing, unrelated `docs/NOW.md`
edit, which was preserved; no product or test source differed.

- [Consolidated versioned verification receipt](/Users/samgeurts/Documents/local-footy-athlete/outputs/programming-remedy-2026-08-28/candidate-8dbcbc00/final-verification.json)
- [Full release log](/Users/samgeurts/Documents/local-footy-athlete/outputs/programming-remedy-2026-08-28/candidate-8dbcbc00/release-exact.log)
- [Compiler-year gate report](/Users/samgeurts/Documents/local-footy-athlete/outputs/programming-remedy-2026-08-28/candidate-8dbcbc00/compiler-year-index.html)
- [Mutation receipts](/Users/samgeurts/Documents/local-footy-athlete/outputs/programming-remedy-2026-08-28/candidate-8dbcbc00/mutations/receipt.json)
- [Simulator receipts, including failed attempts](/Users/samgeurts/Documents/local-footy-athlete/outputs/programming-remedy-2026-08-28/candidate-8dbcbc00/native-flow-receipt.json)
- [Clean Release build receipt](/Users/samgeurts/Documents/local-footy-athlete/outputs/programming-remedy-2026-08-28/candidate-8dbcbc00/native-build-receipt.json)

Completed supplemental checks: all 24 named in-memory domain mutations landed
and failed their intended witnesses; four year-report audits passed; and six
distinct simulator flows have passing receipts. Those are not six uninterrupted
flows in one process. The first cold entry and a readiness relaunch reported
the app stopped; both unchanged flows passed individually on retry. No new app
crash report accompanied the latter. The failed logs remain, and simulator
launch/reload reliability is not claimed fixed. See `native-flow-receipt.json`.

The exact-candidate compiler-year verdict is green: 416/416 measured
athlete-weeks, eight distinct athlete profiles × 52 weeks, and 416 successful
weekly restarts. This uses the existing `yearVerdict` owner, including required
actions, phase transitions, progression/deload arithmetic and mutation controls;
it is not inferred from the illustrated HTML years. All-scope typecheck reports
zero errors in product, devtools and tests. Writer audit: 1,136/1,136 distinct
executable capability owners reviewed, zero unresolved, zero rival authors and
zero derived-output writers. Authority budgets and classifications were not
relaxed.

The clean isolated Release build succeeded, with signature verification exit 0,
version 1.0.0 (1). Its 2,337 tracked source files matched the candidate before
and after the build. Bundle SHA-256:
`3b138b218c12523c493c3fe089158e2187d212c1ef39cfeca5111ac484e7212b`.
The binary was not installed or launched on either phone.

## Corrected year reports

All four reports use this exact candidate and cover 52 weeks / 364 athlete-days
each, with 52 successful weekly restart checks each. The original partial
equipment answers and corrected commercial preset remain separate worlds.

- [Male — corrected commercial kit](/Users/samgeurts/Documents/local-footy-athlete/outputs/programming-remedy-2026-08-28/candidate-8dbcbc00/corrected-commercial-male-year.html)
- [Female — corrected commercial kit](/Users/samgeurts/Documents/local-footy-athlete/outputs/programming-remedy-2026-08-28/candidate-8dbcbc00/corrected-commercial-female-year.html)
- [All four reports, including original partial-kit regressions](/Users/samgeurts/Documents/local-footy-athlete/outputs/programming-remedy-2026-08-28/candidate-8dbcbc00/year-comparison.html)
- [Before/after frequency and spacing](/Users/samgeurts/Documents/local-footy-athlete/outputs/programming-remedy-2026-08-28/candidate-8dbcbc00/before-after-summary.md)

The final artifact audit finds zero projection errors, trainable painful presses
during 17–26 November, corrupted names, empty lifting doses or missing
conditioning modes in these four worlds. Each includes 94 power-row occurrences,
none displaying power rest. `year-audit.json` records the checks and denominator.

Frequency counts displayed row occurrences, not sets, completions or selector
calls. Before/after excludes warm-ups and newly exported Speed because the old
export omitted Speed, and cannot infer old trainability because the old export
omitted Skip metadata. Corrected-commercial male rows: 1,919→1,833 across
125→141 distinct displayed names; female: 2,200→2,005 across 119→137 names.

Each example's adjacent same-part day pairs fell from 26 to 15. The remaining
pairs are all Conditioning in off-season weeks 5–12: Monday/Tuesday and
Thursday/Friday, except the illness week removes one pair. They are not adjacent
Upper or adjacent Lower sessions. The delivered pattern retains Wednesday and
Sunday Mobility while Monday/Thursday carry lower strength plus aerobic work,
Tuesday/Friday conditioning and Saturday Speed plus upper strength. This
explains the spacing trade-off in the delivered example; it is not a proof that
every residual pair is mathematically unavoidable or every quality identical.

## Decisions and remaining work

Sam owns the remaining content decisions in P02/P06/P16, P15, P17, P18 and the
second Mobility/Recovery part in P22. P07's explicit recovery exceptions and
P19's existing strength role are retained; changing either would be an additional
content decision. Implementation owner after those decisions:
programming-remedy or Sam's next assigned task. Existing signed behaviour stays
in force until a decision and its guard land.

QA owner: Sam / release task. The onboarding **2 km skip text-tap inconsistency
remains OPEN-UNKNOWN**, not fixed. The unchanged complete cold-onboarding flow
passed on retry at this candidate, including that tap; the initial attempt
stopped at app launch. A single successful skip does not prove consistency.
The resulting real onboarding and Off-season phase also survived a process
restart. Preserve the skip issue in the next non-seeded pass.
Physical checks must use preserved athlete data; neither phone may be wiped.

After release coordination authorises installation, Sam's acceptance checklist
is: review the shoulder 4/10 pressing case in Day, Week and My Status; confirm
the painful press is absent or honestly withheld and unaffected logged loads
remain; Clear only that injury and restart; add Gunshow beside Mobility, drag
Mobility, Undo, then move again, Save and restart. Check the chosen conditioning
mode and single prescription through logging. These are checks to perform on
preserved data, not claims of physical acceptance or permission to install.

## NOT COVERED

- Physical iPhone installation/acceptance; installation remains held.
- The unsigned product decisions above; all P01–P22 are not closed.
- All seven Add choices in native UI, complete
  Cartesian action/injury/equipment coverage, and globally optimal spacing.
- Native guided painful-trigger entry and the cause of intermittent simulator
  launch/reload interruptions. Trigger legality is measured by the real
  accumulated/compiler journeys, not claimed from an unperformed native flow.
- Rendered HTML layout QA: the in-app Browser rejected the local-file URL;
  no alternate browser route or unperformed visual check is claimed.
- Clinical validation, remote persistence, true OS process-death fault injection,
  and resolution of the onboarding 2 km skip-tap inconsistency.
