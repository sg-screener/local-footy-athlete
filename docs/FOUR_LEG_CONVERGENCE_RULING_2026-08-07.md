# FOUR-LEG CONVERGENCE RULING — 2026-08-07

Rules the STOP in docs/R53_FOUR_LEG_PRICE_STOP_2026-08-07.md. Ruled by
the review seat per RULE-DON'T-ASK; Sam holds a veto.

## The build order — leg (iii) is now load-bearing three times over

1. LEG (iii) BUILDS FIRST, for real, at its two install sites. It pays:
   (a) cell 7's basis mismatch — ONE basis for both deriving and
   evaluating the contract, the same week on both sides; which basis is
   correct is chosen by measurement (lawfulness green, no witness
   regression), not assumption; (b) the published-contract replacement
   below; (c) the §6 collateral it has owned since the derivation-owner
   stop.
2. THEN weekRebuild stops publishing exposureContractV2 — the
   bisect-proven fix for fixture-identity's baseline 3-red (a stored
   contract feeding the derivation meant to produce it). The stale-
   contract correction survives BY leg (iii): the derived contract is
   fixture-aware by construction. The rest of the published declaration
   STAYS — leg (v)'s refutation showed deletion still depends on it,
   and it remains flag-gated evidence, not a build.
3. Leg (iv) rides (proven free). Legs (i)+(ii) land as previously
   priced. Leg (v) stays inert until its dependency is understood —
   likely dies with the fact-door work, not here.

## The two remaining witnesses

- Cell 22's second assertion pins the stored contract that leg (i)
  retires. PRE-AUTHORISED: when leg (i) lands, that assertion re-pins
  to the derived contract's equivalent value in the same commit, citing
  this doc — the dated-supersession form, never a silent edit, never
  before the retirement.
- Cell 23 (conflicted vs safely-rejected) is DIAGNOSED before anything
  is ruled — undo semantics carry athlete decisions; no guess.

## Two laws adopted (loop-audit findings from this stop)

- A RED COUNT IS A CLAIM: every pricing enumerates per-cell identity
  AND failure kind; equal counts with changed composition is movement,
  and two pricings were misread because of it.
- SCAFFOLD ADDS ARE EXPLICIT: never git add -A on the shared tree —
  the seat-file sweep that rewrote scratch/r53-pricing-7's hash is the
  sighting.

## Acknowledged

gate.sh landed and proven both directions — stage 1 of
docs/PARALLEL_GATE_SHADOW_UNIT_2026-08-07.md continues at the next
natural boundary (the sharded runner + agreement law). The piped-exit
counter stands at 2 of 3; gate.sh is the compression already.
