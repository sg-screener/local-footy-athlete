# NOW — overwrite at every checkpoint (pointer, not history)

- **Branches:** feat/stage-b-stage2 GREEN (merge target, untouched);
  **feat/r53-v3-switchover = the live unit and it carries the build**
  (`77565f00`); scratch/r53-pricing-7-probe at `8696193e` holds the shelved
  `DERIVED_IDENTITY` fix and every instrument; scratch/r53-pricing-7
  (`f91ea404`) unchanged.
- **THE BRANCH'S PRICE, re-measured this pass — 155 suites, HEAD `77565f00`:
  2 failures** (`program-control-durable`, `fixture-identity`), down from 3.
  The D13 decompose retired `action-walker:deep`, measured not inferred.
  Report: docs/R53_PARITY_GATE_RED_LEGV_PRICED_2026-08-07.md
- **BUILT this pass, harness only, no product code:**
  (a) the walker's declared reds now declare a SHAPE (`omits`/`invents` sets)
  and the matcher DECOMPOSES a combination into its constituents — the ruled
  D13 answer. 8-shape proof, 3 of which must NOT match, plus a round trip.
  (b) `derivedWeekLawfulnessProof` PORTED — `lastTierFourDerivation`'s missing
  reader — **12/12 unedited**.
  (c) THE PARITY GATE (`lawful-5`), and it is RED.
- **THE PARITY GATE'S FINDING, named to the row:** Monday 2026-08-10
  materialises **7** rows and derives **8**; the missing row is **Vertical
  Jump**. The generated program carries all 8, so the derivation is faithful
  and the WRITE path is short; §18 scores both weeks identically, so it is
  neither a repair nor an authorised reduction. Same coordinate as
  `test:fixture-identity`, which is 5/0 on the merge target only because two
  cells are DECLARED there — this unit deleted the declarations.
- **LEG (v) IS PRICED AND IT IS A TRADE: 9 NEW, 1 FIXED.** 155 suites, sets
  diffed, both arms at HEAD. The one fixed is **`test:fixture-identity` 6/6** —
  condition 1's own witness, and the payer the branch's comment nominated. The
  nine new: `accepted-state-transactions`, `athlete-session-deletion`,
  `readiness-ownership`, `deriving-device-commit`, `illness-clear-game-week`,
  `fact-horizon`, `action-walker`, `action-walker:deep`, `phase-structure` —
  **named, none diagnosed**. Under standing conditions it cannot land.
- **AND THE SAME ARM REACHES ONE WORLD AND NOT THE OTHER.** In the parity
  world the stored overlay STILL carries its declaration and payload with both
  declaration writers removed, and the gate still reds — so a third producer
  exists and is **not named**. `postGenerationConstraintValidation:1742/1813`
  looks exactly like it and was REFUTED BY TAPE (zero hits on that path).
- **Condition 1's re-measure: NOT GREEN, residual named** (the Vertical Jump
  row). Cells 5/6's re-pin NOT done on purpose — the pins already hold the
  derived answer and leg (v) greens them; re-pinning to 7 would be
  expectation-edited-to-match-the-regression.
- **Next, and it is the seat's call:** diagnose the nine worst-first (leg
  (ii)'s seven were one class once measured), or name the parity world's third
  producer first. Recommendation: the nine, worst-first.
- **Parked:** parallel-gate stage 1 remainder (inbox item 2); cells 22/23; the
  53 profile-write debt files; done-flag + todayISO tape oddities.
- **Tracked debts:** door unification (payer of a live red); the D13
  session-template debt (all three entries still carried); hydration
  snapshot-vs-live pin; R5.7 whole-or-none; remaining R5 batches; condition 4
  markedDays proof; the merge.
- **Device-pass flag for Sam (one, and it is the only one):** **completed-day
  display** — a completed day that used to show a softened intensity now shows
  what was actually completed. That is the fact-horizon boundary working.
- **Gate:** full `test:bible` UNPIPED, **EXIT 1** on
  `test:program-control-durable` — the declared red; the chain stops there and
  the 63 suites after it are measured by the sweep. `test:compile` EXIT 0.
  Sam's combined device pass = the merge gate.
