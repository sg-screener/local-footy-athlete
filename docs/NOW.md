# NOW — overwrite at every checkpoint (pointer, not history)

- **Branches:** feat/stage-b-stage2 GREEN (merge target, untouched);
  feat/r53-v3-switchover = the live unit; scratch/r53-pricing-7
  (`9d7d8ddc`) holds legs (iv)/(v), the ported seed fix and this pass's
  probes; -6, -5, -5-before preserved.
- **Current unit:** the seat correction (`a375b49f`). Both owed items
  done; **the recorded hypothesis is REFUTED — STOP with the filler
  named.** See docs/R53_SHARP_PROBE_FILLER_NAMED_2026-08-07.md.
- **RE-PROVEN — the cell-19 blocker is retired**, with the flag LIVE this
  time (`1eb65683`). One worktree, both sides: scaffold's ORIGINAL seed +
  leg (iii) = **21/1**; shipped seed fix + leg (iii) = **22/0**; flags off
  = 22/0. The earlier "22/0" on the main branch was INERT and is
  superseded by this.
- **THE FILLER IS NAMED, and it is NOT the declaration's reader.**
  `validateLiveWeekOverlayWrite` — which matches the hypothesis exactly —
  prints ZERO times on this path. The real chain is
  `buildFixtureProjection` (`acceptedStateTransaction.ts:2029`) →
  `buildWeekScopedWorkoutOverlay` (`weekRebuild.ts:217`, reason
  `one_off_no_game`), from `stageRollingHorizonFixtureRepair` and
  `commitProgramSetupRebuildTransaction`. It builds from the REPLAN's own
  workouts, so the publication's deliberate `{}` is overwritten AFTER by
  this rolling-horizon republish.
- **Not gated on the declaration:** overlays built = 84 flags-off, 59
  leg (v), 59 legs (iii)+(iv)+(v). What leg (v) changes is whether the
  payload SURVIVES the commit for that week (AFTER-txn `[]`).
- **Excluded:** `additionalOverlays` never collide with the primary week;
  `commitWeekScopedOverlay` replaces wholesale (clause (c)'s merge is
  innocent, re-confirmed).
- **Next question, sharp:** why does the projection's payload fail to
  survive the COMMIT for that week under leg (v), when the projection
  still produces it? A question about the commit path, not the build path.
- **Open for the seat:** the correction's "no new ruling needed — build
  leg (iii)(+iv) first" branch does not apply *on its stated grounds*
  (the filler never read the declaration). The convergence order may still
  be right — the seat's call.
- **Parked:** condition 1's re-measure (the premise it waited on changed);
  leg (v) re-price; cell 22's 2nd assertion (leg (i) not landed); cell 23
  undiagnosed; parallel-gate stage 1 items 2–4; the other 53 declared
  profile-write debt files.
- **Tracked debts:** hydration snapshot-vs-live pin; todayISO clock fix
  (146/172 wall-clock stamps); R5.7 whole-or-none; remaining R5 batches;
  condition 4 markedDays proof; the merge.
- **Gate:** last full test:bible UNPIPED on this branch exited 1 on
  test:fixture-identity 3/3 — the branch's pre-existing red, verified at
  HEAD in a clean worktree; the 18 suites it cannot reach ran 18/18. Sam's
  combined device pass = merge gate. Full test:bible UNPIPED before any
  commit.
