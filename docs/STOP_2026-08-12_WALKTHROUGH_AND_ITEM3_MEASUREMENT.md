# STOP — THE OWED WALKTHROUGH IS GREEN, AND ITEM 3'S ASSUMPTION IS REFUTED

**LOOP CHECK:** *a plausible cause named instead of a measurement taken* —
**sighting 4** on the ±7 (attempts 1, 2, 3, and every report about them).
**Disposition: COMPRESS, and the compression is the order's own rule made
concrete** — attempt 2's first act is now a MEASUREMENT with nothing built until
it prints, and §7 of the census names what to instrument. A fifth guess would be
the failure this law exists to stop.

**HEAD:** `2a981d55` on `main`. Branch verified before every commit.

---

## 1. THE WALKTHROUGH SAM CLEARED THE SIMULATOR FOR — GREEN, END TO END

`.maestro/golden/lower-body-deletion.yaml`, `1c887b6c`.

**The assertion that matters is the first one: the conditioning part is VISIBLE
BEFORE THE DELETION.** In every seeded world it was not, until the seed
stabiliser learned to carry its row-id references. **That fix was proven in node
last pass; this is its on-glass half.** Then: strength scope deleted →
conditioning remains; whole day deleted → the day is rest; and rest survives a
checkpoint and relaunch.

**TWO THINGS ONLY RUNNING IT COULD TEACH,** both now commented where they bit:

1. A day offering **ONE** scope SKIPS the "remove what?" step and lands on its
   confirm — so the second deletion has no scope row to tap.
2. Every id goes through `stableTestIdToken`, which turns `whole_day` into
   `whole-day`. **The underscore never reaches the tree.**

## 2. A CORRECTION TO MY OWN PREVIOUS REPORT

I recorded three `maestro` processes holding the device as **a concurrent agent
running flows alongside me**, and wrote that into the receipt and a stop report.
`ps -o etime` says otherwise: **1 day 12 hours, 1 day 10 hours, and a `maestro
hierarchy` at 22 DAYS.** They were abandoned runs still holding the iOS driver.
Killed; the owed run then passed first time.

**The standing lesson survives, the attribution does not:** two Maestro sessions
on one simulator interleave taps, so a collided run is "unknown", not "red" —
**but ask `ps` how old the other process is before calling it a collision.**

## 3. ITEM 3 — THE MEASUREMENT THE SECOND-WALL LAW DEMANDED

Sam freed the generator; this was the first act under that, and it is the one the
order named: *"the dependency record is written by the REPLAN, not the craft
tier, and every attempt so far has ASSUMED the craft tier's anchors feed it."*

**THE ASSUMPTION IS FALSE.** Three writers of `dependency.source` exist and only
one MINTS a date:

| writer | what it does |
| --- | --- |
| `sessionResolver.ts:734` | **MINTS**, from `effectiveGameDatesAround` |
| `section18AcceptedWeekGateway.ts:753` | **COPIES** |
| `fixtureMinimalReplan.ts:577` | **COPIES** |

The craft tier's ±7 feeds `validateProgramWeek` — **findings only**, and no
finding can become a dependency source. **So deleting the ±7 could not have
removed the following week's record directly**, which is what attempt 1's revert
was blamed on. Two downstream candidates are named and ordered in §7 of
`docs/FIXTURE_AUTHORITY_CENSUS_2026-08-12.md`.

**Attempt 2's first act is a measurement, not a fix. Nothing is built until it
prints.**

## 4. NORTH STAR

**Neutral.** One instrument proven on glass, one assumption refuted. No stored
state added, no derivation changed.

## 5. NOT COVERED

- **ITEM 3'S DELETION IS NOT DONE** — only the measurement that unblocks the next
  attempt. The ±7 still fabricates neighbouring games.
- **ITEMS 4 AND 7 ARE NOT STARTED.** Sam handed over three items mid-turn; one
  measurement is what fitted in the remainder of it, and saying "started" of the
  other two would be the word this repo retired.
- **NO SWEEP.** The walkthrough and the targeted suites ran; the chain did not.
- **THE THREE FOREIGN REDS ARE UNCHANGED** — `test:compile` on the other agent's
  untracked `fixtureSettleAfterSetupTests.ts`, and the pre-existing "manifests
  add no seed families" cell in two dev-e2e suites.
