# DERIVER-ACQUIRES-REPAIR-SEARCH RULING — 2026-08-06

Rules the build question at the fourth condition-1 stop: does tier 4
at read acquire the publisher's repair search, or does the retirement
stop at the contract? Ruled by the review seat per RULE-DON'T-ASK;
Sam holds a veto.

## Ruling — the deriver ACQUIRES the search

Tier 4 performs the full authored repair order (Bible :4688 —
relocate to the highest-scoring valid day; substitute; move optional
work; typed reduction last) as part of derivation, output never
stored. The publisher's re-planning capability MOVES into the deriver
— one owner, the code migrating rather than duplicating. Grounds: the
repair-writeback ruling already states it ("the §18 repair becomes
part of DERIVATION"); "the builder owns the week" (a derivation that
cannot re-plan does not own the week it returns); and the
measurement itself — the published week is the lawful one, so
lawfulness lives in the search, and the search must live where the
week is made.

## Binding conditions

1. The search runs only on violation — a conforming week derives
   without it (the common path stays cheap; the work-bill law
   applies).
2. The search is DETERMINISTIC: seeded ordering, no wall-clock. The
   146 wall-clock stamps per derivation are paid in this unit —
   todayISO becomes ONE injected input to derivation (the outcome
   door's clock fix, applied at the deriver), stamps derived from it.
3. Resolved authority carries into the search: every relocation or
   substitution the search performs names the fact or decision that
   authorises it (the fixture fact authorises the G-1 re-plan).
   Athlete-emptied days stay empty — last is not highest.
4. The two lawfulness-suite corrections (derived contract reads the
   decision ledger; declaredSubphase follows derived anchor state)
   are keepers and land with the unit.
5. Condition 1 stands, final pricing: full witness set including the
   lawfulness suite in ALL its worlds (pin's world 8/8 AND cell 3's
   world), fixture-identity 6/6, deletion full with mutation
   witnesses, accepted-state full, phase-structure 11/11, L16 20/20
   both walkers on the real tree (the deep 19/1 must vanish with the
   scaffold), derived-repair-ownership able to red. Green → build →
   run home. Not green → STOP with the residual named.
