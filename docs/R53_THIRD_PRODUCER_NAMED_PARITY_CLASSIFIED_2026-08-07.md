# R5.3 — THE THIRD PRODUCER, NAMED BY TAPE; THE PARITY DIFF, CLASSIFIED (2026-08-07)

LOOP CHECK: attribution-stated-from-two-snapshots-instead-of-a-tape — sighting 3
this unit (the freed-day "filler" was 1, the write-validator source reading was
2) — COMPRESS, and the compression is a correction of my own last report:
**"the write path drops the row" was my claim, and the tape refutes it.** A
statement about which side of a difference MOVED is not made by comparing two
snapshots; it is made by watching the transition. The seat's own order named the
instrument and the instrument overturned the finding it was sent to confirm.

Nineteenth pass. Answers inbox item 1 (a), (b) and the first slice of (c).
**BUILT on `feat/r53-v3-switchover`.**

## THE HEADLINE

| step | result |
|---|---|
| (a) the third producer | **NAMED** — `commitAcceptedStateTransaction`, reached from the athlete's REMOVAL |
| (a) does the stored payload drop the row? | **NO — it is written with all 8 rows at EVERY store write** |
| what does cost the row | **the stored DECLARATION being present at READ**, dose-response across three arms |
| (b) the classification | **STORED-WRONG** — declared, flagged, does not block |
| the parity gate | **16/16 GREEN**, mutation-proven three ways, and now in the bible chain |
| (c) do the nine share a mechanism? | **YES for six of them, and it is one sentence** |

## (a) THE TAPE, AND WHAT IT OVERTURNED

Instrumented the FIELD at its commit point, as ordered: a recording wrapper on
`useProgramStore.setState`, so every path that lands a week overlay is caught by
construction. File-backed. Positive control inside the same run: **38 / 84 / 125
store writes and 15 / 33 / 51 recorded transitions** across the three worlds —
no zero is relied on anywhere.

**THE THIRD PRODUCER:**

```
commitAcceptedStateTransaction (acceptedStateTransaction.ts:837)
  <- commitAthleteSessionDeletionTransaction (:3291)
  <- planChangeProducer.commit (:2830)
  <- commitVerifiedAgainstVisibleWeek (:2551)
```

It writes the DECLARATION and the PAYLOAD in the **same store write**, and it is
reached from the athlete's own removal — which is why retiring the two overlay
writers left the stored week standing. Neither of the writers leg (v) removes is
on this path.

**AND THE PAYLOAD IS NEVER SHORT.** Every payload transition the tape recorded
is `0 -> 8` rows, with `Vertical Jump` present, and the only `8 -> 0` events are
the proof's own deliberate drop. **The stored Monday always holds all eight
rows.** My previous report's "the write path is short a row" is withdrawn.

**SO WHAT COSTS THE ROW? The declaration's presence at READ**, and the evidence
is dose-response across three measured arms of the same world set:

| arm | worlds where Monday loses `Vertical Jump` |
|---|---|
| branch — both overlay writers publish the declaration | **3 of 3** |
| builder publishes, publication call site removed | **1 of 3** |
| both removed (the priced leg (v) arm) | **0 of 3** |

Two hypotheses were tested and REFUTED on the way, both worth recording so
nobody re-runs them: **resolve order** (eight consecutive resolves in one world,
all eight rows — the resolve is idempotent there) and **the write validator**
(`postGenerationConstraintValidation:1742/1813`, which reads exactly like the
producer and taped **zero hits** on this path).

## (b) THE CLASSIFICATION, AND IT IS BUILT INTO THE GATE

The ruling: attribute with a law citation before a diff is anything;
STORED-WRONG gets a declared entry and a device-pass flag and does not block;
unattributed and derived-wrong always block. That is now the gate's structure,
not a note in a report.

`DECLARED_PARITY_DIFF` carries one entry —
`stored_declaration_costs_mondays_power_row`, cited to the FIXTURE IDENTITY law,
with the exact rows it covers, its payer (leg (v)) and its expiry (no stored
`exposureContractV2` for anything to answer from).

**Mutation-proven three ways, which is the only reason to believe it:**

| mutation | result |
|---|---|
| the entry names a different row | **RED** — `UNDECLARED: only-derived ["Vertical Jump"]` |
| the entry is deleted | **RED** in all three worlds |
| an entry is declared for a diff that never happens | **RED** — the stale-debt cell |

Container identity (`microcycleId`, and the `:week-overlay:<date>` id suffix
`cloneWorkoutForOverlay` appends) is NORMALISED and reported rather than
compared: it names which container a row came out of, which is the very thing
leg (v) retires, so comparing it would make the gate fail by definition. Stamps
were already normalised for the same reason.

**`test:derived-week-lawfulness` is 16/16 and is now REGISTERED IN THE BIBLE
CHAIN** (157 entries), beside `test:derived-week-ownership`. It joined in the
commit that greened it, exactly as the last report said it would.

**Device-pass flag, PENDING not current:** when leg (v) lands, Monday gains its
`Vertical Jump` back on the phone. It is not visible today, because leg (v) is
not built.

## (c) THE NINE — SIX OF THEM ARE ONE SENTENCE

Set-diffing the worlds before diagnosing singly, as ordered. Every failing
suite in the leg (v) arm was run and its failure TEXT read:

**CLASS A — the accepted week no longer knows what kind of week it is (6):**

| suite | what it says |
|---|---|
| `readiness-ownership` R15 | "the COMMITTED accepted week must be illness_recovery, got in_season_game_week" |
| `deriving-device-commit` D1/D4 | "not re-authored under illness_recovery (silent no-op), mode=in_season_game_week" |
| `illness-clear-game-week` G3 | "precondition: illness week authored, mode=undefined" |
| `fact-horizon` T1/A1 | "the fact reached the derivation but was never materialised" |
| `accepted-state-transactions` 1/2/5 | "Wednesday game anchor missing", "bye table was not selected", "practice-match table not selected" |
| `athlete-session-deletion` 1/3/8/9 | "bye-build hard conditioning template missing … rebuilt reads []" |

**One mechanism: the week's IDENTITY lives in the stored declaration, and its
readers still read it from storage.** Leg (v) retires the WRITE without
retiring the READ — the shape this repo has a name for. The fix is not nine
fixes: it is the readers deriving their contract, which is what leg (iii)'s
`deriveWeekContract` already does at the three contract-SELECTION lines and
what at least eight other read sites do not.

**CLASS B — a planner offer re-derives differently (2):** `phase-structure`
cell 8 ("the flush SURVIVED the fixture-change rebuild") and `action-walker`
("accepting a lighter day" → `required_minimum_shortfall:main_strength:2`).
Not diagnosed; consistent with class A but not shown to be it.

**CLASS C — the ratchet working (1):** `action-walker`'s stale-debt cell fails
because **`session_list_calls_a_conditioning_day_recovery` stops redding** under
the arm. That is not a defect: leg (v) PAYS a declared D13 debt, and the entry
must be deleted in the commit that lands it. A red that is a debt payment.

So the nine are, at the characterisation level: **six of one mechanism, two
unexplained, one a payment.** The seat's precedent held — leg (ii)'s seven were
one class once measured, and so are most of these.

## WHAT IS ON THE BRANCH

The parity gate's classifier, the declared entry with its citation, the
container normalisation, the stale-declaration ratchet cell, and the chain
registration. **Harness only — no product code moved in this pass either.**

Gate: full `test:bible` UNPIPED and `test:compile` — exit lines in
`docs/NOW.md`. The leg (v) arm stays unlanded at `1d831279` on
`scratch/r53-legv-price`.

## FOR THE SEAT

Leg (v) is now a much better-understood trade than "9 for 1":

1. It fixes `test:fixture-identity` 6/6 **and** an athlete-visible row loss.
2. Six of its nine reds are ONE unfinished half of the same leg — the readers
   of `overlay.exposureContractV2` deriving instead of reading. That is a
   bounded census, and the census gate shape already exists.
3. One of the nine is a debt PAYMENT requiring a deletion, not a fix.
4. Two are unexplained and are the honest unknown.

**Recommendation: leg (v) is re-scoped from "the writer stops" to "the readers
derive, then the writer stops", priced as one unit.** That is the same shape
that made leg (iii) land — one owner, no half-state — and it is the sentence
the eighth-pass ruling was already reaching for.

## NOT COVERED

- Class B's two suites are named, not diagnosed.
- The read-side mechanism by which a present declaration costs the row is
  attributed by dose-response across arms, not by a line-level tape of the
  resolve.
- The reader census for `overlay.exposureContractV2` is sketched from a grep
  (8+ sites), not enumerated by a gate.
- `test:action-walker:deep` was not re-run under the arm; the bounded tier's
  failure text is what class C rests on.
