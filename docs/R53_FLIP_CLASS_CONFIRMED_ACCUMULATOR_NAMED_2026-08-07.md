# R5.3 — THE FLIP (a)+(b): THE CLASS SENTENCE CONFIRMED, A SECOND CLASS FOUND, AND THE CARRIED FACT-HORIZON CLAIM REFUTED (2026-08-07)

LOOP CHECK: **pin-the-"already covered"-claim** fires, and it pays for itself
immediately. (b) ordered the carried `fact-horizon` attribution re-confirmed
rather than believed. It does not hold — different cell, different sentence. The
pin cost one suite run; believing it would have put an unattributed red into the
landing commit.

Twenty-seventh pass. Answers inbox item 1 (a) and (b). **NOTHING BUILT** — the
flip stays flag-gated (`LFA_FLIP_DOOR`), inert.

## (a) THE SET-DIFF AGAINST THE RECORDED DIAGNOSIS

The leg (v) era recorded six reds as one sentence —
*"the accepted week no longer knows what kind of week it is"* — with each cell's
failure TEXT (`R53_THIRD_PRODUCER_NAMED_PARITY_CLASSIFIED_2026-08-07.md:106`).
All six suites of the flip arm were re-run under the arm and their text read.

**THE SENTENCE HOLDS, VIA THE READ SIDE INSTEAD OF THE WRITE SIDE, FOR THREE:**

| suite | recorded text | flip-arm text | verdict |
|---|---|---|---|
| `accepted-state-transactions` 1/2/5 | "Wednesday game anchor missing" / "bye table was not selected" / "practice-match table not selected" | **byte-same, all three** | **CONFIRMED** |
| `athlete-session-deletion` | "bye-build hard conditioning template missing … rebuilt reads []" | **same sentence, WIDER — 4 recorded cells, 14 here** | **CONFIRMED, wider** |
| `illness-clear-game-week` G3 | "precondition: illness week authored, **mode=undefined**" | "precondition: illness week authored, **mode=in_season_game_week**" | **CONFIRMED** — same cell, and the changed value is the mechanism: the read falls back to the covering microcycle (which says *game week*) instead of finding no declaration at all |

**RECORDED CLASS B, UNCHANGED AND STILL UNDIAGNOSED (2):**

| suite | text | verdict |
|---|---|---|
| `phase-structure` 8 | "the flush SURVIVED the fixture-change rebuild on 2026-08-11" | **byte-identical to the recorded Class B.** Not diagnosed, in either pass |
| `action-walker` / `:deep` | "accepting a lighter day derives from the fact and never touches the athlete's surface" | recorded Class B, now firing in the deep walker too |

## THE FINDING (a) DID NOT EXPECT — A SECOND CLASS, AND IT IS THE ACCUMULATOR

Three cells across two suites are **NOT** the recorded sentence and appear in no
recorded diagnosis:

| suite / cell | text |
|---|---|
| `athlete-session-deletion` 22 | "Restore removes only its typed reduction and preserves an unrelated reduction — **owned reduction missing from accepted contract**" |
| `action-walker:deep` | "**Restore over a reduction-authoring removal**: ownership DERIVES and binds equal" |
| `accepted-state-transactions` 7 | "low readiness commits a canonical fact and reduced visible program together — **readiness-reduced week has blockers**" |

All three are the same thing, and this branch already named it in another
context: **the stored declaration is an ACCUMULATOR of the week's authorised
reductions** (`R53_LEGV_READ_HALF_LANDED_TAPE_STOP`, carried in `NOW.md`). The
covering microcycle's base contract does not carry them — reductions accumulate
onto the OVERLAY's declaration as decisions land — so dropping the overlay rung
drops the accumulation, and the Restore path can no longer find the reduction it
owns.

**THIS IS (c)'s NAMED PIECE, AND IT IS NOT YET A STOP.** (c) pre-frames the fix
as replaying the week's decisions onto the base contract at read, and names
"authorised in-week changes" as exactly what replay must supply. The STOP
condition is a piece that *cannot* be recomputed from ledger + facts. This one
is not shown to be that — the opposite, on two prior measurements:

- **the ledger is COMPLETE** — the write-side tape corrected the earlier
  "159 missing decisions" to *330 occurrences = 40 rows = 23 decisions, no door
  fails to append* (`R53_REMOVAL_DOOR_ATTRIBUTION_2026-08-07.md`);
- **the replay already exists** — `deriveWeekContract`'s `withRemovalLedger`
  applies a constraint's typed reduction to a contract and skips one already
  present, so applying twice is identity.

So the accumulator is a BUILD under (c), not a classification STOP. Named here so
it lands as a measurement rather than as a surprise inside the build.

## (b) FACT-HORIZON — THE CARRIED ATTRIBUTION IS **REFUTED**

| | cell | text |
|---|---|---|
| recorded (leg (v) era) | **T1 / A1** | "the fact reached the derivation but was never materialised" |
| flip arm, measured today | **T4** | "illness_severe rewrote 2 completed day(s): 2026-07-20 `Mixed/core/High/4ex` → `Mixed/core/Moderate/5ex`; 2026-07-21 `Team Training/core/High/3ex` → `Team Training/core/Moderate/3ex`" |

**Different cell, different sentence.** T1/A1 do not fail under this arm at all.
So `fact-horizon` is neither the class sentence nor the previously attributed
failure: it is the **completed-day boundary** — a severe illness rewriting days
the athlete has already DONE. That is Sam's own standing device flag
(*completed-day display*), reached from a new direction.

**The carried claim is struck.** It was carried into the last report with an
explicit caveat that it had not been re-confirmed; the caveat was right.

## THE STANDING AFTER (a) AND (b)

| class | suites | status |
|---|---|---|
| **A — the week no longer knows what kind of week it is** | `accepted-state-transactions` (1/2/5), `athlete-session-deletion`, `illness-clear-game-week` | **CONFIRMED**, via the read side |
| **B — a planner offer re-derives differently** | `phase-structure` 8, `action-walker`/`:deep` (lighter day) | recorded, unchanged, **still undiagnosed** |
| **D — the ACCUMULATOR / Restore** | `athlete-session-deletion` 22, `action-walker:deep` (Restore), `accepted-state-transactions` 7 | **NEW, named this pass** |
| **C — the debt paying itself** | `derived-week-lawfulness` | pre-authorised; deletion owed in the landing commit |

**8 of 8 are now accounted for**, where the last pass had 5 undiagnosed and 1
carried. Class B remains characterised but not mechanised — it is the one thing
between here and (d).

## WHY THE ARM IS NOT THE FLIP, AND WHY THAT MATTERS FOR (c)

Worth stating plainly, because it changes how the 8 should be read: **the arm
drops the overlay rung without deriving anything.** Classes A and D are both
"the base contract lacks what the accumulated one had". (c)'s replay is exactly
what supplies it. So these are not eight defects to fix — they are the measured
size of the hole (c) has to fill, and the arm's price is an UPPER bound on the
built flip's price, not an estimate of it.

## WHAT IS MEASURED AND WHAT IS NOT

**MEASURED.** All six suites re-run under `LFA_FLIP_DOOR=1`, TRUE_EXIT read per
suite (never a totals line), failure TEXT read and diffed against the recorded
diagnosis cell by cell.

**NOT MEASURED, and nothing here rests on it.** Class B is characterised by its
text, not by a mechanism — neither pass has diagnosed it. Class D's derivability
rests on two PRIOR measurements (ledger completeness, `withRemovalLedger`
idempotence) re-read this pass but not re-run. `accepted-state-transactions` 7 is
grouped into class D by reading its text as a reduction failure; it is the least
certain of the three and would move if the build says otherwise. No device
evidence. The built flip is not measured at all — only the rung-drop arm is.
