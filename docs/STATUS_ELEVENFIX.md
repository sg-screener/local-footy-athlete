# STATUS - eleven scoped programming fixes

Owner: `elevenfix`. Started 2026-09-01 from exact shared checkpoint
`f4b77a445999301658e33c5bbe515e40f354c905` on branch
`codex/failure-only-state-export`.

## Scope fence

Only Sam's numbered items 1-11 are owned here. Work proceeds in that order.
Unrelated red suites are measured and reported, never repaired. Existing
unrelated tracked/untracked shared work is preserved. Sam stopped the audit
output step on 2026-09-01, so this checkpoint ends after focused final-program
and restart checks. No PDF is generated in this job.

## Starting shared state

- Tracked shared edit not owned here: `docs/NOW.md`.
- Shared untracked handoffs, lived-year status, Maestro flow, output/tmp trees
  and fuse-hidden files are not owned here.
- Item 5 already has a current shared acronym owner and receipt at checkpoint
  `4ba6935c`; this task will verify the full requested capital set rather than
  duplicating that mechanism.
- A prior Christmas-break flow exists, but item 9 specifically asks for one
  pre-season setup answer plus both persisted dates. Its current fit is still
  being measured and no claim is made yet.

## Implemented source owners

1. The opening Off-season exposure contracts and their typed exemption now
   own zero Speed for weeks 1-4. R-311's conflicting Week-1 requirement was
   removed from the Bible, ruling row, registry row and focused gates.
2. The load-authority owner classifies Crab Hold as unloaded, so neither the
   card label nor its control can say `BW`.
3. The annual export projects `mainMuscles` from the signed muscle metadata;
   the PDF renderer adds the reporting-only column.
4. Exercise programming metadata caps automatic Swiss Ball Hamstring Curl at
   `consistent`; manual selection remains legal for advanced athletes.
5. The display-token owner preserves `QL` beside the already-owned SL/DB/RDL
   and other capitals.
6. Canonical upper-session slot tables now ask for one push accessory and one
   pull accessory when legal, retain core/football support, and the composer
   prevents same-family duplication.
7. `annualFootballPhaseCalendar.ts` owns the next audit's 7 Off-season / 19
   Pre-season / 26 In-season weeks: 16 November and 29 March are actual accepted
   phase shifts, not headings.
8. Four-session layouts author Lower-Upper-Lower-Upper and the legal schedule
   scorer makes that chronological alternation win.
9. Pre-season onboarding collects the Christmas yes/no plus last/return dates
   with the shared calendar picker. The persisted profile feeds canonical
   weekly availability and reconstructs identically after process death.
10. The next-audit accepted profile is prepared to use Monday/Wednesday club
    nights in Pre-season and Tuesday/Thursday in-season. The audit itself was
    explicitly deferred by Sam.
11. The composed-dose owner applies the Bible's in-season lower-main 2-4-rep
    band to bilateral RDLs even if a supporting seat selected the movement.

## Verification before checkpoint

- `test:eleven-items`: 17/17 focused cells plus 2/2 real cold-onboarding
  opening-Speed cells; final Christmas week/restart equality included.
- `test:compile`: product 0, devtools 0, tests 0.
- `test:slot-coverage`: 84/84, including generated final upper sessions.
- `test:exercise-display`: 34/34.
- `test:render-truth`: 30/30.
- `test:real-running-speed`: 12/12; `test:weak-point-focus`: 62/62.
- A one-week full-kit annual-driver probe generated successfully and exported
  real `mainMuscles`; its Crab Hold row carried an empty load label and its
  upper session carried both accessory slots.
- Mutation/liveness pass: a combined mutation killed nine focused cells
  (Crab Hold, Swiss Ball, capitals, calendar, both four-session orders, club
  schedule, Christmas span and RDL dose); removing the pull-accessory slot alone
  killed the final upper-session cell. All mutations were restored and the
  focused boundary returned to 17/17.

## Separate red suites - not repaired

- `test:rules-kernel`: 123/124, the existing unclassified `other`-unit cell.
- `test:exercise-intake`: seven existing Reverse Nordic / manual-add / fixture
  cells.
- `test:onboarding-field-influence`: existing `ageRange` and
  `trainingLocation` consumer declarations.
- `test:christmas-break`: its older normal-week fixture is red and the suite
  then calls the missing `weekIdentityForWeekForTest` export. The new focused
  onboarding/restart path is green.
- `test:weekly-scheduler`'s main scheduler cells are green; the chained
  `travel-zero-equipment` mutation cell is separately red.
- `test:rdl-family`: three existing pairing/generation cells; none concerns
  the requested in-season rep ceiling.

## NOT COVERED - checkpoint boundary

- Sam explicitly deferred the next male/female 52-week audit. No final audit
  JSON or PDFs were generated for this checkpoint. Item 10's schedule is wired
  into the driver for that later job. A pre-stop diagnostic JSON under the
  shared untracked `output/` tree is not a deliverable and is not checkpointed.
- No simulator, clean Release build or physical-iPhone acceptance has run.
- No unrelated red suite above was investigated or repaired.
