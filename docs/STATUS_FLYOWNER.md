# STATUS — canonical session-work ownership (`flyowner`)

## Scope

Repair the exact saved/restarted advanced-male Christmas-break journey reported
by Sam at checkpoint `3e6ac18bf90c73de2996fdc43fafc6fcf9a6a69b`:
one logical Fly 20 prescription must not enter both Speed and Conditioning, the
final session title must describe the final rows, and every app/audit/PDF reader
must project the canonical `day.rows` once.

## Ownership and collision check

- Read the other active status files before editing. `yearrepair` already owns
  the annual-export `day.rows` projection and `speedRows` evidence boundary;
  this unit preserves and exercises it rather than creating another exporter.
- `condnames` already made `buildSessionTemplate(...)` the visible component
  composer, including the Speed section. This unit preserves that owner.
- Existing unrelated `docs/NOW.md` and untracked files are not this seat's work
  and will not be touched.

## Options compared before production code

1. **Local deduplication/conditional fix.** Drop a repeated name or catalogue
   identity in the session template or PDF. This could hide the second row, but
   would leave two upstream selections, contradictory deload ownership and a
   title derived from the wrong intent. It also makes display code decide
   programming.
2. **Enforce the existing canonical boundaries.** Give one selected authored
   template one visible role before rows are materialised; make the weekly
   deload quality decision before incompatible selection/transformation; let
   `buildSessionTemplate(...)` build the final visible list; derive the title
   from that final content; keep every export on `day.rows` and validate
   `speedRows` as evidence only.

**Chosen: option 2.** It removes the whole two-owner bug class instead of
hiding one duplicate.

## Running findings

- The canonical workout held only two physical exercise rows: `Warm-up` and
  `Fly 20 (20+20)`, both named by `speedBlock.exerciseIds`. The duplicate was
  created later by `getSessionComponentRows(...)`: its standalone-conditioning
  fallback claimed those same unsectioned rows after the typed Speed bucket had
  already claimed them. `buildSessionTemplate(...)` faithfully rendered both
  buckets, producing four rows from two.
- The annual TypeScript projection already returned `day.rows` once, but the
  Python PDF reader independently appended `speedRows`. It was a second display
  source despite R-307.
- The weekly deload owner was elected before the feasibility pass could finish
  the Speed choice, and chronological metabolic work outranked the later typed
  Speed block. At row transformation, `Fly 20` was also re-inferred from text
  and its Speed warm-up could spend the one quality allowance. The result was a
  reduced 10/10 Fly carrying the easy-aerobic sentence.
- Conditioning identity fell back through the recovery/aerobic container when
  no rows belonged to the Conditioning bucket, so the title described the old
  container rather than the final Speed content.

## Verification status

- **First exact run, before production code:** 164/170 cells passed, six failed.
  The failures were dual Speed/Conditioning ownership, duplicated final rows,
  invalid Speed evidence, 10/10 plus easy-aerobic wording, the wrong title and
  the PDF's second row source.
- **Final exact journey:** 173/173. It uses real onboarding, phase shift,
  production schedule actions, 15 accumulated weeks and a restart every week.
  The final Sunday is exactly one Warm-up plus one Fly row, titled `Speed
  Conditioning`; Fly is three sharp reps and owns the scheduled deload's one
  quality exposure.
- **Neighbouring green gates:** `test:deload-quality-owner` 13/13,
  `test:session-components` 39/39 and `test:programming-audit-projection` 7/7.
  `test:conditioning-intensity-modality` was 19/19.
- **Required liveness mutations, each restored:**
  - re-appending `speedRows` in the PDF killed 2 of 172 cells;
  - letting the standalone fallback reclaim typed Speed rows killed 5 of 172;
  - removing the typed Speed handover to the deload row transform killed the
    two contradiction/quality cells (171/173).
- **Existing red outside this unit:** `test:session-template` is 87/88 on its
  pre-existing numeric-header source assertion. The broader conditioning
  identity command is 63/73 on two ordinary aerobic-dose expectations and
  eight generated-week fixture expectations; none contains the new Speed-only
  identity shape. The large canonical compiler command also reaches an existing
  typed duplicate-component selection failure and then a strength-minimum
  onboarding refusal. These were reported on first run and not repaired here.

## What catches the next defect of this class

`test:session-work-ownership` is in the programming release/Bible chain. It
drives the saved journey through the real doors and compares the app, annual
audit and actual Python PDF projection. Its final assertions inspect canonical
component ownership, final rows, title and deload copy together. The smaller
13-cell deload gate independently pins typed Speed priority and proves a warm-up
cannot consume the Fly exposure.

## Programming ambiguity

None. Existing R-034 and R-261 uniquely determine the answer: a small,
fully-recovered Speed exposure may remain in a deload, counts once, and must not
also become easy aerobic conditioning. No new programming rule was invented.

## NOT COVERED

- Physical iPhone and any phone build (explicitly forbidden for this unit).
- Obsolete audit warnings.
- Native layout/glass rendering, VoiceOver and Dynamic Type.
- Regeneration of the old PDF artifact itself; the production PDF row reader
  and the exact exported payload were exercised directly.
- The unrelated pre-existing red cells listed above.
