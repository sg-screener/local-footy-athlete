# Icon candidates — sheet for Sam's live pick (2026-08-01)

Unit 4 of the day shift, serving the queued icon-picking session ("terminal
proposes 2-3 glyph candidates per flagged row, Sam picks"). Imagery is judged
on glass (§6-II-h), so nothing here ships — this sheet is the AGENDA for the
live session, parked in `docs/PARKED_QUESTIONS_2026-08-01.md` §4.

Flag source: Sam's device-pass note that icon meanings weren't obvious
(recorded in the day-first direction notes: labels under icons), plus the
checklist item 8 inventory. Rows whose glyphs Sam has already explicitly
kept (shield on Accessories; globe on Away; the three WeekReadinessSheet
fixes) are listed as KEEP-unless-vetoed rather than re-opened.

Candidates per flagged row (2-3 each, described; drawn live on device):

| Row | Current | Candidate A | Candidate B | Candidate C |
|---|---|---|---|---|
| Short on time today | hourglass | hourglass (keep) | stopwatch | clock with cut segment |
| I'm sick/flat today | pulse line | thermometer | pulse line (keep) | droplet-off |
| I'm injured | plaster/bandage | plaster (keep) | bandaged limb | medical cross |
| Missing equipment? | (current) | dumbbell with slash | wrench | box/kit outline |
| Swap (menu) | (current) | two arrows circling | left-right arrows | — |
| Add (menu) | plus | plus (keep) | plus-in-circle | — |
| Move (menu) | (current) | arrow into calendar day | drag handle + arrow | — |
| Remove (menu) | (current) | minus-in-circle | bin | — |
| Gunshow row | (current) | flexed arm | dumbbell small | — |
| Mobility row | (current) | figure stretching | wave/flow line | — |
| Rollover retry (NEW, Batch 8) | none (text only) | refresh arrow | none — text stands | — |

Session mechanics: one row at a time on the running app, Sam says keep/A/B/C
or describes a fourth; picks land as one commit with the icon-rule gate
re-run (no two adjacent rows share a glyph — ruling 10's standing check).
