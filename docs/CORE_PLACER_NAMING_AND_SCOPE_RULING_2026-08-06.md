# CORE PLACER — NAMING + FIRING-SCOPE RULING, 2026-08-06

Rules the two items declared open at 61a89757. Ruled by the review seat
per RULE-DON'T-ASK; Sam holds a veto.

## 1. Name alignment — the TYPE names itself

The placed session carries the canonical typed name: "Hard
Conditioning" (workoutCanonicalisation:344). Governing law: the
charter's type-names-itself rule (L-P6 precedent — a typed session
renders its charter name, exactly the law the deep walker enforced for
Gunshow) and one-name-per-session card identity. The template row
("Hard Intervals") is the session's CONTENT, not its identity. The
placer aligns to the canonical name; witness 3's seed is unedited and
greens on its own terms.

## 2. Regression 11 — repairs ride accepted commits only

The principle, from recorded law (doors refuse honestly; a refused
transaction leaves the week unchanged; signed sentences never claim
state-dependent outcomes that are false):

- A repair candidate that ADDS work fires only on a commit whose
  outcome is ACCEPTED. It never fires inside a refused or impossible
  outcome — a refusal leaves the week untouched and its disclosure
  carries no new "I also rebalanced" clause.
- MEASURE which side regression 11's world sits on before changing
  anything: if that world's outcome is a refusal surface, the placer is
  scoped out of it and the original sentence returns; if it is an
  accepted commit that happens to contain an impossible relocation
  attempt, the repair may fire and the disclosure must honestly carry
  it. The measurement decides the branch; this ruling decides both
  branches in advance.

Neither witness nor seed is edited under either branch.
