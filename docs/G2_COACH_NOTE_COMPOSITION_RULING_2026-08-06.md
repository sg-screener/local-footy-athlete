# G-2 COACH NOTE — COMPOSITION RULING, 2026-08-06

Ruled by the review seat per RULE-DON'T-ASK; Sam holds a veto.

Sam's signed Coach Note sentence ships COMPOSED: the signed wording is
the template, character-for-character, with exactly three slots — the
session day, the game day, the injured body part — filled from signed
vocabulary (day names; the Sam-signed injury region vocabulary). A
fixed string would state the wrong body part or day for most worlds,
which the copy law forbids (signed sentences never claim
state-dependent outcomes that are false).

The declared gap stands as filed: SignedCopyParam forbids free-text
placeholders by design, so this sentence lives outside the signedCopy
registry until a vocabulary-signing unit extends the registry to
typed-slot sentences. That unit is queued, not folded in.
