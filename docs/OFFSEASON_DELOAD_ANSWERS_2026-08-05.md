# Off-season deload — the three answers, from the Bible

The survey (`docs/OFFSEASON_DELOAD_SURVEY_2026-08-05.md`) put three questions
up. All three answer from the Bible; **no new ruling was needed**, and none was
taken.

## 1. The week-8 deload is CORRECT and stays

Authorities: `LFA_PROGRAMMING_BIBLE.md:110` ("from week 5 onward, normal 3-4
week deload cycles begin") and `:4613` ("later late-off-season blocks may use
the approved deload policy in Section F"), plus Sam's reconfirmation that late
off-season is where deloads live.

**The instruction to remove the block-2 deload is WITHDRAWN.** The survey's
stop was correct: that week is off-season week 8, not week 4, and removing it
would have deleted a deload the Bible mandates.

Recorded so the withdrawn instruction cannot be re-derived from the ruling doc
it appeared in.

## 2. The unit-7 gap resolves by the deload-SHAPE law

Not by removal. The law is the one already in force:

> **"Same week, same days; the structure doesn't change, the load does."**

A legitimate deload week that changes the day layout, or drops an anchor lift,
breaks it. That is the payment, and it is **its own unit** — differential
prediction first, as usual.

The declared gap `offseason_block2_deload_week_restructures_the_days` keeps its
framing until that unit lands, at which point the mechanism's own
stale-declaration cell forces its deletion.

## 3. The readiness/illness door stays un-phase-gated — DECIDED, not an oversight

`resolveDoorDeloadPolicy` carries no phase gate, so a fatigued athlete can get
a lighter week anywhere, including inside the off-season weeks 1–4 optional
block. That is blessed by the deload-rules line itself:

> `:110` — "Deload as well when the user is fatigued or showing a lack of
> readiness."

It stands. Recorded here as a DECISION so no later sweep reads it as a missing
gate and closes it.
