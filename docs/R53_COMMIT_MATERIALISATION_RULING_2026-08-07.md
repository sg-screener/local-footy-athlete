# R5.3 COMMIT-MATERIALISATION RULING — the tape's hop, ruled — 2026-08-07

LOOP CHECK: write-side producer feeding stored output, found by
measurement — sighting 2 of this exact shape in R5.3 (sighting 1: the
repair search, ruled into tier 4) — COMPRESS: the standing derivation
ruling is the compression and it applies; this doc is that application
plus the one signed-behaviour clause the tape flagged.

Answers docs/R53_TAPE (adc45dcc). Ruled by the review seat per
RULE-DON'T-ASK from recorded law, cited inline. Sam holds a veto.

## The classification — output, not decision

The payload materialised inside commitAcceptedStateTransaction is
COMPUTED OUTPUT: the tape shows it built from the replan's own
workouts, reproducible from ledger + facts, triggered by the proposed
declaration. It records no athlete choice. Standing derivation ruling
stop-condition 1 does not hold — classification is not uncertain. The
signed accept-and-reduce rulings own what accept DOES for the athlete
(present the offer, honest reduction, forward_decision/restoration);
they do not sign WHERE week content is produced. Moving production
from write-time to read-time with proven parity does not touch signed
behaviour — the same transformation class as L15's read-ingress lifts
and the deletion seed's identicalJSON switch.

## The ruling

1. The convergence order SURVIVES with corrected grounds: leg (iii)
   builds first — not because the filler "reads the declaration"
   (refuted), but because the deriver must SUPPLY at read what
   commitAcceptedStateTransaction materialises at write, before the
   declaration that triggers that materialisation can retire.

2. The conformance gate, before leg (v) re-prices: derived week ==
   materialised week, byte-equal (identicalJSON precedent), measured
   on the tape's own fixture worlds (freed week, both flag states).
   The tape stays as the instrument — it already records every hop.

3. The write-time materialisation and the declaration RETIRE TOGETHER
   with leg (v), only after gate 2 proves parity. No intermediate
   state where one is gone and the other compensates.

4. Condition 1's re-measure UNPARKS after gate 2, on the corrected
   order: legs (iii)+(iv) with parity proven, then leg (v) on its
   real price.

## Standing

Nothing else moves. STOP conditions unchanged; full test:bible
UNPIPED before any product commit; STOP at R5 close.
