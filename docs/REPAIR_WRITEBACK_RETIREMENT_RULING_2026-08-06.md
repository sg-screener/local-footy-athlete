# REPAIR WRITE-BACK RETIREMENT RULING — 2026-08-06

Rules the stop in docs/R53_DERIVATION_OWNER_STOP_2026-08-06.md. Ruled
by the review seat per RULE-DON'T-ASK; Sam holds a veto.

## Ruling — the stored §18 repair retires; repair rides derivation

The accepted-week repair loop (programStore.ts:1404-1418 inside
canonicaliseAcceptedStateCandidate) stops writing repaired days into
the overlay. The §18 repair becomes part of DERIVATION: it runs where
the week is derived and its output is never persisted, so the derived
week conforms to its contract by construction — which answers the
practice-match concern (an undeclared main-strength session is dropped
by the repair at read, exactly what cells 5/6 assert).

Grounds: R5.1's law (a landed decision settles by re-deriving — this
writer keeps a stored replan on every accepted commit, the same class
one surface over); the queued unit of 2026-07-29 which named this
exact retirement and its exact entry condition; the mutation price
already measured (3/3 → 5/1 with the write skipped).

Scope is acknowledged as larger than option (b) authorised — this doc
IS the authorisation, as its own ruled unit.

## Binding conditions

1. ENTRY GATE (the 2026-07-29 unit's own condition): enumerate every
   read path of the accepted week and prove each re-derives — no
   reader may see the unrepaired week. Measured, not assumed, before
   the write is removed.
2. Class B is untouchable: overlay contracts carrying DECISIONS
   (illness reductions, undo state, athlete removals) are stored
   decisions and stay. The retirement targets the §18 repair
   write-back only. The census's false Class B line
   (postGenerationConstraintValidation:1735) is corrected in the same
   commit.
3. Cell 6's second route (athlete removal writes overlay content
   first, so the proxy stops holding) is measured and owned in this
   unit — same defect, second door; no proxy left standing.
4. The witness set decides, none of it edited: fixture-identity 3/5/6
   green; athlete-session-deletion 24/24 + 5/5 + 3/3 (the surface that
   already charged 14 regressions once); L16 20/20 both walkers;
   accepted-state; phase-structure 11/11.
5. Then, in order: merge-gating bible UNPIPED with the literal
   BIBLE_TRUE_EXIT line; condition 4 (markedDays proof); merge to
   feat/stage-b-stage2; post-merge bible unpiped; R5.7 whole-or-none;
   remaining batches. STOP at R5 close.
