# OPTION-2 STOP — RULING, 2026-08-06

Rules the reassessment in docs/R53_OPTION2_STOP_L16_RELAUNCH_2026-08-06.md.
Ruled by the review seat per RULE-DON'T-ASK; Sam holds a veto.

## Order — the mechanism is pinned FIRST

Question 3 (the replan's source is path-dependent) is a hypothesis. It
gets pinned by measurement before any architecture lands — the doc's
own warning ("proposing one before question 3 is pinned is how the
last cut got approved on a partial measurement") is adopted as the
instruction. The failure is deterministic, so the missing input is
findable: bisect the derivation's inputs between the two computations
(derive-from-original vs derive-from-accepted) until the diverging
input is named. Rotation/selection history is a candidate worth
checking early — a Lower Hinge <-> Prehab swap is selection-shaped —
but candidates are checked, not assumed.

## Question 4 — both branches ruled in advance; the pin picks one

- If the divergence traces to a MISSING PERSISTED INPUT — a fact,
  decision, or derivation input (e.g. rotation position) the
  declaration fails to carry — the answer is the north star's: carry
  THAT input and derive the week. Grounds: store only decisions;
  LR-26 (stored output inside a decision record: deleted, re-derived);
  the dateOverrides option-C precedent; fixture identity law. Storing
  the accepted sessions wholesale is REJECTED on this branch — it
  resurrects the stored-week class the rebuild exists to kill, the V1
  trap at larger scale.
- If the pin PROVES the accepted week is genuinely irrecoverable from
  any decision-shaped input — that derivation cannot be made
  deterministic over persistable inputs — that proof comes back as its
  own reassessment before anything is stored. Irrecoverability is
  demonstrated, never defaulted to.

## Standing constraints

- The L16 relaunch slice goes green WITHOUT being edited — it is the
  two-directional witness.
- Option 2's stale-contract correction is not revoked; the fix must
  keep it (a cancelled game never pays the week's bills again).
- Condition 4 and the merge stay BLOCKED until the branch prints
  BIBLE_TRUE_EXIT=0.
