# REDUCTION PROVENANCE RULING — 2026-08-06

Rules the condition-1 stop at c45792e9 (fourth door + the cells-5/6
Monday). Ruled by the review seat per RULE-DON'T-ASK; Sam holds a veto.

## The one rule — reductions are split by PROVENANCE

- A reduction backed by a LEDGER DECISION (illness, athlete's explicit
  override, injury episode) is a decision: it persists, and it carries
  its backing decision's id. Class B, exactly as condition 2 protected.
- A reduction derived from FACTS (fixture-derived: practice-match,
  game-load protection) DERIVES at read from the CURRENT facts and is
  never stored. Remove the game and the reduction is gone by
  construction — cell 1 (add-then-remove is identity) greens because
  nothing grafted survives the fact's removal.
- Every reduction carries typed provenance at the one owner: a backing
  decision id, or the fact key it derives from. A reduction with
  neither is a build error, gated — that closes the "nothing tells
  them apart" gap systemically, not per-door.

## Tier 4's authority, bounded

Tier 4 at read performs EXACTLY the reductions the current facts and
decisions author — no more (it never resurrects an athlete-emptied day;
last is not highest — the scaffold defect is adopted as law), and no
less. It evaluates against the derived contract, which now carries
only provenanced reductions.

## Cells 5/6 — the pin is resolved by measurement under this rule

Measure the |8 → |7 delta's provenance:
- If the reduction performing it is authored by facts PRESENT at that
  read (the game stands, practice-match rules apply), the pin is STALE
  — it captured the deriver's answer while tier 4 was missing. It is
  re-ruled to the conformant value by dated supersession doc, never
  edited silently.
- If it traces to anything not backed by current facts or a decision,
  the deriver must return |8, leg (ii) is corrected, and the pin
  stands unedited.

Either way the witness discipline holds: no cell edited without a
dated ruling naming the supersession.

## Then

Re-price all legs + the provenance rule against the full witness set
(condition 1 stands — green before building). If green: build in
order, then the run home unchanged (unpiped bible, condition 4, merge,
post-merge bible, R5.7, remaining batches). STOP at R5 close.
