# Switchover parked questions — Sam's rulings, 2026-08-05

Answers to the five PARKED FOR SAM items in
`docs/STAGE_B_STAGE2_SWITCHOVER_BOUNDARY_2026-08-05.md` §5. Recorded by the
review seat the session Sam ruled; committed by the terminal as authored.

**1. 'Two-Minute Repeats' — RULED: re-sign as plain `≈100% MAS`.** Same
shape and same reasoning as the 15:15 cell
(docs/MAS_CELL_RESIGN_RULING_2026-08-05.md): no function citation in a
signed cell; a point/plain prescription, athlete-facing derivation arrives
with MAS wiring. Workbook cell + equality-gated code cell ship together.

**2. Muscle/experience rows for the 53 templates — RULED: option (a),
batch signing session.** Evidence recorded: Sam's muscle/experience review
(2026-07-25) predates the workbook's final template names by two days — it
covers exactly 2 of the 55 ('MAS 15:15 Blocks', 'Erg EMOM'). Sam's memory
of having done the work is correct; the vocabulary moved under it. The
review seat has produced a PROPOSED sheet following the signed conventions
(conditioning rows: muscles empty, gate + note carry the content):
`docs/MUSCLE_EXPERIENCE_CONDITIONING_PROPOSED_2026-08-05.xlsx` — 53 rows,
proposed gates (run-only sprint families = one_plus_years with the ⚑
debate note mirroring the signed MAS 15:15 row; machine/erg = everyone;
aerobic capacity + flush = everyone; session formats mirror the signed
Erg EMOM note). NOTHING in it is signed until Sam returns it. The
`SELECTABLE_WITHOUT_METADATA` pin stays until the signed rows land.

**3. 'Up-Back Shuttle' — RULED: confirmed.** Later-authored-wins stands;
the 2026-07-25/27 workbook re-authoring supersedes the 2026-07-24 removal.
The test's reading is ratified.

**4. Warm-up copy — SIGNED (Sam, 2026-08-05, verbatim):**

> Warm-up — 5–10 min: start easy, raise the heart rate, then build into
> the movements you're about to train.

One athlete-visible string, every generated session's warm-up row; ships
through the copy sheet the usual equality-bound way; retires the walker's
L-P2 red over the bare `Warm-up` row. Richer per-quality warm-up copy is a
FUTURE signing batch (queued alongside the muscle sheet session) — not a
code decision.

**5. 'Team-Training Warm-Up Dose' — RULED: option (a), gate it.** A
warm-up dose is never a standalone session. Implemented as an AUTHORED
property on the workbook sheet (per the boundary report: a sheet property,
not a code filter), equality-gated into the code cell like every other
property; the sprint pool must exclude it by that property.
