# WEEK-ROW COMPOUND BUCKET RULING + BATCH 12 SIGNING (2026-08-08)

Sam's answers to the slice-2 boundary questions, verbatim, then the seat's
ruled interpretation of the one ambiguity. Refines the 2026-08-08 bucket
vocabulary ruling's week-row half (parked question 2, option none-of-the-
above — Sam supplied his own). Does NOT reopen the duplication ruling (a):
the timeline remains the single enumeration; no title restates it.

## 1. BATCH 12 IS SIGNED — Sam: "yep"

All seven strings, exactly as proposed: **"Today"**, **"Week"** (zoom
control) and **"Time"**, **"Away"**, **"Sick"**, **"Injured"**,
**"Equipment"** (chip labels). Update the copy sheet: batch 12 status
PROPOSED → SIGNED 2026-08-08, equality-bound both directions like every
signed batch.

## 2. CHIPS DO NOT LIGHT — Sam: "a"

Parked question 1 closed: leave as built. No chip shows active state;
Coach Notes below the row names every active fact in full. If lit chips
are ever wanted, that is its own later unit and its honest shape is the
projection answering "is this door's fact active" for all five.

## 3. WEEK ROWS SAY THE DAY'S BUCKETS — COMPOUND. Sam, verbatim:

> "on weekly view it should say whatever the bucket is that day i.e.
> Strength or strength + conditioning. On the daily it can get more
> granular and be like Upper body push and MAS work or whatever it is
> i think"

Built meaning:

- **Week view row = ALL of the day's buckets**, joined with " + ", in
  timeline order, each bucket word once: "Strength + Conditioning",
  "Strength" alone when that is all there is. The "+" joiner format is
  Sam's own, from the quote above.
- **Bucket words are the signed set unchanged**: Strength, Conditioning,
  Rest, Mobility, Accessories, Gunshow, Speed. Power still never appears —
  it folds under Strength before joining, so a power + conditioning day
  reads "Strength + Conditioning".
- **Rest / Practice Match / Game / Team Training rows unchanged**, exactly
  as the slice-2 tape held them.
- **The compound name is derived in the projection** (the words half,
  beside `bucket`/`headline`), never composed by a surface — the same
  L-P/one-name-per-session grounds as the original ruling.

## SEAT-RULED (Sam holds the veto): the day TITLE carries the same
## compound name; the daily granularity lives on the TIMELINE

Sam's "on the daily it can get more granular — Upper body push and MAS
work" is read as describing the daily view's EXISTING timeline, which he
already confirmed ("upper push on one line with conditioning below it is
good"), NOT as reverting the day title to variant names. Grounds, cited:

1. His own duplication ruling (a), signed the same morning: the card title
   stops restating what the timeline enumerates. A variant-worded title
   above a variant-worded timeline is exactly the defect he killed.
2. One name per session: the week row and the day title are two sightings
   of one day's name and must agree; both say the compound bucket name.

So: week row "Strength + Conditioning" → tap in → day title "Strength +
Conditioning" → timeline "Upper Push" / the conditioning variant, one per
line, as built. **If Sam meant the day title itself to go granular, he
says so and this section is superseded — the veto is his, one line.**

## NOT ORDERED HERE

Timeline row wording (whether the conditioning row should say "MAS work"-
level detail) is untouched — no order exists on it. The day SCREEN's
double-labelling (parked question 4) remains parked, its own order when
Sam wants it.
