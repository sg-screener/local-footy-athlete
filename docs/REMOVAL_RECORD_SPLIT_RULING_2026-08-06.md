# REMOVAL-RECORD SPLIT RULING — 2026-08-06

Rules the seventh condition-1 stop (pricing ae242d43): the tier-4
search enters with constraints=(none) because
resolveFinalVisibleSection18Week blanks userRemovalConstraints for a
signed application reason, starving the search's different question.
Ruled by the review seat per RULE-DON'T-ASK; Sam holds a veto.

## Ruling — one representation, two questions: SPLIT

- userRemovalConstraints stays what it is: the APPLICATION input,
  blanked once folded into composed workouts — the signed reason
  stands untouched (re-feeding would re-remove a bin's remainder).
- The surfaces gain removalDecisions: the typed RECORD of the
  athlete's removal decisions — never blanked, read-only, present at
  every depth of derivation. It answers explanation questions only:
  the search's stand-down ("does a decision explain this gap") reads
  it; nothing applies it.
- ONE composer populates both fields from the same source in the same
  breath — the surfaces composer that already owns the context — so
  the two can never drift (the drift would be a third representation,
  the exact disease).

## Conditions

1. The search stands down on the record; cells 15/16 recover — the
   deletion class's typed relocation (the restore-reversible
   ownership) runs before the search may green the week, per the
   constraint-visibility ruling's ordering law.
2. The application path is behaviour-identical: the blanking measured
   before and after, byte-for-byte on the composed weeks.
3. Condition 3's owed pin (hydration snapshot-vs-live) and condition
   4's clock fix (172 wall-clock stamps → one injected todayISO) land
   in the main build, tracked — neither inferred from green suites.
4. Then the final pricing re-runs on the full witness set. Green →
   build → run home (unpiped bible, condition 4 markedDays proof,
   merge, post-merge unpiped bible, R5.7, remaining batches). Not
   green → STOP with the residual named.
