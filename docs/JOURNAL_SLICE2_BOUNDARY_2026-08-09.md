# JOURNAL SLICE 2 + THE COPY GATE — BOUNDARY REPORT (2026-08-09)

LOOP CHECK: **`a green gate watching nothing` — SIGHTING 2, and it was the
mechanism behind the seat's `proposed-copy-ships-unlisted` sighting 2.** Sighting
1 was the copy-BINDING gate reading table rows only. This time BOTH copy gates
carried the identical hand-maintained scope list, so a whole new screen shipped
unseen while the extractor sat exactly on its ceiling and printed PASS.
**COMPRESSED, not iterated:** scope is now DERIVED from the source tree in both
gates, so the class cannot recur. Secondary: **`a cell that reds when its own
unit lands`**, in its rarer inverted form — the census red below was MINE, caught
by an existing gate.

Commits `a49daa80` (copy) and `a168b7bb` (slice 2).

---

## §1 WHAT IS ON HIS PHONE

A note box on the Journal tab: the design's own prompt, eight tag chips, a save
button, and this week's notes listed underneath. **The athlete can write about
their week and it survives a relaunch.**

## §2 ONE NEW INPUT — THE ONLY KIND THE NORTH STAR ALLOWS

A note is an **answer**: the athlete typed it. Nothing derived is stored — the
week count is read off the notes rather than kept as a counter, deliberately.

**A NOTE NEVER DERIVES PROGRAM STATE**, and that is now a gate rather than a
promise. Cell [5] sweeps `rules/`, `utils/`, `services/` and `hooks/` and
requires that **no engine module reads this store at all**. The design called
this non-negotiable; a comment saying so would have been worth nothing.

**ARMOURED AT BIRTH**, not retro-fitted — one door, both wipe refusals, the
quarantine boundary, guarded storage. The recipe's own lesson is that a store
grows writers around its door in the weeks before anyone gets to the armour.

**The tape carries counts only.** A note's text is the athlete's words about
their life; a cell asserts neither the text nor a tag reaches the serialized log.

**The tag vocabulary is closed** — base five plus the addendum's three. The NOTE
is free text; the TAGS are not, or the app grows an unauthored vocabulary on the
athlete's device.

**It joined all four registries in the same commit**: hydration gate, both
fresh-install reset paths, `DECISION_EVENTS`, and the census's persisted-store
ownership registry.

## §3 SLICE 1'S GATE WAS RE-POINTED, NOT DELETED

Slice 1 asserted the Journal reached **no** writer. True then, deliberately false
now. The cell asserts the SHAPE instead: exactly one door, and still no
transaction, no ledger append, no raw store write. **The forbidden list is
unchanged** — nothing was relaxed, one thing was named. "A cell that reds when
its own unit lands" is only half the hazard; the other half is a cell quietly
deleted because its unit made it red.

## §4 THE COPY DEFECT AND ITS COMPRESSION

**The defect was mine and the seat was right.** Slice 1's words shipped
**unlisted**, and its report cited "journal batch 13" — batch 13 is the bucket
vocabulary, batch 14 the compound name, and no journal batch existed. A citation
is a claim; that one pointed at someone else's work and nothing checked it.

**(a) Batch 15 recorded** — tab and headings, the five day-shape words with their
letters, the honest-absence sentences. **The H/M/E/G letters are called out as
terminal-chosen**: Sam ruled the five SHAPES, never that a shape shows as one
letter, and "M" for Moderate sits one letter from nothing. The composed count
lines are listed in TEMPLATE form and deliberately not put in a bound table row
(batch 14-b: a row that cannot fail is worse than no row).

**(b) The compression: gate scope is derived from the tree, in both gates.** An
opt-in list fails the same way every time — whoever adds a surface is whoever
would have to remember to add it, and if they remembered they would not have
needed the gate.

**The binder carried the same hole, and recording batch 15 exposed it.** It went
red with *"proposed to Sam and not in the app"* for strings plainly in the app,
on a screen it was not looking at. **A hand-scoped gate does not merely miss
things; it reports confident falsehoods about what it misses.**

**The extractor now spans lines** — the fix that file named as owed and deferred
as "a unit of its own". Four string shapes instead of two, adding the dominant
React idiom (a `<Text>` whose sentence sits on its own line), which had been
invisible since the extractor was written.

**Ceiling 130 → 561, UP, and attributed: +102 scope, +329 shape.** Not one of the
431 newly-counted strings is new — the instrument improved, the app did not get
wordier. The exact inverse of the 141 → 130 note. The ratchet is strictly
stronger afterwards and may only fall from here. Slice 2 then moved it 561 → 565,
and **that rise is the other kind** — four genuinely new sentences, recorded as
batch 16 before the commit landed.

**A parser that had been lying for five batches**, found on the way: the binder's
sub-heading regex matched only `### 5a.` while every batch from 10 on writes
`**15-a. …**`. Its own comment says the failure message must name the right
batch; it had silently stopped doing so.

## §5 REDS THE FULL CHAIN CAUGHT THAT MY MODEL DID NOT CONTAIN

Three, across two commits, none of which "a note box" or "a read-only screen"
would have suggested looking at:

1. **`test:legacy-census` LR-4 71 → 72** (slice 1) — the screen read the profile
   MIRROR. Its text says an undeclared hit is a new violation, not a miscount:
   **the violation was fixed, not the number.**
2. **`test:keyboard-convention`** (slice 2) — I rendered a raw `TextInput`. The
   shared owner handles avoidance and dismissal; a screen that rolls its own is a
   screen the keyboard can cover.
3. **`test:legacy-census` LR-2 0 → 1** (slice 2) — a new persisted store is
   **unowned by definition** until the registry declares its door. Fixed by
   declaring the owner.

That is the argument for the full chain over the suites I think are related,
three times in one day.

## §6 A PROCESS SLIP OF MINE, OWNED

`git add -A` swept `docs/JOURNAL_LOAD_MODEL_RULING_2026-08-08.md` — a Sam ruling
the seat had written and I had **not read** — into `a168b7bb`. The content was
correct to commit ("commit as authored"), and it did not change slice 2's scope
(its own sequencing line is "note input first"). But committing a document I had
not read is exactly the shape this repo has a law about: **a doc is a claim, and
I signed one unread.** Recorded here rather than quietly fixed, and the ruling is
now read, honoured in `journalWeek.ts` and reported below.

## §7 THE LOAD RULING, RECORDED

Sam's afternoon ruling supersedes the morning one **in part**: the signed
**2/1/0 weights are DEMOTED to the fallback rung, not retired**, and the
**day-shape derivation stands untouched**. The load slice builds three layers:
strength TONNAGE and conditioning sRPE as two native streams, combined **only in
ratio space** against each stream's own four-week normal, plus a region layer
feeding observation lines. `journalWeek.ts` now says so at the constant, so the
next reader does not mistake the floor for the model.

## §8 NORTH STAR — TOWARD

Slice 2 adds exactly one stored thing and it is an input. The gate that forbids
any engine module from reading it is the convergence-relevant part: it makes
"notes never derive program state" structurally true rather than conventionally
true.

## §9 NOT COVERED

- **NO DEVICE EVIDENCE.** The note box, the tag chips, the keyboard behaviour
  and the strip are unverified by eye. **No cell mounts the screen.**
- **RESURFACING IS NOT BUILT** — old notes at relevant moments. Named as owed by
  the order and by the design; not started.
- **Relaunch survival is asserted by hydration-gate membership, not by an actual
  process restart.** The quarantine predicate is registered and not exercised
  here.
- **The load comparison is still not built**; slice 1's honest state stands, and
  the new three-layer model is ruled but unbuilt.
- The order's literal ask — *"a string that resolves to NO sheet entry is a
  RED"* — is **not built**, reported rather than faked. It would red on ~565
  pre-existing strings, which is the declared stage-4 migration, not a gate.
- **Composed/dynamic sentences still have no literal to match** and are bound by
  their suites instead.
- The binder parses PROPOSED strings from batch 5's section onward, so batches
  are recorded but **not all equality-bound**; named as owed.
- All journal copy is **PROPOSED and unsigned** (batches 15 and 16).
- Depth (L13): 0 in both new suites.

## §10 L12 — WHAT CATCHES THE NEXT DEFECT OF THIS CLASS

The class this pass actually produced twice is **a gate whose SCOPE is narrower
than its claim**. Both copy gates, and both silently.

What catches the next one: **scope must be derived, and its derivation asserted
against the tree.** The new cell compares the set of scanned screen directories
against the set on disk and requires equality — so replacing the roots with an
enumerated list reds, which is the point: the defect was the enumeration, not the
entries. The same question is now worth asking of every source-scanning gate in
the repo — *what does this one NOT look at, and is that list hand-written?* — and
that is named as owed rather than swept today.
