# Verified programming candidate — phone installation held

Owner: programming-remedy. Installation is on hold until Sam approves. Neither
physical phone was installed, reset or wiped. No exercises or Estimated 1RM work
were added. The verified bb42e664 application and its original evidence remain.

Application candidate: `7eff7748cbd5ec1df9e5a6bb116bbf319f268d36`.
Evidence: `outputs/final-acceptance-2026-08-28/candidate-7eff7748/`.
This report supersedes only the remaining-work statements in
`PROGRAMMING_RELEASE_FOLLOWUP_2026-08-28.md`; earlier fixes and decisions stand.
Exact-version release: **23/23 units green, 26 current contracts**, including
real all-scope TypeScript checking and the three repaired diagnostics. The
release compiler-year witness passed **416/416 athlete-weeks across eight
distinct athletes**. A clean generic-device Release build succeeded and was
not installed. All 2378 tracked files match the candidate's Git blobs before
and after verification. `PROGRAMMING_PHONE_ACCEPTANCE_RECEIPT_2026-08-28.json`
preserves the receipt and binary/bundle hashes in Git; `verification.json` and
the complete logs/reports remain in the evidence folder. The later receipt/tape
save changes no application source or programming semantics.

## Changes at the existing owners

**Mobility icons.** `dayTimeline` now uses `optionalKindForPart`, the existing
typed component resolver already used by labels, then the shared icon mapping.
Mobility uses the existing person; Recovery retains the battery. No displayed
name inference, new glyph, programming change or completion write. The real
onboarding/addition witness covers 16 gender/base/addition coordinates, including
standalone Mobility, Strength + Mobility, Gunshow + Mobility and Mobility +
Recovery, misleading labels, separate completion, Undo and restart. The initial
run failed 24 icon assertions; the corrected run passed all 312 assertions.

**P07.** Displayed instructions now follow the actual typed conditioning option,
through the existing formatter and Session/Day projection. Machine variants say
easy active recovery instead of walk/walk-back or spin/paddle. Time-based machine
work retains its original distance as a clearly marked running equivalent.
Running walking recovery and complete rest are preserved. Stored prescriptions,
machine selection, rounds, dose numbers and recovery durations do not change.

The exact-candidate comparison inspects 404 displayed variants (54 eligible
templates, dose/equipment variants), not just required-field presence. There are
56 wording-changed variants across six distinct templates: 30 m Repeats, Erg
Short-Burst Repeats, 150–200 m Hard Repeats, 1 km Repeats, 400 m Repeats and
Extensive Tempo. Zero stored-dose, machine-selection, numeric or complete-rest
differences. Separately, all 55 catalogue entries and 110 stored dose variants
exactly match bb42e664, including the compatible retired Bodyweight Circuit.
`conditioningClarity` checks all templates, supported/limited machine sets,
actual Day/Session text and generated modality-change/restart journeys.

## Diagnostic cleanup, not changes to correct programming

| Diagnostic | Explicit disposition |
| --- | --- |
| Slot coverage | Deleted `applyPoolRotation` expectation retired. Rebound to existing block selection, current seven-slot split, typed intent and actual equipment. Original deficiency ceilings retained. Nordic omission and positive coverage separated per R-233. |
| Power counting | Gateway stripping/budget repair, universal order zero and July-golden equality expectations retired. Immutable historical golden retained behind an explicit historical option; updating it is refused. Current compiler budgets, counting fences, determinism and visible power-before-main-lift ordering remain tested. |
| Section 18 v2 | Incomplete fixture repaired with complete current onboarding answers. Existing expectations and eight mutation arms retained. |
| Law registry | Missing game-feedback reference rebound to the current scheduled/practice result journey; historical census entry 18 is no longer mislabelled a law. Still red for 21 of 211 distinct law rows marked UNENFORCED. This repo-wide process/verification debt is not waived, hidden or labelled a current programming defect. |

The first three diagnostics are now current release contracts. The existing four
C12 category failures were already repaired at the current typed owner; those
protections and four mutation checks remain. No deleted engine was restored.
The whole historical `test:bible` fleet is not claimed green.

The optional post-save `test:repo-law-guards` diagnostic reports 51 passed / 12
failed process assertions on both bb42e664 and the final shared checkout. The
same assertion names fail: historical report format, inbox/history/budget,
unreferenced old flows, golden-flow receipts, stopwatch writer and source-anchor
debt. The new tapes initially lacked discoverable run references; those were
added below and the comparison proves no new orphaned flow remains. Other
failure details match except the other seat's preserved, uncommitted NOW file:
24,667 bytes against a 24,576-byte budget; the committed candidate has 24,427
bytes. No application behaviour or test threshold was changed. Full classification:
`repo-law-comparison.json`. These 12 assertions are not the same counting unit
as the 21 unenforced law-registry rows.

## Native verification and automation classification

Native runs use an existing Debug simulator shell and candidate-equivalent
JavaScript, not a newly installed Release binary. Reached dates and actual
components are asserted. The separate clean Release build is not installed.

The original simulator attempts are retained. A wrong-target Maestro driver was
proved by the target screenshot versus driver process; the wrapper now refuses
cross-simulator driver conflicts before a run. Review controls must be centred
clear of the fixed footer, speed tiles expose combined title/subtitle labels,
and generation requires the visible Start button. Navigation scripts must use
selectable dates and cannot treat a successful tap command as proof of Day view.
These are instrument corrections, not reasons to change correct programming.

Re-runnable native entry points (use `scripts/dev-e2e/run-maestro-ios.sh` with
the explicit simulator UUID and `E2E_METRO_URL`; never a physical phone):

- `.maestro/visible/combined-mobility-icon-matrix.yaml`: all four Day/Session/reopen shapes.
- `.maestro/visible/combined-mobility-icon-week.yaml`: expanded Week cards.
- `.maestro/visible/combined-mobility-icons.yaml`: retained focused Strength + Mobility tape.
- `.maestro/visible/final-gminus1-decline.yaml`: warning refusal and reopening.
- `.maestro/visible/final-native-refusals.yaml`: occupied-day refusal and game protection.
- `.maestro/visible/onboarding-speed-answers.yaml`: dedicated disposable simulator only;
  two full journeys, including Review and reopening.

The pre-existing untracked `.maestro/visible/gminus1-add-undo-reopen.yaml` is an
unfinished draft, not delivered acceptance coverage. The final decline tape and
the prior bb42e664 accepted-Add/Undo evidence are the supported receipts instead.

Native icon checks pass for all four requested shapes: Day, Session, cold
reopening and actual expanded Week cards. Screenshots are under `native/`.
The combined Strength and Gunshow Day sections show the person; Mobility +
Recovery shows person and battery separately in Day and Session. Week summary
icons retain their existing aggregate role; expanded section headings do not
have separate glyphs. No new Week icon surface was invented.

The G−1 hard-conditioning warning was reached and declined; reopening preserved
the unchanged Gunshow day. An occupied Strength + Team Training day refused a
third session; Back → Back dismissed it and reopening retained the same day.
The actual Saturday game had no Add Session or session-options mutation door.
Receipts: `native-gminus1-decline.log`, `native-occupied-game-refusals.log`.
Two complete independent speed-answer onboarding journeys passed on the
dedicated disposable simulator: no-team-training selection, 2 km Skip, the
critical Review values, Acceleration only, Top-speed only, Generate, Start and
cold reopening. Both Review screenshots show the correct selected answer and
“Haven't tested.” After the second reopen, the actual native persisted profile
also contains Top-speed only and the canonical 2 km envelope with `seconds: null`.
The first storage reader expected a bare null; checking `recordTwoKmTime` showed
the existing envelope shape, so the reader—not the app—was corrected. Receipt:
`native-profile-after-reopen.json`. This does not close physical 2 km Skip QA.

The 39 selected behaviour mutants all landed and were caught (31 retained
injury/speed/power/Undo protections plus eight new icon/wording/result/ordering
mutants). All four retained category mutants and the actual-year wording mutant
were also caught. These are distinct mutation cases, not athlete counts.

## Delivered years and spacing

All four final years pass the strict audit: male/female × original partial kit
and corrected commercial gym, each 52 weeks / 364 athlete-days / 52 successful
weekly restart comparisons. That is 208/208 restart checks across four distinct
athlete/input worlds. There are no trainable painful-press rows in the specified
injury window, invalid flushes, retired automatic circuits, missing machine
identities, incorrect machine recovery wording, or invalid Landmine roles.

The identical-input comparison permits only eight changed displayed-note
occurrences (Extensive Tempo), with every other delivered program field exact:
exercises, doses, loads, rest, roles, placement and modifiers. All 23 actual
adjacent energy date-pairs per year remain the same. Reintroducing walking into
one actual delivered machine prescription makes the strict annual audit fail;
the original output is not modified. Receipts: `year-audit.json`,
`identical-input-year-comparison.json`, `year-wording-mutation.json`.

Browser review inspected rendered male commercial Week 8 (the reported Bike
wording) and female commercial Week 29 (actual flush machine order/8-minute
total and power-only Landmine/Primer placement). Report links are
`corrected-commercial-male-year.html`, `corrected-commercial-female-year.html`
and `year-comparison.html` in the evidence folder.

The prior spacing assessment remains the benchmark: 23 actual adjacent energy
date-pairs per year, not the older 15 same-part heading pairs. Five are easy to
hard, ten easy/controlled to easy/controlled, eight easy/controlled to Speed;
none hard to hard. Five exposures in seven days imply at least three cyclic
adjacencies in normal build. Six legal receiver alternatives were inspected;
moving adjacency introduced a four-day core streak or six core-active days.
Some adjacency is unavoidable; no individual pair is claimed compulsory.
The illness exception was assessed separately. Final comparison must confirm
the actual dates and prescriptions remain unchanged, not merely the total.

## Original P01–P22 status

“Implemented” below is not physical acceptance. No programming answer is pending
from Sam. Approval to install and subsequent phone acceptance remain separate.

| Items | Status |
| --- | --- |
| P01, P09, P14 | Implemented: structured prescriptions, actual machines and compatible retirement of the incomplete automatic circuit. Catalogue and actual-output comparison retained. |
| P02, P06, P16 | Implemented: single-leg priority; Leg Press/conventional Deadlift remain manual choices and suitable fallbacks. No change this pass. |
| P03 | Implemented: fixture-qualified automatic extras/manual Add. Native refusal results above delimit coverage. |
| P04 | Implemented: power rest retained in data, hidden in display. Four-year audit required. |
| P05, P10, P21 | Implemented/reviewed: selection history and spacing. Actual-pair classification above, not blanket “spacing solved.” |
| P07 | Wording fix implemented, regression/mutation-verified and reviewed in final delivered years; physical acceptance remains. |
| P08 | Implemented: advanced loaded-curl preference with legitimate fallback; preserved. |
| P11, P12 | Implemented: lift-name/machine identity and rollover/projection protection; annual restart checks required. |
| P13 | Shared injury authority, indirect support loading, explicit pain, replacements and simple flow implemented and previously natively verified. Existing Clear/Undo/restart protections retained; not physically accepted. |
| P15 | Distinct unmet acceleration/top-speed needs implemented without blanket in-season exclusion. Both native answer journeys, compiler and restart checks pass; phone acceptance remains. |
| P17 | Existing jumps remain eligible in the ordinary pool; no new hierarchy or exercise. |
| P18 | Agreed under-15-minute easy flush/G+2 optional-core/recovery-only rules unchanged. |
| P19 | Landmine selectable as power only, no strength slot/credit; unchanged. |
| P20 | Named final automated/native checks pass; physical acceptance remains. |
| P22 | Same-day duplicate exclusion, separate completion, Undo/restart and accumulated removal retained. Component-icon correction added without changing those semantics. |

## What catches the next defect

The icon witness resolves actual component IDs across accumulated combinations
and checks Day/Week and Session mapping, not labels. Conditioning checks compare
actual displayed work/recovery with typed machine identity and restart, not just
nonempty fields. The annual comparator permits only the independently reviewed
wording substitutions and rejects any other program/dose/placement change.
Mutation receipts must prove both injection and the intended failing assertion.
Native tapes prove the reached date, sheet result and reopened state.

## Remaining work and phone acceptance

- Sam: approve installation when ready. No installation is authorised by this
  report, and neither phone needs to be wiped.
- After approval, verify existing combined days on the phone: Mobility person,
  Recovery battery, correct Week summaries, Session view and reopening. Check
  machine conditioning wording while confirming unchanged work/rest.
- Physical acceptance remains for the existing programming fixes. Retain the
  separate 2 km Skip QA item; do not reset an existing phone to manufacture an
  onboarding test. No new programming/product answer is needed from Sam.
- The 21 unenforced legacy process/verification laws remain explicitly separate
  unresolved repo debt, not a waived or green historical test fleet.
  The 12 baseline process-check failures above remain recorded as well.

## NOT COVERED

- Physical iPhone acceptance or Release runtime on either phone; installation
  awaits Sam's approval. No wipe or reset is authorised.
- The recorded onboarding 2 km Skip physical-QA inconsistency remains open.
  Repeated successful simulator journeys do not substitute for phone acceptance.
- The 21 unenforced legacy process/verification laws and the entire historical
  diagnostic fleet; unrelated stale diagnostics are not silently promoted.
- Every athlete/availability combination, every possible alert route, clinical
  validation, new exercises or Estimated 1RM. The whole original list is not
  declared finished.
