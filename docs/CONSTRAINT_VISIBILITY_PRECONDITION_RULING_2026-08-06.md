# CONSTRAINT-VISIBILITY PRECONDITION RULING — 2026-08-06

Rules the fifth condition-1 stop: 5 of 12 gateway callers never pass
userRemovalConstraints, so the repair search relocates where a
decision explains the gap. Ruled by the review seat per
RULE-DON'T-ASK; Sam holds a veto.

## Ruling — the constraints stop being caller-threaded

Approved as a PRECONDITION UNIT, built and priced on its own before
the main pricing re-runs — and built systemically, not as five patched
call sites: the gateway resolves userRemovalConstraints ITSELF from
the one canonical persisted source in its evaluation context. The
twelve caller-threaded parameters retire. No caller can forget an
input it no longer supplies — the class dies, not the instances
(project law: systemic fix, never edge-case patches; the same family
as empty-pending-list-reads-as-approval).

## Conditions

1. Measured entry: the canonical source is reachable at all 12 sites.
   In generation paths that predate any removal, absence is
   legitimately empty — an EMPTY list from the canonical source is
   truth; an empty list from a forgotten parameter was the lie.
2. Ordering is law: a decision-authorised relocation (the deletion
   class's own, carrying typed ownership that makes restore
   reversible) runs BEFORE the search may green the week. The
   stand-down already built becomes reachable — cells 15/16's
   relocated ownership must reappear (19→17 recovers to 24).
3. The three migration lessons are keepers in the build: the :4688
   order is load-bearing (typed reduction LAST, never pre-search);
   only the lift travels, power counts as luggage; proofs measure
   tier 4's own contract.
4. Condition 2 of the search ruling (146 wall-clock stamps → one
   injected todayISO) remains owed in the main build, tracked, not
   inferred.
5. Then the final pricing re-runs on the full witness set. Green →
   build → run home. Not green → STOP with the residual named.
