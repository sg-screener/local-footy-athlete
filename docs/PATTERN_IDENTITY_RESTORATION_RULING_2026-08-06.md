# PATTERN IDENTITY — THE RESTORATION SIDE, RULED — 2026-08-06

Rules the eighth condition-1 stop (51b2cea6): deletion cells 21/22,
restoration — "who owns pattern identity" seen from the other side.
Completes docs/PATTERN_IDENTITY_RULING_2026-08-06.md. Ruled by the
review seat per RULE-DON'T-ASK; Sam holds a veto.

## Ruling — two owners, split by WHAT the identity is

- The DERIVER owns the identity of everything it PLANS (the
  pattern-identity ruling, unchanged): planned layout derives from the
  derived contract, stored planning output stays dead.
- The REMOVAL DECISION owns the identity of what it REMOVED. Sam has
  already ruled this: restoration.workout STAYS — real product
  behaviour (docs/LR26_LR27_RULINGS_2026-08-05.md, LR-28). What the
  athlete binned is part of the decision RECORD, captured at decision
  time; a restore brings back exactly that payload, and the week then
  re-derives AROUND it. That is not a stored output — it is what was
  decided about, the same way a ledger entry carries its own terms.

Leg (i) therefore proceeds with one boundary stated: it retires the
stored PLANNING week, and it must not touch decision payloads —
restoration.workout and its kin are inputs to derivation, not rivals
of it. Cells 21/22 green by construction when the restore path reads
the decision record and the deriver plans around the restored session.

## Also — undo symmetry (recorded law, not new)

Where the ledger returns to a prior state (a removal's removal, no
intervening decisions), determinism already guarantees identity —
add-then-remove is identity, remove-then-restore likewise. Where
intervening decisions changed the inputs, the re-derived week
legitimately differs around the restored payload; no cell may pin
otherwise without a dated ruling.

## Then

Re-price with leg (i) active under this boundary — full witness set,
including deletion 24/24 with mutation witnesses, accepted-state
full, fixture-identity 6/6. Green → build → land the two tracked
debts (hydration snapshot-vs-live pin; 146/172-stamp todayISO clock
fix) in the main build → run home (unpiped bible, condition 4
markedDays proof, merge, post-merge unpiped bible, R5.7, remaining
batches). Not green → STOP with the residual named.

## Precision note on the citation

Sam's LR-28 ruling, quoted exactly: the BEHAVIOUR is ruled permanent
("must give the athlete back what it displaced… restoring it is the
north star's own sentence"); the snapshot MECHANISM "stays for now",
with a queued census unit to replace the stored copy with
reference + derive-at-read, gated on an acted world reaching the
displace composition (L13/L11). This unit therefore treats the
snapshot as an untouchable input TODAY and changes nothing about it;
the queued unit remains queued with its entry gate intact. Nothing
here accelerates or supersedes it.
