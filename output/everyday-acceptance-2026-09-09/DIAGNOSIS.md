# Diagnosis of the walkthrough findings — 2026-09-09 (read-only; nothing changed)

Each section: verified cause → dependents → smallest complete correction → tests. Line numbers are at commit 326ef8ab.

## F3 — a skipped session reads "Session complete" / DONE (verified)

**Cause.** `src/screens/home/HomeScreenV2.tsx:821-822` (`renderDayRow`): `sessionLogged = decidingRows.length > 0 && decidingRows.every(e => e.completion !== null)`. `completion` is `'full' | 'partial' | 'skipped' | null` (`src/types/sessionOutcome.ts:7`), so "skipped" counts as logged. Saving with nothing ticked writes `'skipped'` for every component (`src/utils/sessionExecutionChecklist.ts:148,186-190`). That one boolean feeds `isCompleted` at `:2637` (`sessionLogged ?? feedbackReceipts.length > 0` — the fallback is equally skip-blind) which draws the Day card "Session complete" + View summary (`:2887-2903`) and the Week DONE badge (`:2466, :2511`). The row-level rule is already right (`:3445, :3288, :3027` use `full || partial`), so the athlete sees grey rows under a green "complete" header. Same defect one screen over: `src/screens/home/useDayWorkout.ts:142` `isAlreadyComplete = !!persistedReceipt && !justSaved` locks a skipped day read-only with a "Session complete" moment (`DayWorkoutScreenV2.tsx:2232-2239`).

**Dependents that must NOT change.** `src/utils/missedSessions.ts:88-96` (`componentAnswered`, `completion != null`) is deliberately "was it answered" — it suppresses the missed prompt after a skip and stays as is. `SessionCompleteMoment.tsx:70` already skips 'skipped'.

**Correction.** One owner in `src/rules/dayTimeline.ts`: `timelineEntryWorked(c) = c === 'full' || c === 'partial'` and `dayTimelineSessionCompleted(entries)` (same own-work/club fallback as today's lines 819-822). Use it at `HomeScreenV2.tsx:821`, drop the receipts fallback at `:2637`, replace the three inline `full || partial` checks, and make `useDayWorkout.ts:142` require `completion !== 'skipped'`. Consequence to confirm with Sam: a skipped day shows its normal tier badge and "Start Session" again (no SKIPPED badge exists in `SessionTierBadge`).

**Tests.** No test pins `sessionLogged` today (grep is empty). Add cells in `src/__tests__/dayFirstTimelineTests.ts` (all-skipped → false, one partial → true, unanswered → false, club-only skipped → false); extend `sessionExecutionChecklistTests.ts:524` with the negative (no `day-complete-*`, no `view-completed-session-button` after a skipped save); rewrite the already-red cell `explorerRenderedObservationUITests.ts:63-66` (it pins the pre-`sessionLogged` line — 13 passed / 1 failed today, a pre-existing red); glass cell in `.maestro/golden/day-card-dropdowns.yaml`.

## F2 — Day card prints raw names, no "/ side", other timed doses (verified)

**Cause (name).** `src/rules/projectVisibleWeek.ts:457-461` `rowName` → `signedCopy(exerciseNameCopyId(canonical))`, whose sheet text is registered at `src/rules/projectionCopy.ts:2259-2266` with `conditioningDisplayTitleForName(name)` — only the inner half of the real owner `formatExerciseDisplayName` (`src/utils/exerciseDisplay.ts:92-104`: aliases such as 'pissing dog against wall' → 'Wall Hip Opener', then conditioning titles, then acronym casing). The session screen uses the full owner (`DayWorkoutScreenV2.tsx:485-487`). Mobility rows bypass the projection entirely: `HomeScreenV2.tsx:3266/3324` print `exercise.name` from the raw pool.
**Cause (dose).** `prescriptionCopy` (`projectVisibleWeek.ts:395-437`) never reads `row.perSide` and emits `{min}-{max}s` for duration rows; the session's `formatStrengthSetsReps`/`formatLowLoadSetsReps` (`dayWorkoutHelpers.ts:263-279, 332-390`) append " / side" and print `sets × high-target`. Two formatters, one truth.

**Correction.** `projectionCopy.ts:2266` (and `:2176`, `:595`, `:2235`) register `formatExerciseDisplayName(name)` (a strict superset, keeps L-P2 signed-copy law); `HomeScreenV2.tsx:3266/3324` use `formatExerciseDisplayName(exercise.name)`; extend `prescriptionCopy` with per-side and timed sets×target ids registered beside `row.prescription.sets_distance` (`projectionCopy.ts:~1869`), mirroring `formatLowLoadSetsReps`. Keep the range ids for rows with no finite sets. Leave `:2247` COD names (R-331).

**Tests.** New agreement cell in `visibleSurfaceAgreementTests.ts` (card name/dose == session name/dose for every seeded row — the cell whose absence is the defect); exhibits in `exerciseDisplayTests.ts`; per-side + timed cells in `athleteVisibleProjectionTests.ts`; `projectionOwnershipTests` and `surfaceAgreementTests` L-P2 stay green; re-run `.maestro/visible/one-order.yaml` (row testIDs re-tokenise); pin one row's text in `day-card-dropdowns.yaml`.

## F14 — coach says "Game Day Saturday and Sunday" and "readiness recorded as flat" (verified input, model-side output)

**Verified.** The reply is model-generated: `CoachTabScreen.tsx:152-241` → `deriveCoachSnapshot` (`src/rules/liveAthleteSnapshot.ts:160-251`) → `buildCoachModelInput`/`projectCoachSnapshotForModel` (`src/rules/coachModelContext.ts:164-271`) → `supabase/functions/coach-chat/index.ts:141-174` (gpt-5.6-terra, message returned verbatim). The snapshot sent is CORRECT: one `kind:'game'` day (Sat 18, `dayKind` in `projectVisibleWeek.ts:223-227`), `thisWeek.kinds.games === 1`, Sunday `rest`; readiness `{state:'not_recorded', signal:null, factKind:'none'}` (`liveAthleteSnapshot.ts:253-258`; pinned by `coachSnapshotTests.ts:199-205`). The word "flat" is not in the model input; `coachingEngine.ts` has no 'flat' literal (my earlier guess was wrong).

**Why the mouth is free to invent:** (1) `evaluateCoachResponseContract` (`src/rules/coachResponseContract.ts:202-222`) accepts a SELF-DECLARED grounding receipt (`basis:['athlete_snapshot']`) and never compares a claim with a snapshot value; (2) the model payload carries bare ISO dates with no weekday and no explicit fixture list (`coachModelContext.ts:169-187`), so ISO→"Saturday/Sunday" is done by the model; (3) the prompt (`src/dev/coachLab/coachLabBrainPack.ts:31`) only covers the signal-present readiness case — nothing says "not_recorded means say nothing about a tier"; (4) `boundedConversation` (`coachModelContext.ts:84-98`) feeds the coach's own previous answer back and the authority order (`coachLabBrainPack.ts:38-44`) never ranks it below the snapshot, so turn 1's invention returns in turn 2; (5, contributory) retrieval seeds its query with `JSON.stringify(snapshot.readiness)` (`coachLabKnowledgeRetriever.ts:112-118`), pulling the Bible's readiness-tier rows (`docs/LFA_PROGRAMMING_BIBLE.md:3754, 5025`) precisely when nothing was reported. Hypothesis (no provider transcript in repo): that retrieval is where "flat" comes from.

**Why the guards missed it.** Every coach guard checks the input (`test:coach-snapshot`), the envelope (`coach-chat-integration` stub fetch, `coachCleanroom` census, contract schema/read-only) or mutation receipts (`coach-interpretation-receipt`, `coach-week-diff`); none compares reply text with snapshot facts, and the single lab fixture (`coachLabCases.ts:234-241`) is permanently `flat` with one game.

**Correction (AI-boundary projection + its gate, no phrase handlers).** `coachModelContext.ts`: emit `reported:false`/`'no_readiness_reported'` for a null signal; add per-day `weekday: weekdayName(date)` and a week-level exhaustive `fixtures`/`fixtureCount`. `coachLabBrainPack.ts`: three contract lines (no tier when nothing reported; `fixtures` is exhaustive and weekdays are supplied; recent turns are wording history, not facts). `coachResponseContract.ts` + `coach-chat/index.ts:156`: pass the snapshot into the contract and add `readinessClaimsGrounded` / `fixtureClaimsGrounded` refusals in the existing `READ_ONLY_FALSE_CHANGE_PATTERNS` shape, routed to `'refused'`.

**Tests.** `coachSnapshotTests.ts` (null signal → `"reported":false`, no tier word; `fixtures` == game days; weekday present); seed-level cell for `standard-in-season-week` (fixtureCount 1, Sun rest); `coachChatIntegrationTests.ts` born-red cells (stubbed replies "readiness is recorded as flat" / "Game Day Saturday and Sunday" → refused); contract unit cells; a second lab fixture with no readiness and one game in `coachLabCases.ts`; `coachLabRetrievalTests.ts` empty-readiness retrieval cell.

## NEW (Sam) — "No, skip it" on the missed-session notice does nothing (VERIFIED by experiment)

**Path.** Chip `HomeScreenV2.tsx:3931-3936` → `onSkip` at `:1285` (promise floated, no busy flag, no error surfaced) → `handleSkipMissedSession` (`src/screens/home/useHomeScreen.ts:1400-1421`) → `missedSessionSkippedFeedback` (`src/utils/missedSessions.ts:244-284`) → `createRecordSessionOutcomeIntentFromFeedback` → `commitSessionOutcomeTransaction` (`src/store/sessionOutcomeTransaction.ts:243-…`). Failure handling is `if (!result.ok) logger.warn(...)` at `useHomeScreen.ts:1420` — nothing reaches the athlete.

**Cause (verified headlessly, `scratch exp-skip.cjs`, standard seed with today = Wed 15 Jul).** `missedSessionSkippedFeedback` answers only ONE half of a day (`missedSessions.ts:260-262`: strength halves for kind 'session', club half for 'team_training'), but `normalizeIntent` demands an outcome for EVERY component (`sessionOutcomeTransaction.ts:449-457`: `incomplete_component_outcomes`). On a club night (components power, strength, team_training) both chips fail:
- Tue 14 'session' chip → payload [power:skipped, strength:skipped] → `ok=false code=incomplete_component_outcomes`, notice stays.
- Tue 14 'team_training' chip → payload [team_training:skipped] → same failure.
- Mon 13 (single door, power+strength) → `ok=true`, notice clears.
So the button is inert on exactly the days that show two prompts (Sam's "Thursday strength" is a club night). `detectMissedSessions` would clear the notice if the write landed (`componentAnswered`, `:84-92`). A second, smaller bug: `handleSkipMissedSession`'s deps are `[weekDays]` only (`useHomeScreen.ts:1421`), so `existing` feedback can be stale.

**Why no guard caught it.** `missedSessionPromptOwnershipTests.ts` is a source-regex suite; `missedSessionsTests.ts` cell [6] pre-answers the club half so the set is complete.

**Correction.** In `missedSessions.ts:266-284` emit a complete component set: the named half 'skipped', other halves carried from `existing` or explicitly `completion: null`; in `sessionOutcomeTransaction.ts:449-457` count only STATED outcomes (allow explicit null = "not answered", persisted unchanged — the asymmetry `componentAnswered` already reads). Do not fan the skip across all halves (that reintroduces the 2026-08-21 defect the helper's docblock records). Fix the deps array. Surface `!result.ok` to the athlete instead of a silent warn.

**Tests.** `missedSessionsTests.ts` [7a] two-door day, no feedback, skip 'session' → ok true, strength skipped, club unanswered; [7b] one notice (team_training) remains; [7c] mirror for the club chip; [7d] deps guard. 7a fails today with `incomplete_component_outcomes`.

## F1 — onboarding calendar wraps to six columns (verified numerically)

Both calendars are the same component `src/components/calendar/DateCalendarPicker.tsx:160-164` (`cell.width = `${100/7}%``). Yoga stores 14.285714… as float32 and breaks lines on a strict `>` (`node_modules/react-native/ReactCommon/yoga/yoga/algorithm/FlexLine.cpp:76-82`). Onboarding grid width = 402 − 2×20 (`OnboardingLayout.tsx:254-258`) = 362 pt → 7 × 51.7142868 = 362.00003 > 362 → wraps. The away sheet = 402 − 2×24 (`ui/Sheet.tsx:216-227`, spacing.lg) = 354 → fits. Any width ≡ 3, 5, 6 (mod 7) wraps, so other phones/paddings hit it too; the two Christmas-break calendars (`TeamTrainingDaysScreen.tsx:177,193`) are predicted to wrap the same way. **Correction:** render seven cells per row as explicit rows with `flex:1` (no percentage), or at minimum `width:'14.2857%'`. **Test:** a computable cell in `test:onboarding-presentation` that reimplements Yoga's float32 resolution and asserts 7 cells fit for every width 300–500 and for the two live container widths read from source.

## F9 — two cards painted over each other on the board (verified in code, not re-run)

`src/screens/home/WeekBoard.tsx:485-502` `onFinalize` sets `lifted.value = 0` unconditionally, but a dispatched drop returns 'held' and keeps its transform; `dragStyle` (`:504-528`) keys opacity/border/zIndex/elevation off `lifted`, so the held Game card turns 4 %-white with zIndex 0 over Sunday's real card while the fixture transaction runs (`HomeScreenV2.tsx:716-722` awaits it). Second factor: `weekBoardSnapTarget.ts:60-63` excludes games from preferring the open slot, so the game snaps onto the occupied Gunshow box (`rules/weekBoard.ts:255-257` allows a game to land on anything). Data was correct. **Correction:** only un-lift in `onFinalize` when the drop was not dispatched (reset `lifted` in `glideHome`), and let a game prefer the open slot. **Tests:** `weekBoardTests.ts` snap-target cell with a synthetic row.

## First tap ignored / counts stale 5–40 s (verified mechanism for the lag; tap loss = hypothesis with experiment)

Not a touch gate (no `pointerEvents`, no busy flag on those rows, `keyboardShouldPersistTaps` set, no debounced selector — all checked). The lag is real store latency: resolving an injury/travel fact runs one `runCoachMutationTransaction` that does `rebuildDerivedWorldNow()` + two full-context verify readbacks (`temporarySourceFactTransaction.ts:240-322`) on the JS thread; My Status reads `acceptedMaterialContext` live and it only changes when that lands. Amplifier on the same screens: `useResolvedWeekForDate` (`src/hooks/useSchedule.ts:163-176`) rebuilds the whole week projection on every render with no `useMemo`, invalidating `selectActiveCoachNotes` each time (`useActiveModifiers.ts:86-122`). Hypothesis for the dropped tap: JS-thread saturation during those blocks (a Pressable straddling a long block loses the responder), plus `SessionActionSheet.tsx:206-211` remounting its body one commit after opening (`epoch` key at `:328`). **Experiment:** log onPressIn/onPress on "Yes, remove it" and a severity row, tap 5 s after open, time `buildProgramTabProjectedWeek`. **Correction:** memoise `useResolvedWeekForDate`; derive `epoch` during render; keep the fade.

## F5 — no undo toast after a session removal (verified by reading; one-line glass experiment named)

The model chain is fine: `undoToastFor` (`src/rules/undoToast.ts:57-70`) returns a model for `plan_change/remove_session` in both scopes ("You removed a session", `journalChanges.ts:99-112`), pinned by `undoReversalTests.ts:278`. The cause is timing under a window: the ledger entry lands inside `commitPlanChange` (`PlanChangeSheet.tsx:423-425` → `planChangeProducer.ts:2258`) while the remove sheet — an RN `<Modal>` (`src/components/ui/Sheet.tsx:168-176`), not a navigation modal, so Home stays focused — is still up showing "…was removed… Done". `UndoToast.tsx:101-107` arms its 6 s timer at that instant; the toast lives behind the modal window (an in-screen zIndex can never rise above a Modal) and `setSeenEntryId` fires before the athlete taps Done, after which `undoToastFor` returns null for that entry for good. Every Day-card removal takes this path (`confirm_remove` has no `closeOnSuccess`, `PlanChangeSheet.tsx:1247-1256`). The Maestro flow taps Done in ~2 s so the guard stays green while a human never sees it; the flow's own comment (`.maestro/audit/bin-undo-toast.yaml:20-27`) records the six-second life behind the sheet. **Experiment:** tap Done within 2 s of "Yes, remove it" — the toast should appear. **Correction:** in `Sheet.tsx` expose `useAnySheetOpen()` (one module counter, `useSyncExternalStore`); in `UndoToast.tsx` treat "covered" as a third state: don't arm the timer or render while covered, re-arm when the sheet closes; do NOT route it into the unfocused branch (which advances the seen marker). **Tests:** the Maestro flow with an 8 s wait before `plan-change-done` (red today); a pure `undoToastCountdownArmed({hasModel,isFocused,covered})` cell; add `scope:'strength'` to cell 11b.

## F7 — swapped row renders one step late (verified reading; latency, not staleness)

The mobility rows are re-derived from the live ledger on every render (`DayWorkoutScreenV2.tsx:856-864` with a fresh `useAthleteContext()` object as a dep) and the chained-swap projection is correct (`derivedExerciseDecisions.ts:82-99`); dl-5's `fromExercise: "Horse Stance Hold"` proves the tree HAD taken dl-4 before the second tap. What the athlete sees is latency: a derived swap writes nothing to the program (`programControlActions.ts:695-703`) yet `mutate` still calls `rebuildDerivedWorldNow()` (`programControlActions.ts:2218` → full regeneration + ledger replay, `quiescentBoot.ts:722-820`) synchronously, after two AsyncStorage reads (`coachMutationTransaction.ts:211-212`), and the ↻ has no in-flight state. **Correction:** skip the world rebuild for derived-only edits (guard at `:2218`) and disable the row's ↻ while its swap is awaited. **Tests:** chained-swap cell in `quickExerciseActionsTests.ts`; a counter cell proving a derived swap performs no regeneration (red today); Maestro cell waiting ≤1.5 s for the new name.

## F6 — a whole-day removal rewrites today's logged session (verified)

"I also rebalanced Monday" is honest projection of `repairedDates` (`planChangeProducer.ts:2869`; `acceptedStateTransaction.ts:2751-2789`: any other day whose visible signature changed). The seat comes from `completeWeeklyCore` (`src/rules/canonicalWeeklyPlaneCompletion.ts:259`, via `weeklyLegCoverage.ts:122`): lower-strength days sort first (`:166`), so Monday `sched:2026-07-13:1:lower` is candidate #1 for the week's core row. Root cause: of the four callers, only `buildFixtureMinimalReplan` (`src/utils/fixtureMinimalReplan.ts:1301, :1312` — the athlete bin/move and fixture-move repair) passes NO `placeableFromISO` history boundary; the generator (`canonicalWeeklyRowCompiler.ts:585, :982`) and the injury compiler (`canonicalWeeklyInjuryCompiler.ts:274-286`) do. No repair path reads `sessionFeedback` completion at all (grep), and the repair stub declares `todayISO: args.weekStart` (`acceptedStateTransaction.ts:1935`). The governed-boundary owner (`stampSection18GovernedBoundary`, `weeklyExposureContractV2.ts:314`; enforced by `section18AcceptedWeekGateway.ts:576-600`) only learns boundaries from source facts (`canonicalWeeklySourceFactCompiler.ts:121`), never from a recorded outcome. The badge vanishes because `sessionExecutionChecklist.ts:226` requires `recorded.length === section.items.length`; one inserted row → component completion null → `sessionLogged` false. Dependents: fixture-move replan, athlete bin/move/add, and boot replay of either (`acceptedStateTransaction.ts:2296/1713/2039/2965-2979/3080/2464`). **Correction:** derive a delivered-day boundary (day after the latest logged date in the week, floored at week start, capped at today) at `stageAthleteMutationConstraint`/`buildFixtureProjection`; pass it as `placeableFromISO` at both `fixtureMinimalReplan.ts` sites; stamp it via `stampSection18GovernedBoundary` so the gateway refuses any writer that reaches a logged day. **Tests:** sibling of `injuryLimitedKitTests.ts:100`: log Monday (skipped and full), bin Fri whole_day → Monday byte-identical, no "rebalanced" clause; then move the game and re-assert; a checklist cell for a section that gains a row after logging. Suites: athlete-session-deletion, fixture-mutation-transaction, day-first-timeline, injury-limited-kit, midline.

## F8 — the re-placed Gunshow has zero exercises (REPRODUCED headlessly; removal-fact meaning verified)

Reproduced with no removal and no move: `spent-week-friday` day 5 → `Gunshow, optional, rows 0`; isolated to `buildDerivedSession('arms_pump', '2026-07-25', …)` → 0 rows. Mechanism: `sessionBuilder.ts:1182-1183` overrides the caller's kit with `equipmentTagsOnDate` (`canonicalWeeklyAvailabilityState.ts:84`), which is `['bodyweight']` on any non-gym day; on bodyweight the three Gunshow families filter to nothing (the one bodyweight curl is banned by `GUNSHOW_AUTOMATIC_EXCLUDED_IDS`, `exercisePools.ts:247`); `finaliseDerivedSession` (`sessionBuilder.ts:1460`) returns a named, optional, empty Workout. The R-130 placer (`weeklyScheduler.ts:2045-2060`) puts the Gunshow on G−1 whenever it is a rest day — precisely the days with no kit — and never asks about equipment. Standard seed: G−1 was Fri 17 (gym day, rows present); after the move G−1 = Sat 18 (non-gym) → empty. The fixture-move path recomposes correctly via `fixtureMinimalReplan.ts:425-464`, calling the same builder. `optionalTopUpPlacement.ts:133-136` already holds the rule "an empty composition is no session" — R-130's placement is the one optional placer without it. The athlete's removal is date-scoped by construction: `UserRemovalConstraint.targetDate` + id `user-removal:<date>:<scope>:<planEntryId>` (`userRemovalConstraints.ts:27-32`), so re-placing on the 18th after binning the 17th is lawful today; "never offer me a Gunshow" would be a different fact. **Correction:** (1) drop, don't render — apply the top-up rule at `defaultProgram.ts:2260` and `fixtureMinimalReplan.ts:459-462`; (2) gate the R-130 offer on the G−1 date being trainable (`equipmentTagsOnDate(profile, g1Date)` beyond bodyweight — Primer is bodyweight-legal, needs Sam's word); (3) copy in `gameChangeCoachNotes.ts` saying the Gunshow moved with the game. **Tests:** `optionalTopUpTests.ts` Gunshow sibling + builder cell; dev-E2E witness "no seed day has a zero-exercise session" (red today on spent-week-friday); `g1MoveDurabilityTests`/move-game-relaunch cell (bin Fri, move game, every Sat session ≥1 row). Also `gunshowSlotCount` returns 0 under a severe shoulder/elbow/wrist/neck injury — same empty card by a second route.

## F13 — reopen refused after away→return (partly verified; not reproduced headlessly)

Verified by reading: `coach-updates` is the only durable home for a fixture-move constraint (`programStore.ts:341-379` persists no `activeConstraints`; `coachUpdatesStore.ts:874-878` + `compatibilityPersistence.ts:9-11`); "Schedule is back to normal" routes `clear_adjustment` → `clear_fatigue_status` (`programControlActions.ts:1449-1461, 2087-2129`) → `transactTemporarySourceFact resolve` → `commitTemporarySourceFactSet` (`temporarySourceFactTransaction.ts:240-320`) whose `mutate` runs `rebuildDerivedWorldNow()`; that rebuild slates `activeConstraints = []` and recomposes from facts only (`quiescentBoot.ts:708, 746, 766` — `composeTemporarySourceFactCompatibility` called without the pre-slate constraints although `temporarySourceFact.ts:1173-1180` would preserve non-fact ones if given them); the mirror subscriber then republishes the accepted set over `coach-updates` (`coachUpdatesStore.ts:940-957`). Two silencers exist on the path (`asyncStorageCompat.ts:30-37, :119`: writes dropped during ledger replay or a write stage). And the plain-seed checkpoint stores the MEMORY fingerprint map (`DevE2ESeedCoordinator.ts:423-435`) while the relaunch compares DISK (`:868-871`) — the scenario checkpoint (`:512-576`) stores the persisted map; the refusal wording "expected=<memory> actual=<persisted>" is that asymmetry. **Experiment run (`exp-f13.cjs`):** seed → move game → "Game moved" in accepted, memory and disk → away (bodyweight) → resolve the travel fact through `transactTemporarySourceFact` → "Game moved" survived in all three; `droppedDurableWrites` unchanged. So the simplest erasure hypothesis did NOT reproduce with that door. Not replicated: the UI's `clear_adjustment` program-control route, the earlier injury report+clear and tired fact in the same world, and the equipment fact — the on-device sequence. Status: on-device fact stands (persisted `activeConstraints` empty at refusal, `device-state-at-j6c-refusal.json`), cause still ranked hypotheses (late write after checkpoint / write-stage swallow on an early-return branch / the UI route). **Correction candidates:** pass the pre-slate constraints into `composeTemporarySourceFactCompatibility` at `quiescentBoot.ts:708` (harmless, closes the class); make the plain checkpoint store the persisted map like the scenario one (two lines, `DevE2ESeedCoordinator.ts:423-435`). **Tests:** `devE2ECoordinatorTests.ts` cell where memory and disk differ by one key (fails today: the fake returns the same object); `coachUpdatesOwnershipTests.ts` cell (game_change + travel fact, resolve, constraint survives in three places); `durableWriteNeverSilentTests.ts` cell asserting zero dropped `coach-updates` writes across the UI clear route. Next diagnostic step if Sam wants it: replay the exact on-device sequence (injury→clear, tired, away, clear via `executeProgramControlActionDurably` clear_adjustment) headlessly with the three-place readout.
