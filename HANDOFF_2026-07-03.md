# Handoff — Friday 3 July 2026

For a fresh Claude session. Mount the parent folder
`/Users/samgeurts/Documents/local-footy-athlete` (the LFA subfolder is empty).
Read `AGENTS.md` and `CLAUDE.md` before changing anything.

## Where the project is

The coach-revision migration (snapshot → propose → validate → apply → verify)
is COMPLETE and live-verified — zero false "Done"s across all testing. The
conditioning overhaul v1 shipped and passed its live gates this morning:
8 registry templates (6 easy/flush any week, 2 hard work-capacity unlocked
only on bye weeks), bye detection automatic, validator-enforced
(`template_bye_week_only`). A real bug found live got a systemic fix: fresh
and resume paths now share ONE confirmation-transaction owner
(`enterCoachRevisionConfirmationTransaction`, commit `8aa98bf`, test [26]).

## The new direction (Sam's pivot, signed off today)

**The plan is the interface.** Instead of chat-first free text, athletes tap
a day and pick from a menu of legal changes. Design + signed-off decisions in
`ATHLETE_CHANGE_VOCABULARY.md`: athlete language ("I'm cooked", "I'm
injured"); feedback auto-prompts after sessions; edit horizon = this week +
next 2 (view-only beyond); chat folds into the sheet as "something else…".

**Phase 1 is BUILT but NOT yet live-verified** (committed `ccfa70b` +
`6349428`):

- `src/utils/coachRevisionPolicy.ts` — policy extracted to ONE shared owner
  (dates, template signatures, bye unlock). Both doors import it.
- `src/utils/planChangeProducer.ts` — deterministic producer: lists only
  legal options per day, builds the same CoachRevisionProposal shape as the
  LLM, applies through the same writer. Core invariant (test-enforced, 49
  tests): the menu can never offer what the validator would reject.
- `src/screens/home/PlanChangeSheet.tsx` + wiring in `HomeScreenV2.tsx` —
  "Want to change something?" / "Add a session?" links on the today hero and
  expanded day rows.

**IMMEDIATE NEXT STEP: live-verify Phase 1 in the Simulator** (needs Metro
restart first — see workflow below): tap a day → sheet → swap / bin / move /
add; confirm hard templates only appear on bye weeks; check "something else"
hands off to chat with day prefill; verify the board after each change.

## Then, in rough order

1. Vocabulary Phase 2: game add/move/remove already exists on this screen
   (GameDaySheet, calendar marks) — link cleanly from the sheet.
2. Phase 3: feedback auto-prompt after sessions (SessionFeedback store
   exists end-to-end); "I'm injured" / "I'm cooked" flows
   (applyAdjustmentEvents machinery exists).
3. `RUNNING_RULES_PLAN.md` — approved direction, Option B (week-level
   running-budget owner + first post-generation verification). BLOCKED on
   Sam's answers to its section 5 (esp. do team/game days count toward the
   4-run cap?). Note: a 4-run cap already exists inline
   (sessionResolver.ts:1251) but only counts Pass-2 placements.
4. Backlog: patch-shape output (14s chat latency lever, parked); move v2
   (occupied-destination merges); Stage 4 default-flip + Stage 5 legacy
   deletions; Tempo Run/MetCon/6x1km stub metadata cleanup (low value, high
   blast radius, deliberately skipped).

## Workflow facts (hard-won, do not relearn)

- **Metro NEVER sees Claude's file edits.** No hot reload, no cmd+R pickup.
  Batch all edits + tests, then ask Sam to restart Metro (`Ctrl+C`,
  `npx expo start --clear`) — one restart per verification round. Check
  `buildFingerprint` in turn_start logs (template count/ids) before trusting
  any live result.
- Simulator = full computer-use tier; Terminal = click/read-only. Type into
  the Simulator ONLY via write_clipboard + cmd+V, zoom-verify before send.
- Supabase deploys + Metro restarts need Sam's terminal (sandbox blocks
  supabase.co and the CLI).
- Tests: `npx sucrase-node src/__tests__/<file>.ts` (ts-node unavailable).
  Suites: controller 154, proposal 77, writer 34, matrix 34, producer 49.
  Typecheck: `npx tsc --noEmit`.
- Git: commit with `-c user.name="Sam Geurts" -c user.email="geursamuel@gmail.com"`,
  then `rm -f .git/*.lock` (sandbox artifact). Remind Sam to `git push`
  (remote: github.com/sg-screener/local-footy-athlete, private).
  **Unpushed right now: `667eb8b`, `ccfa70b`, `6349428`** (+ this handoff).
- Session-end habit: append a plain-English entry to `DEV_DIARY.md` that
  Renee (Sam's partner, non-technical) can read. Not yet written for the
  afternoon session — cover the pivot + Phase 1 when it live-passes.

## Guardrails (standing)

- No phrase/regex patches — systemic fixes only; architecture reassessment
  if the same bug class appears twice (see repo CLAUDE.md).
- Don't touch program generation / coaching philosophy without a signed-off
  plan (Sam likes main lifts on team training days — leave that alone).
- Reliability > latency. A safe refusal beats a confident wrong edit.
- No API keys in code or chat; secrets live in deployed Supabase secrets.

## Key docs

`ATHLETE_CHANGE_VOCABULARY.md` (the pivot), `RUNNING_RULES_PLAN.md` (awaiting
answers), `CONDITIONING_SESSION_INVENTORY.md` (session catalogue + overhaul
record), `AI_COACH_ARCHITECTURE_REVIEW.md` + stage plan docs (history),
`DEV_DIARY.md` (Renee-readable log).
