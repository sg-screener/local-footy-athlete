/* Retired tag-selector expectations are not a current programming contract.
 * This historical entry now executes the real-onboarding canonical picker,
 * accepted edit and restart witness. The old selector is test-only history.
 */
require('sucrase/register');
require('./canonicalStrengthTemplateTests.ts');
