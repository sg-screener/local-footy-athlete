node_modules/
.expo/
.claude/
dist/
.DS_Store
artifacts/
supabase/.temp/
*.bak
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.env
.env.local

# Test-harness build output — regenerated, never committed. The maintained
# test scripts run TypeScript directly via sucrase-node; this only appears if
# the legacy tsc compile is invoked manually.
.qa-compiled/
